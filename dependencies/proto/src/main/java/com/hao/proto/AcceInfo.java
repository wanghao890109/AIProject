// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.AcceInfo in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 用户状态(装饰)和道具(背包系统),GiftInfo->UserDeco->DecoInfo
 */
public final class AcceInfo extends Message<AcceInfo, AcceInfo.Builder> {
  public static final ProtoAdapter<AcceInfo> ADAPTER = new ProtoAdapter_AcceInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 装饰类型 1、等级挂件 2、头像挂件 3、公屏气泡、4、进房动效、5、房间背景
   */
  public int acceType;

  /**
   * 装扮Id
   */
  public int acceId;

  /**
   * 特效名字
   */
  public String acceName;

  /**
   * 是否在用，0, 1在用，2、下架
   */
  public int status;

  /**
   * 特效类型, 0, 1普通，2、SVG
   */
  public int showType;

  /**
   * 特效地址
   */
  public String showUrl;

  /**
   * 上架时间
   */
  public long stTime;

  /**
   * 下架时间
   */
  public long edTime;

  public AcceInfo(int acceType, int acceId, String acceName, int status, int showType,
      String showUrl, long stTime, long edTime) {
    this(acceType, acceId, acceName, status, showType, showUrl, stTime, edTime, ByteString.EMPTY);
  }

  public AcceInfo(int acceType, int acceId, String acceName, int status, int showType,
      String showUrl, long stTime, long edTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.acceType = acceType;
    this.acceId = acceId;
    if (acceName == null) {
      throw new IllegalArgumentException("acceName == null");
    }
    this.acceName = acceName;
    this.status = status;
    this.showType = showType;
    if (showUrl == null) {
      throw new IllegalArgumentException("showUrl == null");
    }
    this.showUrl = showUrl;
    this.stTime = stTime;
    this.edTime = edTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.acceType = acceType;
    builder.acceId = acceId;
    builder.acceName = acceName;
    builder.status = status;
    builder.showType = showType;
    builder.showUrl = showUrl;
    builder.stTime = stTime;
    builder.edTime = edTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", acceType=").append(acceType);
    builder.append(", acceId=").append(acceId);
    if (acceName != null) builder.append(", acceName=").append(Internal.sanitize(acceName));
    builder.append(", status=").append(status);
    builder.append(", showType=").append(showType);
    if (showUrl != null) builder.append(", showUrl=").append(Internal.sanitize(showUrl));
    builder.append(", stTime=").append(stTime);
    builder.append(", edTime=").append(edTime);
    return builder.replace(0, 2, "AcceInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<AcceInfo, Builder> {
    public int acceType;

    public int acceId;

    public String acceName;

    public int status;

    public int showType;

    public String showUrl;

    public long stTime;

    public long edTime;

    public Builder() {
      acceType = 0;
      acceId = 0;
      acceName = "";
      status = 0;
      showType = 0;
      showUrl = "";
      stTime = 0L;
      edTime = 0L;
    }

    /**
     * 装饰类型 1、等级挂件 2、头像挂件 3、公屏气泡、4、进房动效、5、房间背景
     */
    public Builder acceType(int acceType) {
      this.acceType = acceType;
      return this;
    }

    /**
     * 装扮Id
     */
    public Builder acceId(int acceId) {
      this.acceId = acceId;
      return this;
    }

    /**
     * 特效名字
     */
    public Builder acceName(String acceName) {
      this.acceName = acceName;
      return this;
    }

    /**
     * 是否在用，0, 1在用，2、下架
     */
    public Builder status(int status) {
      this.status = status;
      return this;
    }

    /**
     * 特效类型, 0, 1普通，2、SVG
     */
    public Builder showType(int showType) {
      this.showType = showType;
      return this;
    }

    /**
     * 特效地址
     */
    public Builder showUrl(String showUrl) {
      this.showUrl = showUrl;
      return this;
    }

    /**
     * 上架时间
     */
    public Builder stTime(long stTime) {
      this.stTime = stTime;
      return this;
    }

    /**
     * 下架时间
     */
    public Builder edTime(long edTime) {
      this.edTime = edTime;
      return this;
    }

    @Override
    public AcceInfo build() {
      return new AcceInfo(acceType, acceId, acceName, status, showType, showUrl, stTime, edTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_AcceInfo extends ProtoAdapter<AcceInfo> {
    public ProtoAdapter_AcceInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, AcceInfo.class, "type.googleapis.com/haiyaa.AcceInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(AcceInfo value) {
      int result = 0;
      if (!Objects.equals(value.acceType, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(1, value.acceType);
      }
      if (!Objects.equals(value.acceId, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.acceId);
      }
      if (!Objects.equals(value.acceName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.acceName);
      }
      if (!Objects.equals(value.status, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.status);
      }
      if (!Objects.equals(value.showType, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(6, value.showType);
      }
      if (!Objects.equals(value.showUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(7, value.showUrl);
      }
      if (!Objects.equals(value.stTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(11, value.stTime);
      }
      if (!Objects.equals(value.edTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(12, value.edTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, AcceInfo value) throws IOException {
      if (!Objects.equals(value.acceType, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 1, value.acceType);
      if (!Objects.equals(value.acceId, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.acceId);
      if (!Objects.equals(value.acceName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.acceName);
      if (!Objects.equals(value.status, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.status);
      if (!Objects.equals(value.showType, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 6, value.showType);
      if (!Objects.equals(value.showUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 7, value.showUrl);
      if (!Objects.equals(value.stTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 11, value.stTime);
      if (!Objects.equals(value.edTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 12, value.edTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public AcceInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.acceType(ProtoAdapter.INT32.decode(reader)); break;
          case 2: builder.acceId(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.status(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.acceName(ProtoAdapter.STRING.decode(reader)); break;
          case 6: builder.showType(ProtoAdapter.INT32.decode(reader)); break;
          case 7: builder.showUrl(ProtoAdapter.STRING.decode(reader)); break;
          case 11: builder.stTime(ProtoAdapter.INT64.decode(reader)); break;
          case 12: builder.edTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public AcceInfo redact(AcceInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
