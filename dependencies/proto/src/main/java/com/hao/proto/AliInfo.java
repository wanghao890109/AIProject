// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.AliInfo in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 支付宝信息
 */
public final class AliInfo extends Message<AliInfo, AliInfo.Builder> {
  public static final ProtoAdapter<AliInfo> ADAPTER = new ProtoAdapter_AliInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 人员
   */
  public long userId;

  /**
   * 真实姓名, 必填
   */
  public String realName;

  /**
   * 支付包账号，提现时必填, 身份认证时不填
   */
  public String aliId;

  /**
   * 身份证编号，选填
   */
  public String idCard;

  /**
   * 发起时间，选填
   */
  public long ctTime;

  /**
   * 最新进度时间，选填
   */
  public long upTime;

  public AliInfo(long userId, String realName, String aliId, String idCard, long ctTime,
      long upTime) {
    this(userId, realName, aliId, idCard, ctTime, upTime, ByteString.EMPTY);
  }

  public AliInfo(long userId, String realName, String aliId, String idCard, long ctTime,
      long upTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    if (realName == null) {
      throw new IllegalArgumentException("realName == null");
    }
    this.realName = realName;
    if (aliId == null) {
      throw new IllegalArgumentException("aliId == null");
    }
    this.aliId = aliId;
    if (idCard == null) {
      throw new IllegalArgumentException("idCard == null");
    }
    this.idCard = idCard;
    this.ctTime = ctTime;
    this.upTime = upTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.realName = realName;
    builder.aliId = aliId;
    builder.idCard = idCard;
    builder.ctTime = ctTime;
    builder.upTime = upTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    if (realName != null) builder.append(", realName=").append(Internal.sanitize(realName));
    if (aliId != null) builder.append(", aliId=").append(Internal.sanitize(aliId));
    if (idCard != null) builder.append(", idCard=").append(Internal.sanitize(idCard));
    builder.append(", ctTime=").append(ctTime);
    builder.append(", upTime=").append(upTime);
    return builder.replace(0, 2, "AliInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<AliInfo, Builder> {
    public long userId;

    public String realName;

    public String aliId;

    public String idCard;

    public long ctTime;

    public long upTime;

    public Builder() {
      userId = 0L;
      realName = "";
      aliId = "";
      idCard = "";
      ctTime = 0L;
      upTime = 0L;
    }

    /**
     * 人员
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 真实姓名, 必填
     */
    public Builder realName(String realName) {
      this.realName = realName;
      return this;
    }

    /**
     * 支付包账号，提现时必填, 身份认证时不填
     */
    public Builder aliId(String aliId) {
      this.aliId = aliId;
      return this;
    }

    /**
     * 身份证编号，选填
     */
    public Builder idCard(String idCard) {
      this.idCard = idCard;
      return this;
    }

    /**
     * 发起时间，选填
     */
    public Builder ctTime(long ctTime) {
      this.ctTime = ctTime;
      return this;
    }

    /**
     * 最新进度时间，选填
     */
    public Builder upTime(long upTime) {
      this.upTime = upTime;
      return this;
    }

    @Override
    public AliInfo build() {
      return new AliInfo(userId, realName, aliId, idCard, ctTime, upTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_AliInfo extends ProtoAdapter<AliInfo> {
    public ProtoAdapter_AliInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, AliInfo.class, "type.googleapis.com/haiyaa.AliInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(AliInfo value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.realName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.realName);
      }
      if (!Objects.equals(value.aliId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.aliId);
      }
      if (!Objects.equals(value.idCard, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.idCard);
      }
      if (!Objects.equals(value.ctTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(8, value.ctTime);
      }
      if (!Objects.equals(value.upTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(9, value.upTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, AliInfo value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.realName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.realName);
      if (!Objects.equals(value.aliId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.aliId);
      if (!Objects.equals(value.idCard, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.idCard);
      if (!Objects.equals(value.ctTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 8, value.ctTime);
      if (!Objects.equals(value.upTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 9, value.upTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public AliInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.realName(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.aliId(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.idCard(ProtoAdapter.STRING.decode(reader)); break;
          case 8: builder.ctTime(ProtoAdapter.INT64.decode(reader)); break;
          case 9: builder.upTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public AliInfo redact(AliInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
