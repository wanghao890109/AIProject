// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.AppleAuditing in haiyaa.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 配置类信息
 */
public final class AppleAuditing extends Message<AppleAuditing, AppleAuditing.Builder> {
  public static final ProtoAdapter<AppleAuditing> ADAPTER = new ProtoAdapter_AppleAuditing();

  private static final long serialVersionUID = 0L;

  /**
   * 苹果是否版本审核中，端上采取特殊审核策略
   */
  public boolean audit;

  public AppleAuditing(boolean audit) {
    this(audit, ByteString.EMPTY);
  }

  public AppleAuditing(boolean audit, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.audit = audit;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.audit = audit;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", audit=").append(audit);
    return builder.replace(0, 2, "AppleAuditing{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<AppleAuditing, Builder> {
    public boolean audit;

    public Builder() {
      audit = false;
    }

    /**
     * 苹果是否版本审核中，端上采取特殊审核策略
     */
    public Builder audit(boolean audit) {
      this.audit = audit;
      return this;
    }

    @Override
    public AppleAuditing build() {
      return new AppleAuditing(audit, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_AppleAuditing extends ProtoAdapter<AppleAuditing> {
    public ProtoAdapter_AppleAuditing() {
      super(FieldEncoding.LENGTH_DELIMITED, AppleAuditing.class, "type.googleapis.com/haiyaa.AppleAuditing", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(AppleAuditing value) {
      int result = 0;
      if (!Objects.equals(value.audit, false)) {
        result += ProtoAdapter.BOOL.encodedSizeWithTag(1, value.audit);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, AppleAuditing value) throws IOException {
      if (!Objects.equals(value.audit, false)) ProtoAdapter.BOOL.encodeWithTag(writer, 1, value.audit);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public AppleAuditing decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.audit(ProtoAdapter.BOOL.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public AppleAuditing redact(AppleAuditing value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
