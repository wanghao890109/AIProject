// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.AudioInfo in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class AudioInfo extends Message<AudioInfo, AudioInfo.Builder> {
  public static final ProtoAdapter<AudioInfo> ADAPTER = new ProtoAdapter_AudioInfo();

  private static final long serialVersionUID = 0L;

  public String name;

  /**
   * 语音文件时长;
   */
  public int secs;

  /**
   * 文件大小;
   */
  public long size;

  public String url;

  public AudioInfo(String name, int secs, long size, String url) {
    this(name, secs, size, url, ByteString.EMPTY);
  }

  public AudioInfo(String name, int secs, long size, String url, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (name == null) {
      throw new IllegalArgumentException("name == null");
    }
    this.name = name;
    this.secs = secs;
    this.size = size;
    if (url == null) {
      throw new IllegalArgumentException("url == null");
    }
    this.url = url;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.name = name;
    builder.secs = secs;
    builder.size = size;
    builder.url = url;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (name != null) builder.append(", name=").append(Internal.sanitize(name));
    builder.append(", secs=").append(secs);
    builder.append(", size=").append(size);
    if (url != null) builder.append(", url=").append(Internal.sanitize(url));
    return builder.replace(0, 2, "AudioInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<AudioInfo, Builder> {
    public String name;

    public int secs;

    public long size;

    public String url;

    public Builder() {
      name = "";
      secs = 0;
      size = 0L;
      url = "";
    }

    public Builder name(String name) {
      this.name = name;
      return this;
    }

    /**
     * 语音文件时长;
     */
    public Builder secs(int secs) {
      this.secs = secs;
      return this;
    }

    /**
     * 文件大小;
     */
    public Builder size(long size) {
      this.size = size;
      return this;
    }

    public Builder url(String url) {
      this.url = url;
      return this;
    }

    @Override
    public AudioInfo build() {
      return new AudioInfo(name, secs, size, url, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_AudioInfo extends ProtoAdapter<AudioInfo> {
    public ProtoAdapter_AudioInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, AudioInfo.class, "type.googleapis.com/haiyaa.AudioInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(AudioInfo value) {
      int result = 0;
      if (!Objects.equals(value.name, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.name);
      }
      if (!Objects.equals(value.secs, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(2, value.secs);
      }
      if (!Objects.equals(value.size, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(3, value.size);
      }
      if (!Objects.equals(value.url, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.url);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, AudioInfo value) throws IOException {
      if (!Objects.equals(value.name, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.name);
      if (!Objects.equals(value.secs, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 2, value.secs);
      if (!Objects.equals(value.size, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 3, value.size);
      if (!Objects.equals(value.url, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.url);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public AudioInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.name(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.secs(ProtoAdapter.UINT32.decode(reader)); break;
          case 3: builder.size(ProtoAdapter.UINT64.decode(reader)); break;
          case 4: builder.url(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public AudioInfo redact(AudioInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
