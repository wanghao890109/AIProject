// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.BagItemS in gift.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class BagItemS extends Message<BagItemS, BagItemS.Builder> {
  public static final ProtoAdapter<BagItemS> ADAPTER = new ProtoAdapter_BagItemS();

  private static final long serialVersionUID = 0L;

  public List<BagItem> items;

  /**
   * 转盘终点[0-7]
   */
  public int endAt;

  public BagItemS(List<BagItem> items, int endAt) {
    this(items, endAt, ByteString.EMPTY);
  }

  public BagItemS(List<BagItem> items, int endAt, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.items = Internal.immutableCopyOf("items", items);
    this.endAt = endAt;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.items = Internal.copyOf(items);
    builder.endAt = endAt;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!items.isEmpty()) builder.append(", items=").append(items);
    builder.append(", endAt=").append(endAt);
    return builder.replace(0, 2, "BagItemS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<BagItemS, Builder> {
    public List<BagItem> items;

    public int endAt;

    public Builder() {
      items = Internal.newMutableList();
      endAt = 0;
    }

    public Builder items(List<BagItem> items) {
      Internal.checkElementsNotNull(items);
      this.items = items;
      return this;
    }

    /**
     * 转盘终点[0-7]
     */
    public Builder endAt(int endAt) {
      this.endAt = endAt;
      return this;
    }

    @Override
    public BagItemS build() {
      return new BagItemS(items, endAt, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_BagItemS extends ProtoAdapter<BagItemS> {
    public ProtoAdapter_BagItemS() {
      super(FieldEncoding.LENGTH_DELIMITED, BagItemS.class, "type.googleapis.com/haiyaa.BagItemS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(BagItemS value) {
      int result = 0;
      result += BagItem.ADAPTER.asRepeated().encodedSizeWithTag(1, value.items);
      if (!Objects.equals(value.endAt, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.endAt);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, BagItemS value) throws IOException {
      BagItem.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.items);
      if (!Objects.equals(value.endAt, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.endAt);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public BagItemS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.items.add(BagItem.ADAPTER.decode(reader)); break;
          case 2: builder.endAt(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public BagItemS redact(BagItemS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.items, BagItem.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
