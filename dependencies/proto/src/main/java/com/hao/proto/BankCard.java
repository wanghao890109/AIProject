// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.BankCard in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 银行卡，只能绑定一张，输入验证码后绑定完成，需要先解绑，再重新绑定
 */
public final class BankCard extends Message<BankCard, BankCard.Builder> {
  public static final ProtoAdapter<BankCard> ADAPTER = new ProtoAdapter_BankCard();

  private static final long serialVersionUID = 0L;

  /**
   * 人员
   */
  public long userId;

  /**
   * 姓名 - 必填
   */
  public String userName;

  /**
   * 银行卡号 - 必填
   */
  public String cardId;

  /**
   * 银行名 - 必填
   */
  public String bankName;

  /**
   * 开户行所在省份
   */
  public String province;

  /**
   * 银行预留手机号
   */
  public long phoneId;

  /**
   * 验证码，带着验证码来绑定或解绑银行卡
   */
  public int vcode;

  /**
   * 发起时间
   */
  public long ctTime;

  /**
   * 最新进度时间
   */
  public long upTime;

  public BankCard(long userId, String userName, String cardId, String bankName, String province,
      long phoneId, int vcode, long ctTime, long upTime) {
    this(userId, userName, cardId, bankName, province, phoneId, vcode, ctTime, upTime, ByteString.EMPTY);
  }

  public BankCard(long userId, String userName, String cardId, String bankName, String province,
      long phoneId, int vcode, long ctTime, long upTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    if (userName == null) {
      throw new IllegalArgumentException("userName == null");
    }
    this.userName = userName;
    if (cardId == null) {
      throw new IllegalArgumentException("cardId == null");
    }
    this.cardId = cardId;
    if (bankName == null) {
      throw new IllegalArgumentException("bankName == null");
    }
    this.bankName = bankName;
    if (province == null) {
      throw new IllegalArgumentException("province == null");
    }
    this.province = province;
    this.phoneId = phoneId;
    this.vcode = vcode;
    this.ctTime = ctTime;
    this.upTime = upTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.userName = userName;
    builder.cardId = cardId;
    builder.bankName = bankName;
    builder.province = province;
    builder.phoneId = phoneId;
    builder.vcode = vcode;
    builder.ctTime = ctTime;
    builder.upTime = upTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    if (userName != null) builder.append(", userName=").append(Internal.sanitize(userName));
    if (cardId != null) builder.append(", cardId=").append(Internal.sanitize(cardId));
    if (bankName != null) builder.append(", bankName=").append(Internal.sanitize(bankName));
    if (province != null) builder.append(", province=").append(Internal.sanitize(province));
    builder.append(", phoneId=").append(phoneId);
    builder.append(", vcode=").append(vcode);
    builder.append(", ctTime=").append(ctTime);
    builder.append(", upTime=").append(upTime);
    return builder.replace(0, 2, "BankCard{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<BankCard, Builder> {
    public long userId;

    public String userName;

    public String cardId;

    public String bankName;

    public String province;

    public long phoneId;

    public int vcode;

    public long ctTime;

    public long upTime;

    public Builder() {
      userId = 0L;
      userName = "";
      cardId = "";
      bankName = "";
      province = "";
      phoneId = 0L;
      vcode = 0;
      ctTime = 0L;
      upTime = 0L;
    }

    /**
     * 人员
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 姓名 - 必填
     */
    public Builder userName(String userName) {
      this.userName = userName;
      return this;
    }

    /**
     * 银行卡号 - 必填
     */
    public Builder cardId(String cardId) {
      this.cardId = cardId;
      return this;
    }

    /**
     * 银行名 - 必填
     */
    public Builder bankName(String bankName) {
      this.bankName = bankName;
      return this;
    }

    /**
     * 开户行所在省份
     */
    public Builder province(String province) {
      this.province = province;
      return this;
    }

    /**
     * 银行预留手机号
     */
    public Builder phoneId(long phoneId) {
      this.phoneId = phoneId;
      return this;
    }

    /**
     * 验证码，带着验证码来绑定或解绑银行卡
     */
    public Builder vcode(int vcode) {
      this.vcode = vcode;
      return this;
    }

    /**
     * 发起时间
     */
    public Builder ctTime(long ctTime) {
      this.ctTime = ctTime;
      return this;
    }

    /**
     * 最新进度时间
     */
    public Builder upTime(long upTime) {
      this.upTime = upTime;
      return this;
    }

    @Override
    public BankCard build() {
      return new BankCard(userId, userName, cardId, bankName, province, phoneId, vcode, ctTime, upTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_BankCard extends ProtoAdapter<BankCard> {
    public ProtoAdapter_BankCard() {
      super(FieldEncoding.LENGTH_DELIMITED, BankCard.class, "type.googleapis.com/haiyaa.BankCard", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(BankCard value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.userName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.userName);
      }
      if (!Objects.equals(value.cardId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.cardId);
      }
      if (!Objects.equals(value.bankName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.bankName);
      }
      if (!Objects.equals(value.province, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.province);
      }
      if (!Objects.equals(value.phoneId, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(6, value.phoneId);
      }
      if (!Objects.equals(value.vcode, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(7, value.vcode);
      }
      if (!Objects.equals(value.ctTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(8, value.ctTime);
      }
      if (!Objects.equals(value.upTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(9, value.upTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, BankCard value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.userName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.userName);
      if (!Objects.equals(value.cardId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.cardId);
      if (!Objects.equals(value.bankName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.bankName);
      if (!Objects.equals(value.province, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.province);
      if (!Objects.equals(value.phoneId, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 6, value.phoneId);
      if (!Objects.equals(value.vcode, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 7, value.vcode);
      if (!Objects.equals(value.ctTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 8, value.ctTime);
      if (!Objects.equals(value.upTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 9, value.upTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public BankCard decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.userName(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.cardId(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.bankName(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.province(ProtoAdapter.STRING.decode(reader)); break;
          case 6: builder.phoneId(ProtoAdapter.INT64.decode(reader)); break;
          case 7: builder.vcode(ProtoAdapter.INT32.decode(reader)); break;
          case 8: builder.ctTime(ProtoAdapter.INT64.decode(reader)); break;
          case 9: builder.upTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public BankCard redact(BankCard value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
