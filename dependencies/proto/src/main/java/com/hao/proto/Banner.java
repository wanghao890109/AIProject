// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.Banner in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class Banner extends Message<Banner, Banner.Builder> {
  public static final ProtoAdapter<Banner> ADAPTER = new ProtoAdapter_Banner();

  private static final long serialVersionUID = 0L;

  /**
   * 长图
   */
  public String longImage;

  /**
   * 短图
   */
  public String shortImage;

  public String title;

  public String subTitle;

  public String jumpUrl;

  public Banner(String longImage, String shortImage, String title, String subTitle,
      String jumpUrl) {
    this(longImage, shortImage, title, subTitle, jumpUrl, ByteString.EMPTY);
  }

  public Banner(String longImage, String shortImage, String title, String subTitle, String jumpUrl,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (longImage == null) {
      throw new IllegalArgumentException("longImage == null");
    }
    this.longImage = longImage;
    if (shortImage == null) {
      throw new IllegalArgumentException("shortImage == null");
    }
    this.shortImage = shortImage;
    if (title == null) {
      throw new IllegalArgumentException("title == null");
    }
    this.title = title;
    if (subTitle == null) {
      throw new IllegalArgumentException("subTitle == null");
    }
    this.subTitle = subTitle;
    if (jumpUrl == null) {
      throw new IllegalArgumentException("jumpUrl == null");
    }
    this.jumpUrl = jumpUrl;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.longImage = longImage;
    builder.shortImage = shortImage;
    builder.title = title;
    builder.subTitle = subTitle;
    builder.jumpUrl = jumpUrl;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (longImage != null) builder.append(", longImage=").append(Internal.sanitize(longImage));
    if (shortImage != null) builder.append(", shortImage=").append(Internal.sanitize(shortImage));
    if (title != null) builder.append(", title=").append(Internal.sanitize(title));
    if (subTitle != null) builder.append(", subTitle=").append(Internal.sanitize(subTitle));
    if (jumpUrl != null) builder.append(", jumpUrl=").append(Internal.sanitize(jumpUrl));
    return builder.replace(0, 2, "Banner{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<Banner, Builder> {
    public String longImage;

    public String shortImage;

    public String title;

    public String subTitle;

    public String jumpUrl;

    public Builder() {
      longImage = "";
      shortImage = "";
      title = "";
      subTitle = "";
      jumpUrl = "";
    }

    /**
     * 长图
     */
    public Builder longImage(String longImage) {
      this.longImage = longImage;
      return this;
    }

    /**
     * 短图
     */
    public Builder shortImage(String shortImage) {
      this.shortImage = shortImage;
      return this;
    }

    public Builder title(String title) {
      this.title = title;
      return this;
    }

    public Builder subTitle(String subTitle) {
      this.subTitle = subTitle;
      return this;
    }

    public Builder jumpUrl(String jumpUrl) {
      this.jumpUrl = jumpUrl;
      return this;
    }

    @Override
    public Banner build() {
      return new Banner(longImage, shortImage, title, subTitle, jumpUrl, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_Banner extends ProtoAdapter<Banner> {
    public ProtoAdapter_Banner() {
      super(FieldEncoding.LENGTH_DELIMITED, Banner.class, "type.googleapis.com/haiyaa.Banner", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(Banner value) {
      int result = 0;
      if (!Objects.equals(value.longImage, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.longImage);
      }
      if (!Objects.equals(value.shortImage, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.shortImage);
      }
      if (!Objects.equals(value.title, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.title);
      }
      if (!Objects.equals(value.subTitle, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.subTitle);
      }
      if (!Objects.equals(value.jumpUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.jumpUrl);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, Banner value) throws IOException {
      if (!Objects.equals(value.longImage, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.longImage);
      if (!Objects.equals(value.shortImage, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.shortImage);
      if (!Objects.equals(value.title, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.title);
      if (!Objects.equals(value.subTitle, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.subTitle);
      if (!Objects.equals(value.jumpUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.jumpUrl);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public Banner decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.longImage(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.shortImage(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.title(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.subTitle(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.jumpUrl(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public Banner redact(Banner value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
