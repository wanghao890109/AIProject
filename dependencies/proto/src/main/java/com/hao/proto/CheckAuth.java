// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.CheckAuth in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class CheckAuth extends Message<CheckAuth, CheckAuth.Builder> {
  public static final ProtoAdapter<CheckAuth> ADAPTER = new ProtoAdapter_CheckAuth();

  private static final long serialVersionUID = 0L;

  /**
   * 查询用户B
   */
  public long userId;

  /**
   * 设备名
   */
  public String devId;

  /**
   * 命令查询
   */
  public long cmd;

  /**
   * 后台存储，待校验账户等级
   */
  public long accStatus;

  /**
   * 从用户来，待校验Token
   */
  public String tokenId;

  public CheckAuth(long userId, String devId, long cmd, long accStatus, String tokenId) {
    this(userId, devId, cmd, accStatus, tokenId, ByteString.EMPTY);
  }

  public CheckAuth(long userId, String devId, long cmd, long accStatus, String tokenId,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    if (devId == null) {
      throw new IllegalArgumentException("devId == null");
    }
    this.devId = devId;
    this.cmd = cmd;
    this.accStatus = accStatus;
    if (tokenId == null) {
      throw new IllegalArgumentException("tokenId == null");
    }
    this.tokenId = tokenId;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.devId = devId;
    builder.cmd = cmd;
    builder.accStatus = accStatus;
    builder.tokenId = tokenId;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    if (devId != null) builder.append(", devId=").append(Internal.sanitize(devId));
    builder.append(", cmd=").append(cmd);
    builder.append(", accStatus=").append(accStatus);
    if (tokenId != null) builder.append(", tokenId=").append(Internal.sanitize(tokenId));
    return builder.replace(0, 2, "CheckAuth{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<CheckAuth, Builder> {
    public long userId;

    public String devId;

    public long cmd;

    public long accStatus;

    public String tokenId;

    public Builder() {
      userId = 0L;
      devId = "";
      cmd = 0L;
      accStatus = 0L;
      tokenId = "";
    }

    /**
     * 查询用户B
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 设备名
     */
    public Builder devId(String devId) {
      this.devId = devId;
      return this;
    }

    /**
     * 命令查询
     */
    public Builder cmd(long cmd) {
      this.cmd = cmd;
      return this;
    }

    /**
     * 后台存储，待校验账户等级
     */
    public Builder accStatus(long accStatus) {
      this.accStatus = accStatus;
      return this;
    }

    /**
     * 从用户来，待校验Token
     */
    public Builder tokenId(String tokenId) {
      this.tokenId = tokenId;
      return this;
    }

    @Override
    public CheckAuth build() {
      return new CheckAuth(userId, devId, cmd, accStatus, tokenId, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_CheckAuth extends ProtoAdapter<CheckAuth> {
    public ProtoAdapter_CheckAuth() {
      super(FieldEncoding.LENGTH_DELIMITED, CheckAuth.class, "type.googleapis.com/haiyaa.CheckAuth", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(CheckAuth value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.devId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.devId);
      }
      if (!Objects.equals(value.cmd, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(3, value.cmd);
      }
      if (!Objects.equals(value.accStatus, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(4, value.accStatus);
      }
      if (!Objects.equals(value.tokenId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.tokenId);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, CheckAuth value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.devId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.devId);
      if (!Objects.equals(value.cmd, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 3, value.cmd);
      if (!Objects.equals(value.accStatus, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 4, value.accStatus);
      if (!Objects.equals(value.tokenId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.tokenId);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public CheckAuth decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.devId(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.cmd(ProtoAdapter.INT64.decode(reader)); break;
          case 4: builder.accStatus(ProtoAdapter.INT64.decode(reader)); break;
          case 5: builder.tokenId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public CheckAuth redact(CheckAuth value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
