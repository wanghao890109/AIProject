// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.CoinRecord in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 金币明细，加钻不要直接操作数据库，而要走接口，才能进明细记录里
 */
public final class CoinRecord extends Message<CoinRecord, CoinRecord.Builder> {
  public static final ProtoAdapter<CoinRecord> ADAPTER = new ProtoAdapter_CoinRecord();

  private static final long serialVersionUID = 0L;

  public long userId;

  /**
   * 所在房间
   */
  public long roomId;

  /**
   * 类型图标
   */
  public String icon;

  /**
   * 钻石变化数量，可能为负值
   */
  public int alter;

  /**
   * 标题
   */
  public String title;

  /**
   * 变化时间
   */
  public long upTime;

  public CoinRecord(long userId, long roomId, String icon, int alter, String title, long upTime) {
    this(userId, roomId, icon, alter, title, upTime, ByteString.EMPTY);
  }

  public CoinRecord(long userId, long roomId, String icon, int alter, String title, long upTime,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.roomId = roomId;
    if (icon == null) {
      throw new IllegalArgumentException("icon == null");
    }
    this.icon = icon;
    this.alter = alter;
    if (title == null) {
      throw new IllegalArgumentException("title == null");
    }
    this.title = title;
    this.upTime = upTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.roomId = roomId;
    builder.icon = icon;
    builder.alter = alter;
    builder.title = title;
    builder.upTime = upTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    builder.append(", roomId=").append(roomId);
    if (icon != null) builder.append(", icon=").append(Internal.sanitize(icon));
    builder.append(", alter=").append(alter);
    if (title != null) builder.append(", title=").append(Internal.sanitize(title));
    builder.append(", upTime=").append(upTime);
    return builder.replace(0, 2, "CoinRecord{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<CoinRecord, Builder> {
    public long userId;

    public long roomId;

    public String icon;

    public int alter;

    public String title;

    public long upTime;

    public Builder() {
      userId = 0L;
      roomId = 0L;
      icon = "";
      alter = 0;
      title = "";
      upTime = 0L;
    }

    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 所在房间
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 类型图标
     */
    public Builder icon(String icon) {
      this.icon = icon;
      return this;
    }

    /**
     * 钻石变化数量，可能为负值
     */
    public Builder alter(int alter) {
      this.alter = alter;
      return this;
    }

    /**
     * 标题
     */
    public Builder title(String title) {
      this.title = title;
      return this;
    }

    /**
     * 变化时间
     */
    public Builder upTime(long upTime) {
      this.upTime = upTime;
      return this;
    }

    @Override
    public CoinRecord build() {
      return new CoinRecord(userId, roomId, icon, alter, title, upTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_CoinRecord extends ProtoAdapter<CoinRecord> {
    public ProtoAdapter_CoinRecord() {
      super(FieldEncoding.LENGTH_DELIMITED, CoinRecord.class, "type.googleapis.com/haiyaa.CoinRecord", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(CoinRecord value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.roomId);
      }
      if (!Objects.equals(value.icon, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.icon);
      }
      if (!Objects.equals(value.alter, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.alter);
      }
      if (!Objects.equals(value.title, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.title);
      }
      if (!Objects.equals(value.upTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(6, value.upTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, CoinRecord value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.roomId);
      if (!Objects.equals(value.icon, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.icon);
      if (!Objects.equals(value.alter, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.alter);
      if (!Objects.equals(value.title, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.title);
      if (!Objects.equals(value.upTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 6, value.upTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public CoinRecord decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.icon(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.alter(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.title(ProtoAdapter.STRING.decode(reader)); break;
          case 6: builder.upTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public CoinRecord redact(CoinRecord value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
