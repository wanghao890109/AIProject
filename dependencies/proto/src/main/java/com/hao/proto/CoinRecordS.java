// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.CoinRecordS in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class CoinRecordS extends Message<CoinRecordS, CoinRecordS.Builder> {
  public static final ProtoAdapter<CoinRecordS> ADAPTER = new ProtoAdapter_CoinRecordS();

  private static final long serialVersionUID = 0L;

  public List<CoinRecord> records;

  public CoinRecordS(List<CoinRecord> records) {
    this(records, ByteString.EMPTY);
  }

  public CoinRecordS(List<CoinRecord> records, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.records = Internal.immutableCopyOf("records", records);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.records = Internal.copyOf(records);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!records.isEmpty()) builder.append(", records=").append(records);
    return builder.replace(0, 2, "CoinRecordS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<CoinRecordS, Builder> {
    public List<CoinRecord> records;

    public Builder() {
      records = Internal.newMutableList();
    }

    public Builder records(List<CoinRecord> records) {
      Internal.checkElementsNotNull(records);
      this.records = records;
      return this;
    }

    @Override
    public CoinRecordS build() {
      return new CoinRecordS(records, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_CoinRecordS extends ProtoAdapter<CoinRecordS> {
    public ProtoAdapter_CoinRecordS() {
      super(FieldEncoding.LENGTH_DELIMITED, CoinRecordS.class, "type.googleapis.com/haiyaa.CoinRecordS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(CoinRecordS value) {
      int result = 0;
      result += CoinRecord.ADAPTER.asRepeated().encodedSizeWithTag(1, value.records);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, CoinRecordS value) throws IOException {
      CoinRecord.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.records);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public CoinRecordS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.records.add(CoinRecord.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public CoinRecordS redact(CoinRecordS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.records, CoinRecord.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
