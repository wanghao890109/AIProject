// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.CommentInfoS in moment.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class CommentInfoS extends Message<CommentInfoS, CommentInfoS.Builder> {
  public static final ProtoAdapter<CommentInfoS> ADAPTER = new ProtoAdapter_CommentInfoS();

  private static final long serialVersionUID = 0L;

  /**
   *  评论
   */
  public List<CommentInfo> Comments;

  public CommentInfoS(List<CommentInfo> Comments) {
    this(Comments, ByteString.EMPTY);
  }

  public CommentInfoS(List<CommentInfo> Comments, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.Comments = Internal.immutableCopyOf("Comments", Comments);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.Comments = Internal.copyOf(Comments);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!Comments.isEmpty()) builder.append(", Comments=").append(Comments);
    return builder.replace(0, 2, "CommentInfoS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<CommentInfoS, Builder> {
    public List<CommentInfo> Comments;

    public Builder() {
      Comments = Internal.newMutableList();
    }

    /**
     *  评论
     */
    public Builder Comments(List<CommentInfo> Comments) {
      Internal.checkElementsNotNull(Comments);
      this.Comments = Comments;
      return this;
    }

    @Override
    public CommentInfoS build() {
      return new CommentInfoS(Comments, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_CommentInfoS extends ProtoAdapter<CommentInfoS> {
    public ProtoAdapter_CommentInfoS() {
      super(FieldEncoding.LENGTH_DELIMITED, CommentInfoS.class, "type.googleapis.com/haiyaa.CommentInfoS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(CommentInfoS value) {
      int result = 0;
      result += CommentInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.Comments);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, CommentInfoS value) throws IOException {
      CommentInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.Comments);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public CommentInfoS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.Comments.add(CommentInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public CommentInfoS redact(CommentInfoS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.Comments, CommentInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
