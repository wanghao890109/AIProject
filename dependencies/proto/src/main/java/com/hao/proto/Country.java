// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.Country in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class Country extends Message<Country, Country.Builder> {
  public static final ProtoAdapter<Country> ADAPTER = new ProtoAdapter_Country();

  private static final long serialVersionUID = 0L;

  /**
   * 国家名
   */
  public String Name;

  /**
   * 简写
   */
  public String ShortName;

  /**
   * 地区码
   */
  public String Code;

  public Country(String Name, String ShortName, String Code) {
    this(Name, ShortName, Code, ByteString.EMPTY);
  }

  public Country(String Name, String ShortName, String Code, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (Name == null) {
      throw new IllegalArgumentException("Name == null");
    }
    this.Name = Name;
    if (ShortName == null) {
      throw new IllegalArgumentException("ShortName == null");
    }
    this.ShortName = ShortName;
    if (Code == null) {
      throw new IllegalArgumentException("Code == null");
    }
    this.Code = Code;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.Name = Name;
    builder.ShortName = ShortName;
    builder.Code = Code;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (Name != null) builder.append(", Name=").append(Internal.sanitize(Name));
    if (ShortName != null) builder.append(", ShortName=").append(Internal.sanitize(ShortName));
    if (Code != null) builder.append(", Code=").append(Internal.sanitize(Code));
    return builder.replace(0, 2, "Country{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<Country, Builder> {
    public String Name;

    public String ShortName;

    public String Code;

    public Builder() {
      Name = "";
      ShortName = "";
      Code = "";
    }

    /**
     * 国家名
     */
    public Builder Name(String Name) {
      this.Name = Name;
      return this;
    }

    /**
     * 简写
     */
    public Builder ShortName(String ShortName) {
      this.ShortName = ShortName;
      return this;
    }

    /**
     * 地区码
     */
    public Builder Code(String Code) {
      this.Code = Code;
      return this;
    }

    @Override
    public Country build() {
      return new Country(Name, ShortName, Code, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_Country extends ProtoAdapter<Country> {
    public ProtoAdapter_Country() {
      super(FieldEncoding.LENGTH_DELIMITED, Country.class, "type.googleapis.com/haiyaa.Country", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(Country value) {
      int result = 0;
      if (!Objects.equals(value.Name, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.Name);
      }
      if (!Objects.equals(value.ShortName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.ShortName);
      }
      if (!Objects.equals(value.Code, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.Code);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, Country value) throws IOException {
      if (!Objects.equals(value.Name, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.Name);
      if (!Objects.equals(value.ShortName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.ShortName);
      if (!Objects.equals(value.Code, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.Code);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public Country decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.Name(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.ShortName(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.Code(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public Country redact(Country value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
