// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.DecoInUse in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 用户状态(装饰)和道具(背包系统),GiftInfo->DecoInfo, 勋章，这里需要重新调整
 */
public final class DecoInUse extends Message<DecoInUse, DecoInUse.Builder> {
  public static final ProtoAdapter<DecoInUse> ADAPTER = new ProtoAdapter_DecoInUse();

  private static final long serialVersionUID = 0L;

  public long userId;

  public DecoInfo deco;

  /**
   * 是否在用，0，1在用，2、未装扮
   */
  public int status;

  /**
   * 未用的状态看可用天数
   */
  public int ableDay;

  /**
   * 开始使用时间
   */
  public long stTime;

  /**
   * 在用的状态看结束时间
   */
  public long edTime;

  public DecoInUse(long userId, DecoInfo deco, int status, int ableDay, long stTime, long edTime) {
    this(userId, deco, status, ableDay, stTime, edTime, ByteString.EMPTY);
  }

  public DecoInUse(long userId, DecoInfo deco, int status, int ableDay, long stTime, long edTime,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.deco = deco;
    this.status = status;
    this.ableDay = ableDay;
    this.stTime = stTime;
    this.edTime = edTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.deco = deco;
    builder.status = status;
    builder.ableDay = ableDay;
    builder.stTime = stTime;
    builder.edTime = edTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    if (deco != null) builder.append(", deco=").append(deco);
    builder.append(", status=").append(status);
    builder.append(", ableDay=").append(ableDay);
    builder.append(", stTime=").append(stTime);
    builder.append(", edTime=").append(edTime);
    return builder.replace(0, 2, "DecoInUse{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DecoInUse, Builder> {
    public long userId;

    public DecoInfo deco;

    public int status;

    public int ableDay;

    public long stTime;

    public long edTime;

    public Builder() {
      userId = 0L;
      status = 0;
      ableDay = 0;
      stTime = 0L;
      edTime = 0L;
    }

    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    public Builder deco(DecoInfo deco) {
      this.deco = deco;
      return this;
    }

    /**
     * 是否在用，0，1在用，2、未装扮
     */
    public Builder status(int status) {
      this.status = status;
      return this;
    }

    /**
     * 未用的状态看可用天数
     */
    public Builder ableDay(int ableDay) {
      this.ableDay = ableDay;
      return this;
    }

    /**
     * 开始使用时间
     */
    public Builder stTime(long stTime) {
      this.stTime = stTime;
      return this;
    }

    /**
     * 在用的状态看结束时间
     */
    public Builder edTime(long edTime) {
      this.edTime = edTime;
      return this;
    }

    @Override
    public DecoInUse build() {
      return new DecoInUse(userId, deco, status, ableDay, stTime, edTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DecoInUse extends ProtoAdapter<DecoInUse> {
    public ProtoAdapter_DecoInUse() {
      super(FieldEncoding.LENGTH_DELIMITED, DecoInUse.class, "type.googleapis.com/haiyaa.DecoInUse", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(DecoInUse value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.deco, null)) {
        result += DecoInfo.ADAPTER.encodedSizeWithTag(2, value.deco);
      }
      if (!Objects.equals(value.status, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.status);
      }
      if (!Objects.equals(value.ableDay, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.ableDay);
      }
      if (!Objects.equals(value.stTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(5, value.stTime);
      }
      if (!Objects.equals(value.edTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(8, value.edTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, DecoInUse value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.deco, null)) DecoInfo.ADAPTER.encodeWithTag(writer, 2, value.deco);
      if (!Objects.equals(value.status, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.status);
      if (!Objects.equals(value.ableDay, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.ableDay);
      if (!Objects.equals(value.stTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 5, value.stTime);
      if (!Objects.equals(value.edTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 8, value.edTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DecoInUse decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.deco(DecoInfo.ADAPTER.decode(reader)); break;
          case 3: builder.status(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.ableDay(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.stTime(ProtoAdapter.INT64.decode(reader)); break;
          case 8: builder.edTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public DecoInUse redact(DecoInUse value) {
      Builder builder = value.newBuilder();
      if (builder.deco != null) builder.deco = DecoInfo.ADAPTER.redact(builder.deco);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
