// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.DecoInfoS in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class DecoInfoS extends Message<DecoInfoS, DecoInfoS.Builder> {
  public static final ProtoAdapter<DecoInfoS> ADAPTER = new ProtoAdapter_DecoInfoS();

  private static final long serialVersionUID = 0L;

  public List<DecoInfo> decos;

  public DecoInfoS(List<DecoInfo> decos) {
    this(decos, ByteString.EMPTY);
  }

  public DecoInfoS(List<DecoInfo> decos, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.decos = Internal.immutableCopyOf("decos", decos);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.decos = Internal.copyOf(decos);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!decos.isEmpty()) builder.append(", decos=").append(decos);
    return builder.replace(0, 2, "DecoInfoS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DecoInfoS, Builder> {
    public List<DecoInfo> decos;

    public Builder() {
      decos = Internal.newMutableList();
    }

    public Builder decos(List<DecoInfo> decos) {
      Internal.checkElementsNotNull(decos);
      this.decos = decos;
      return this;
    }

    @Override
    public DecoInfoS build() {
      return new DecoInfoS(decos, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DecoInfoS extends ProtoAdapter<DecoInfoS> {
    public ProtoAdapter_DecoInfoS() {
      super(FieldEncoding.LENGTH_DELIMITED, DecoInfoS.class, "type.googleapis.com/haiyaa.DecoInfoS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(DecoInfoS value) {
      int result = 0;
      result += DecoInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.decos);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, DecoInfoS value) throws IOException {
      DecoInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.decos);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DecoInfoS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.decos.add(DecoInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public DecoInfoS redact(DecoInfoS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.decos, DecoInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
