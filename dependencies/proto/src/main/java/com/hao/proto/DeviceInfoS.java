// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.DeviceInfoS in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class DeviceInfoS extends Message<DeviceInfoS, DeviceInfoS.Builder> {
  public static final ProtoAdapter<DeviceInfoS> ADAPTER = new ProtoAdapter_DeviceInfoS();

  private static final long serialVersionUID = 0L;

  public List<DeviceInfo> devs;

  public DeviceInfoS(List<DeviceInfo> devs) {
    this(devs, ByteString.EMPTY);
  }

  public DeviceInfoS(List<DeviceInfo> devs, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.devs = Internal.immutableCopyOf("devs", devs);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.devs = Internal.copyOf(devs);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!devs.isEmpty()) builder.append(", devs=").append(devs);
    return builder.replace(0, 2, "DeviceInfoS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DeviceInfoS, Builder> {
    public List<DeviceInfo> devs;

    public Builder() {
      devs = Internal.newMutableList();
    }

    public Builder devs(List<DeviceInfo> devs) {
      Internal.checkElementsNotNull(devs);
      this.devs = devs;
      return this;
    }

    @Override
    public DeviceInfoS build() {
      return new DeviceInfoS(devs, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DeviceInfoS extends ProtoAdapter<DeviceInfoS> {
    public ProtoAdapter_DeviceInfoS() {
      super(FieldEncoding.LENGTH_DELIMITED, DeviceInfoS.class, "type.googleapis.com/haiyaa.DeviceInfoS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(DeviceInfoS value) {
      int result = 0;
      result += DeviceInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.devs);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, DeviceInfoS value) throws IOException {
      DeviceInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.devs);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DeviceInfoS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.devs.add(DeviceInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public DeviceInfoS redact(DeviceInfoS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.devs, DeviceInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
