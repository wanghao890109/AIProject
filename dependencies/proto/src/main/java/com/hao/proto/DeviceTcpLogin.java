// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.DeviceTcpLogin in login.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * GWLogin
 */
public final class DeviceTcpLogin extends Message<DeviceTcpLogin, DeviceTcpLogin.Builder> {
  public static final ProtoAdapter<DeviceTcpLogin> ADAPTER = new ProtoAdapter_DeviceTcpLogin();

  private static final long serialVersionUID = 0L;

  /**
   * DeviceInfo   device   = 1; // 设备信息
   * 登录信息
   */
  public CheckAccount accCheck;

  /**
   * 如果有Token, 则无密登录
   */
  public String tokenId;

  public DeviceTcpLogin(CheckAccount accCheck, String tokenId) {
    this(accCheck, tokenId, ByteString.EMPTY);
  }

  public DeviceTcpLogin(CheckAccount accCheck, String tokenId, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.accCheck = accCheck;
    if (tokenId == null) {
      throw new IllegalArgumentException("tokenId == null");
    }
    this.tokenId = tokenId;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.accCheck = accCheck;
    builder.tokenId = tokenId;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (accCheck != null) builder.append(", accCheck=").append(accCheck);
    if (tokenId != null) builder.append(", tokenId=").append(Internal.sanitize(tokenId));
    return builder.replace(0, 2, "DeviceTcpLogin{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DeviceTcpLogin, Builder> {
    public CheckAccount accCheck;

    public String tokenId;

    public Builder() {
      tokenId = "";
    }

    /**
     * DeviceInfo   device   = 1; // 设备信息
     * 登录信息
     */
    public Builder accCheck(CheckAccount accCheck) {
      this.accCheck = accCheck;
      return this;
    }

    /**
     * 如果有Token, 则无密登录
     */
    public Builder tokenId(String tokenId) {
      this.tokenId = tokenId;
      return this;
    }

    @Override
    public DeviceTcpLogin build() {
      return new DeviceTcpLogin(accCheck, tokenId, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DeviceTcpLogin extends ProtoAdapter<DeviceTcpLogin> {
    public ProtoAdapter_DeviceTcpLogin() {
      super(FieldEncoding.LENGTH_DELIMITED, DeviceTcpLogin.class, "type.googleapis.com/haiyaa.DeviceTcpLogin", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(DeviceTcpLogin value) {
      int result = 0;
      if (!Objects.equals(value.accCheck, null)) {
        result += CheckAccount.ADAPTER.encodedSizeWithTag(2, value.accCheck);
      }
      if (!Objects.equals(value.tokenId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.tokenId);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, DeviceTcpLogin value) throws IOException {
      if (!Objects.equals(value.accCheck, null)) CheckAccount.ADAPTER.encodeWithTag(writer, 2, value.accCheck);
      if (!Objects.equals(value.tokenId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.tokenId);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DeviceTcpLogin decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 2: builder.accCheck(CheckAccount.ADAPTER.decode(reader)); break;
          case 3: builder.tokenId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public DeviceTcpLogin redact(DeviceTcpLogin value) {
      Builder builder = value.newBuilder();
      if (builder.accCheck != null) builder.accCheck = CheckAccount.ADAPTER.redact(builder.accCheck);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
