// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.DrawOrderS in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class DrawOrderS extends Message<DrawOrderS, DrawOrderS.Builder> {
  public static final ProtoAdapter<DrawOrderS> ADAPTER = new ProtoAdapter_DrawOrderS();

  private static final long serialVersionUID = 0L;

  public List<DrawOrder> orders;

  public DrawOrderS(List<DrawOrder> orders) {
    this(orders, ByteString.EMPTY);
  }

  public DrawOrderS(List<DrawOrder> orders, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.orders = Internal.immutableCopyOf("orders", orders);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.orders = Internal.copyOf(orders);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!orders.isEmpty()) builder.append(", orders=").append(orders);
    return builder.replace(0, 2, "DrawOrderS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DrawOrderS, Builder> {
    public List<DrawOrder> orders;

    public Builder() {
      orders = Internal.newMutableList();
    }

    public Builder orders(List<DrawOrder> orders) {
      Internal.checkElementsNotNull(orders);
      this.orders = orders;
      return this;
    }

    @Override
    public DrawOrderS build() {
      return new DrawOrderS(orders, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DrawOrderS extends ProtoAdapter<DrawOrderS> {
    public ProtoAdapter_DrawOrderS() {
      super(FieldEncoding.LENGTH_DELIMITED, DrawOrderS.class, "type.googleapis.com/haiyaa.DrawOrderS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(DrawOrderS value) {
      int result = 0;
      result += DrawOrder.ADAPTER.asRepeated().encodedSizeWithTag(1, value.orders);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, DrawOrderS value) throws IOException {
      DrawOrder.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.orders);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DrawOrderS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.orders.add(DrawOrder.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public DrawOrderS redact(DrawOrderS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.orders, DrawOrder.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
