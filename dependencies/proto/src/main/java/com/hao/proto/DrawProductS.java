// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.DrawProductS in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * 提现商品
 */
public final class DrawProductS extends Message<DrawProductS, DrawProductS.Builder> {
  public static final ProtoAdapter<DrawProductS> ADAPTER = new ProtoAdapter_DrawProductS();

  private static final long serialVersionUID = 0L;

  public List<DrawProduct> products;

  public DrawProductS(List<DrawProduct> products) {
    this(products, ByteString.EMPTY);
  }

  public DrawProductS(List<DrawProduct> products, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.products = Internal.immutableCopyOf("products", products);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.products = Internal.copyOf(products);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!products.isEmpty()) builder.append(", products=").append(products);
    return builder.replace(0, 2, "DrawProductS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<DrawProductS, Builder> {
    public List<DrawProduct> products;

    public Builder() {
      products = Internal.newMutableList();
    }

    public Builder products(List<DrawProduct> products) {
      Internal.checkElementsNotNull(products);
      this.products = products;
      return this;
    }

    @Override
    public DrawProductS build() {
      return new DrawProductS(products, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_DrawProductS extends ProtoAdapter<DrawProductS> {
    public ProtoAdapter_DrawProductS() {
      super(FieldEncoding.LENGTH_DELIMITED, DrawProductS.class, "type.googleapis.com/haiyaa.DrawProductS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(DrawProductS value) {
      int result = 0;
      result += DrawProduct.ADAPTER.asRepeated().encodedSizeWithTag(1, value.products);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, DrawProductS value) throws IOException {
      DrawProduct.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.products);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public DrawProductS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.products.add(DrawProduct.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public DrawProductS redact(DrawProductS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.products, DrawProduct.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
