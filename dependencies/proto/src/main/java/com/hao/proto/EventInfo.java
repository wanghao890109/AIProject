// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.EventInfo in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

/**
 * OrderEvent
 */
public final class EventInfo extends Message<EventInfo, EventInfo.Builder> {
  public static final ProtoAdapter<EventInfo> ADAPTER = new ProtoAdapter_EventInfo();

  private static final long serialVersionUID = 0L;

  /**
   * EventId，后端生成
   */
  public long eventId;

  /**
   * 事件名称
   */
  public String eventName;

  /**
   * 事件主题
   */
  public String topic;

  /**
   * 事件描述
   */
  public String descrip;

  /**
   * 隐私类型，不填，跟房间对应。 0 未设置 1、默认公开 2、社交 3、私人
   */
  public int privacy;

  /**
   * 创建者信息
   */
  public UserBase creater;

  /**
   * Co-Host or Guest
   */
  public List<UserBase> cohost;

  /**
   * 事件时间
   */
  public long eventTime;

  public EventInfo(long eventId, String eventName, String topic, String descrip, int privacy,
      UserBase creater, List<UserBase> cohost, long eventTime) {
    this(eventId, eventName, topic, descrip, privacy, creater, cohost, eventTime, ByteString.EMPTY);
  }

  public EventInfo(long eventId, String eventName, String topic, String descrip, int privacy,
      UserBase creater, List<UserBase> cohost, long eventTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.eventId = eventId;
    if (eventName == null) {
      throw new IllegalArgumentException("eventName == null");
    }
    this.eventName = eventName;
    if (topic == null) {
      throw new IllegalArgumentException("topic == null");
    }
    this.topic = topic;
    if (descrip == null) {
      throw new IllegalArgumentException("descrip == null");
    }
    this.descrip = descrip;
    this.privacy = privacy;
    this.creater = creater;
    this.cohost = Internal.immutableCopyOf("cohost", cohost);
    this.eventTime = eventTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.eventId = eventId;
    builder.eventName = eventName;
    builder.topic = topic;
    builder.descrip = descrip;
    builder.privacy = privacy;
    builder.creater = creater;
    builder.cohost = Internal.copyOf(cohost);
    builder.eventTime = eventTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", eventId=").append(eventId);
    if (eventName != null) builder.append(", eventName=").append(Internal.sanitize(eventName));
    if (topic != null) builder.append(", topic=").append(Internal.sanitize(topic));
    if (descrip != null) builder.append(", descrip=").append(Internal.sanitize(descrip));
    builder.append(", privacy=").append(privacy);
    if (creater != null) builder.append(", creater=").append(creater);
    if (!cohost.isEmpty()) builder.append(", cohost=").append(cohost);
    builder.append(", eventTime=").append(eventTime);
    return builder.replace(0, 2, "EventInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<EventInfo, Builder> {
    public long eventId;

    public String eventName;

    public String topic;

    public String descrip;

    public int privacy;

    public UserBase creater;

    public List<UserBase> cohost;

    public long eventTime;

    public Builder() {
      eventId = 0L;
      eventName = "";
      topic = "";
      descrip = "";
      privacy = 0;
      cohost = Internal.newMutableList();
      eventTime = 0L;
    }

    /**
     * EventId，后端生成
     */
    public Builder eventId(long eventId) {
      this.eventId = eventId;
      return this;
    }

    /**
     * 事件名称
     */
    public Builder eventName(String eventName) {
      this.eventName = eventName;
      return this;
    }

    /**
     * 事件主题
     */
    public Builder topic(String topic) {
      this.topic = topic;
      return this;
    }

    /**
     * 事件描述
     */
    public Builder descrip(String descrip) {
      this.descrip = descrip;
      return this;
    }

    /**
     * 隐私类型，不填，跟房间对应。 0 未设置 1、默认公开 2、社交 3、私人
     */
    public Builder privacy(int privacy) {
      this.privacy = privacy;
      return this;
    }

    /**
     * 创建者信息
     */
    public Builder creater(UserBase creater) {
      this.creater = creater;
      return this;
    }

    /**
     * Co-Host or Guest
     */
    public Builder cohost(List<UserBase> cohost) {
      Internal.checkElementsNotNull(cohost);
      this.cohost = cohost;
      return this;
    }

    /**
     * 事件时间
     */
    public Builder eventTime(long eventTime) {
      this.eventTime = eventTime;
      return this;
    }

    @Override
    public EventInfo build() {
      return new EventInfo(eventId, eventName, topic, descrip, privacy, creater, cohost, eventTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_EventInfo extends ProtoAdapter<EventInfo> {
    public ProtoAdapter_EventInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, EventInfo.class, "type.googleapis.com/haiyaa.EventInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(EventInfo value) {
      int result = 0;
      if (!Objects.equals(value.eventId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.eventId);
      }
      if (!Objects.equals(value.eventName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.eventName);
      }
      if (!Objects.equals(value.topic, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.topic);
      }
      if (!Objects.equals(value.descrip, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.descrip);
      }
      if (!Objects.equals(value.privacy, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(5, value.privacy);
      }
      if (!Objects.equals(value.creater, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(6, value.creater);
      }
      result += UserBase.ADAPTER.asRepeated().encodedSizeWithTag(7, value.cohost);
      if (!Objects.equals(value.eventTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(8, value.eventTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, EventInfo value) throws IOException {
      if (!Objects.equals(value.eventId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.eventId);
      if (!Objects.equals(value.eventName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.eventName);
      if (!Objects.equals(value.topic, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.topic);
      if (!Objects.equals(value.descrip, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.descrip);
      if (!Objects.equals(value.privacy, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 5, value.privacy);
      if (!Objects.equals(value.creater, null)) UserBase.ADAPTER.encodeWithTag(writer, 6, value.creater);
      UserBase.ADAPTER.asRepeated().encodeWithTag(writer, 7, value.cohost);
      if (!Objects.equals(value.eventTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 8, value.eventTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public EventInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.eventId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.eventName(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.topic(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.descrip(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.privacy(ProtoAdapter.INT32.decode(reader)); break;
          case 6: builder.creater(UserBase.ADAPTER.decode(reader)); break;
          case 7: builder.cohost.add(UserBase.ADAPTER.decode(reader)); break;
          case 8: builder.eventTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public EventInfo redact(EventInfo value) {
      Builder builder = value.newBuilder();
      if (builder.creater != null) builder.creater = UserBase.ADAPTER.redact(builder.creater);
      Internal.redactElements(builder.cohost, UserBase.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
