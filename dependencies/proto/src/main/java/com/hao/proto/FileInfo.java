// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.FileInfo in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class FileInfo extends Message<FileInfo, FileInfo.Builder> {
  public static final ProtoAdapter<FileInfo> ADAPTER = new ProtoAdapter_FileInfo();

  private static final long serialVersionUID = 0L;

  public String name;

  /**
   * 文件大小;
   */
  public long size;

  public String url;

  public FileInfo(String name, long size, String url) {
    this(name, size, url, ByteString.EMPTY);
  }

  public FileInfo(String name, long size, String url, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (name == null) {
      throw new IllegalArgumentException("name == null");
    }
    this.name = name;
    this.size = size;
    if (url == null) {
      throw new IllegalArgumentException("url == null");
    }
    this.url = url;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.name = name;
    builder.size = size;
    builder.url = url;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (name != null) builder.append(", name=").append(Internal.sanitize(name));
    builder.append(", size=").append(size);
    if (url != null) builder.append(", url=").append(Internal.sanitize(url));
    return builder.replace(0, 2, "FileInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<FileInfo, Builder> {
    public String name;

    public long size;

    public String url;

    public Builder() {
      name = "";
      size = 0L;
      url = "";
    }

    public Builder name(String name) {
      this.name = name;
      return this;
    }

    /**
     * 文件大小;
     */
    public Builder size(long size) {
      this.size = size;
      return this;
    }

    public Builder url(String url) {
      this.url = url;
      return this;
    }

    @Override
    public FileInfo build() {
      return new FileInfo(name, size, url, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_FileInfo extends ProtoAdapter<FileInfo> {
    public ProtoAdapter_FileInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, FileInfo.class, "type.googleapis.com/haiyaa.FileInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(FileInfo value) {
      int result = 0;
      if (!Objects.equals(value.name, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.name);
      }
      if (!Objects.equals(value.size, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.size);
      }
      if (!Objects.equals(value.url, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.url);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, FileInfo value) throws IOException {
      if (!Objects.equals(value.name, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.name);
      if (!Objects.equals(value.size, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.size);
      if (!Objects.equals(value.url, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.url);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public FileInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.name(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.size(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.url(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public FileInfo redact(FileInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
