// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.FuzzySearch in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

/**
 * OrderEvent
 */
public final class FuzzySearch extends Message<FuzzySearch, FuzzySearch.Builder> {
  public static final ProtoAdapter<FuzzySearch> ADAPTER = new ProtoAdapter_FuzzySearch();

  private static final long serialVersionUID = 0L;

  /**
   * 用户
   */
  public List<UserInfo> Users;

  /**
   * 房间
   */
  public List<RoomInfo> Rooms;

  public FuzzySearch(List<UserInfo> Users, List<RoomInfo> Rooms) {
    this(Users, Rooms, ByteString.EMPTY);
  }

  public FuzzySearch(List<UserInfo> Users, List<RoomInfo> Rooms, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.Users = Internal.immutableCopyOf("Users", Users);
    this.Rooms = Internal.immutableCopyOf("Rooms", Rooms);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.Users = Internal.copyOf(Users);
    builder.Rooms = Internal.copyOf(Rooms);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!Users.isEmpty()) builder.append(", Users=").append(Users);
    if (!Rooms.isEmpty()) builder.append(", Rooms=").append(Rooms);
    return builder.replace(0, 2, "FuzzySearch{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<FuzzySearch, Builder> {
    public List<UserInfo> Users;

    public List<RoomInfo> Rooms;

    public Builder() {
      Users = Internal.newMutableList();
      Rooms = Internal.newMutableList();
    }

    /**
     * 用户
     */
    public Builder Users(List<UserInfo> Users) {
      Internal.checkElementsNotNull(Users);
      this.Users = Users;
      return this;
    }

    /**
     * 房间
     */
    public Builder Rooms(List<RoomInfo> Rooms) {
      Internal.checkElementsNotNull(Rooms);
      this.Rooms = Rooms;
      return this;
    }

    @Override
    public FuzzySearch build() {
      return new FuzzySearch(Users, Rooms, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_FuzzySearch extends ProtoAdapter<FuzzySearch> {
    public ProtoAdapter_FuzzySearch() {
      super(FieldEncoding.LENGTH_DELIMITED, FuzzySearch.class, "type.googleapis.com/haiyaa.FuzzySearch", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(FuzzySearch value) {
      int result = 0;
      result += UserInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.Users);
      result += RoomInfo.ADAPTER.asRepeated().encodedSizeWithTag(2, value.Rooms);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, FuzzySearch value) throws IOException {
      UserInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.Users);
      RoomInfo.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.Rooms);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public FuzzySearch decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.Users.add(UserInfo.ADAPTER.decode(reader)); break;
          case 2: builder.Rooms.add(RoomInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public FuzzySearch redact(FuzzySearch value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.Users, UserInfo.ADAPTER);
      Internal.redactElements(builder.Rooms, RoomInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
