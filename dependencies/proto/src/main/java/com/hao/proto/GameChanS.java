// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.GameChanS in game.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class GameChanS extends Message<GameChanS, GameChanS.Builder> {
  public static final ProtoAdapter<GameChanS> ADAPTER = new ProtoAdapter_GameChanS();

  private static final long serialVersionUID = 0L;

  public List<GameChan> chans;

  /**
   * Game 优先显示的ChannelId
   */
  public int defaultChan;

  public GameChanS(List<GameChan> chans, int defaultChan) {
    this(chans, defaultChan, ByteString.EMPTY);
  }

  public GameChanS(List<GameChan> chans, int defaultChan, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.chans = Internal.immutableCopyOf("chans", chans);
    this.defaultChan = defaultChan;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.chans = Internal.copyOf(chans);
    builder.defaultChan = defaultChan;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!chans.isEmpty()) builder.append(", chans=").append(chans);
    builder.append(", defaultChan=").append(defaultChan);
    return builder.replace(0, 2, "GameChanS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<GameChanS, Builder> {
    public List<GameChan> chans;

    public int defaultChan;

    public Builder() {
      chans = Internal.newMutableList();
      defaultChan = 0;
    }

    public Builder chans(List<GameChan> chans) {
      Internal.checkElementsNotNull(chans);
      this.chans = chans;
      return this;
    }

    /**
     * Game 优先显示的ChannelId
     */
    public Builder defaultChan(int defaultChan) {
      this.defaultChan = defaultChan;
      return this;
    }

    @Override
    public GameChanS build() {
      return new GameChanS(chans, defaultChan, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_GameChanS extends ProtoAdapter<GameChanS> {
    public ProtoAdapter_GameChanS() {
      super(FieldEncoding.LENGTH_DELIMITED, GameChanS.class, "type.googleapis.com/haiyaa.GameChanS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(GameChanS value) {
      int result = 0;
      result += GameChan.ADAPTER.asRepeated().encodedSizeWithTag(1, value.chans);
      if (!Objects.equals(value.defaultChan, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.defaultChan);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, GameChanS value) throws IOException {
      GameChan.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.chans);
      if (!Objects.equals(value.defaultChan, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.defaultChan);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public GameChanS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.chans.add(GameChan.ADAPTER.decode(reader)); break;
          case 2: builder.defaultChan(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public GameChanS redact(GameChanS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.chans, GameChan.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
