// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.GameItem in game.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

/**
 * for cli
 */
public final class GameItem extends Message<GameItem, GameItem.Builder> {
  public static final ProtoAdapter<GameItem> ADAPTER = new ProtoAdapter_GameItem();

  private static final long serialVersionUID = 0L;

  public RoomGame RmGame;

  /**
   * 房间ID
   */
  public long roomId;

  /**
   * 房间名
   */
  public String roomName;

  /**
   * 准备中头像
   */
  public List<UserBase> Users;

  /**
   * 锁房状态，0未锁，1锁房
   */
  public int swLock;

  public GameItem(RoomGame RmGame, long roomId, String roomName, List<UserBase> Users, int swLock) {
    this(RmGame, roomId, roomName, Users, swLock, ByteString.EMPTY);
  }

  public GameItem(RoomGame RmGame, long roomId, String roomName, List<UserBase> Users, int swLock,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.RmGame = RmGame;
    this.roomId = roomId;
    if (roomName == null) {
      throw new IllegalArgumentException("roomName == null");
    }
    this.roomName = roomName;
    this.Users = Internal.immutableCopyOf("Users", Users);
    this.swLock = swLock;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.RmGame = RmGame;
    builder.roomId = roomId;
    builder.roomName = roomName;
    builder.Users = Internal.copyOf(Users);
    builder.swLock = swLock;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (RmGame != null) builder.append(", RmGame=").append(RmGame);
    builder.append(", roomId=").append(roomId);
    if (roomName != null) builder.append(", roomName=").append(Internal.sanitize(roomName));
    if (!Users.isEmpty()) builder.append(", Users=").append(Users);
    builder.append(", swLock=").append(swLock);
    return builder.replace(0, 2, "GameItem{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<GameItem, Builder> {
    public RoomGame RmGame;

    public long roomId;

    public String roomName;

    public List<UserBase> Users;

    public int swLock;

    public Builder() {
      roomId = 0L;
      roomName = "";
      Users = Internal.newMutableList();
      swLock = 0;
    }

    public Builder RmGame(RoomGame RmGame) {
      this.RmGame = RmGame;
      return this;
    }

    /**
     * 房间ID
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 房间名
     */
    public Builder roomName(String roomName) {
      this.roomName = roomName;
      return this;
    }

    /**
     * 准备中头像
     */
    public Builder Users(List<UserBase> Users) {
      Internal.checkElementsNotNull(Users);
      this.Users = Users;
      return this;
    }

    /**
     * 锁房状态，0未锁，1锁房
     */
    public Builder swLock(int swLock) {
      this.swLock = swLock;
      return this;
    }

    @Override
    public GameItem build() {
      return new GameItem(RmGame, roomId, roomName, Users, swLock, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_GameItem extends ProtoAdapter<GameItem> {
    public ProtoAdapter_GameItem() {
      super(FieldEncoding.LENGTH_DELIMITED, GameItem.class, "type.googleapis.com/haiyaa.GameItem", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(GameItem value) {
      int result = 0;
      if (!Objects.equals(value.RmGame, null)) {
        result += RoomGame.ADAPTER.encodedSizeWithTag(1, value.RmGame);
      }
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.roomId);
      }
      if (!Objects.equals(value.roomName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.roomName);
      }
      result += UserBase.ADAPTER.asRepeated().encodedSizeWithTag(4, value.Users);
      if (!Objects.equals(value.swLock, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(8, value.swLock);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, GameItem value) throws IOException {
      if (!Objects.equals(value.RmGame, null)) RoomGame.ADAPTER.encodeWithTag(writer, 1, value.RmGame);
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.roomId);
      if (!Objects.equals(value.roomName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.roomName);
      UserBase.ADAPTER.asRepeated().encodeWithTag(writer, 4, value.Users);
      if (!Objects.equals(value.swLock, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 8, value.swLock);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public GameItem decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.RmGame(RoomGame.ADAPTER.decode(reader)); break;
          case 2: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.roomName(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.Users.add(UserBase.ADAPTER.decode(reader)); break;
          case 8: builder.swLock(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public GameItem redact(GameItem value) {
      Builder builder = value.newBuilder();
      if (builder.RmGame != null) builder.RmGame = RoomGame.ADAPTER.redact(builder.RmGame);
      Internal.redactElements(builder.Users, UserBase.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
