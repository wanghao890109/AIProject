// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.GamerState in game.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class GamerState extends Message<GamerState, GamerState.Builder> {
  public static final ProtoAdapter<GamerState> ADAPTER = new ProtoAdapter_GamerState();

  private static final long serialVersionUID = 0L;

  public long RoomId;

  public long UserId;

  /**
   * GS_READY 表示准备，其他表示未准备 OR GS_NONE
   */
  public int State;

  public GamerState(long RoomId, long UserId, int State) {
    this(RoomId, UserId, State, ByteString.EMPTY);
  }

  public GamerState(long RoomId, long UserId, int State, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.RoomId = RoomId;
    this.UserId = UserId;
    this.State = State;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.RoomId = RoomId;
    builder.UserId = UserId;
    builder.State = State;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", RoomId=").append(RoomId);
    builder.append(", UserId=").append(UserId);
    builder.append(", State=").append(State);
    return builder.replace(0, 2, "GamerState{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<GamerState, Builder> {
    public long RoomId;

    public long UserId;

    public int State;

    public Builder() {
      RoomId = 0L;
      UserId = 0L;
      State = 0;
    }

    public Builder RoomId(long RoomId) {
      this.RoomId = RoomId;
      return this;
    }

    public Builder UserId(long UserId) {
      this.UserId = UserId;
      return this;
    }

    /**
     * GS_READY 表示准备，其他表示未准备 OR GS_NONE
     */
    public Builder State(int State) {
      this.State = State;
      return this;
    }

    @Override
    public GamerState build() {
      return new GamerState(RoomId, UserId, State, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_GamerState extends ProtoAdapter<GamerState> {
    public ProtoAdapter_GamerState() {
      super(FieldEncoding.LENGTH_DELIMITED, GamerState.class, "type.googleapis.com/haiyaa.GamerState", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(GamerState value) {
      int result = 0;
      if (!Objects.equals(value.RoomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.RoomId);
      }
      if (!Objects.equals(value.UserId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.UserId);
      }
      if (!Objects.equals(value.State, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.State);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, GamerState value) throws IOException {
      if (!Objects.equals(value.RoomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.RoomId);
      if (!Objects.equals(value.UserId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.UserId);
      if (!Objects.equals(value.State, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.State);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public GamerState decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.RoomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.UserId(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.State(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public GamerState redact(GamerState value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
