// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.HeartRecordS in gift.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class HeartRecordS extends Message<HeartRecordS, HeartRecordS.Builder> {
  public static final ProtoAdapter<HeartRecordS> ADAPTER = new ProtoAdapter_HeartRecordS();

  private static final long serialVersionUID = 0L;

  /**
   * 爱心墙
   */
  public List<HeartRecord> Records;

  /**
   * 上一次阅读的时间，判断New
   */
  public long lastTime;

  public HeartRecordS(List<HeartRecord> Records, long lastTime) {
    this(Records, lastTime, ByteString.EMPTY);
  }

  public HeartRecordS(List<HeartRecord> Records, long lastTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.Records = Internal.immutableCopyOf("Records", Records);
    this.lastTime = lastTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.Records = Internal.copyOf(Records);
    builder.lastTime = lastTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!Records.isEmpty()) builder.append(", Records=").append(Records);
    builder.append(", lastTime=").append(lastTime);
    return builder.replace(0, 2, "HeartRecordS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<HeartRecordS, Builder> {
    public List<HeartRecord> Records;

    public long lastTime;

    public Builder() {
      Records = Internal.newMutableList();
      lastTime = 0L;
    }

    /**
     * 爱心墙
     */
    public Builder Records(List<HeartRecord> Records) {
      Internal.checkElementsNotNull(Records);
      this.Records = Records;
      return this;
    }

    /**
     * 上一次阅读的时间，判断New
     */
    public Builder lastTime(long lastTime) {
      this.lastTime = lastTime;
      return this;
    }

    @Override
    public HeartRecordS build() {
      return new HeartRecordS(Records, lastTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_HeartRecordS extends ProtoAdapter<HeartRecordS> {
    public ProtoAdapter_HeartRecordS() {
      super(FieldEncoding.LENGTH_DELIMITED, HeartRecordS.class, "type.googleapis.com/haiyaa.HeartRecordS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(HeartRecordS value) {
      int result = 0;
      result += HeartRecord.ADAPTER.asRepeated().encodedSizeWithTag(1, value.Records);
      if (!Objects.equals(value.lastTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(2, value.lastTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, HeartRecordS value) throws IOException {
      HeartRecord.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.Records);
      if (!Objects.equals(value.lastTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 2, value.lastTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public HeartRecordS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.Records.add(HeartRecord.ADAPTER.decode(reader)); break;
          case 2: builder.lastTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public HeartRecordS redact(HeartRecordS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.Records, HeartRecord.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
