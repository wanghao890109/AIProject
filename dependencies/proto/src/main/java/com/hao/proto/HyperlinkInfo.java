// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.HyperlinkInfo in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 图文链接
 */
public final class HyperlinkInfo extends Message<HyperlinkInfo, HyperlinkInfo.Builder> {
  public static final ProtoAdapter<HyperlinkInfo> ADAPTER = new ProtoAdapter_HyperlinkInfo();

  private static final long serialVersionUID = 0L;

  public HLinkType type;

  /**
   * 链接标题
   */
  public String title;

  public String link;

  /**
   * 链接图片
   */
  public String imgUrl;

  /**
   * 链接内容
   */
  public String text;

  public String extInfo;

  public HyperlinkInfo(HLinkType type, String title, String link, String imgUrl, String text,
      String extInfo) {
    this(type, title, link, imgUrl, text, extInfo, ByteString.EMPTY);
  }

  public HyperlinkInfo(HLinkType type, String title, String link, String imgUrl, String text,
      String extInfo, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (type == null) {
      throw new IllegalArgumentException("type == null");
    }
    this.type = type;
    if (title == null) {
      throw new IllegalArgumentException("title == null");
    }
    this.title = title;
    if (link == null) {
      throw new IllegalArgumentException("link == null");
    }
    this.link = link;
    if (imgUrl == null) {
      throw new IllegalArgumentException("imgUrl == null");
    }
    this.imgUrl = imgUrl;
    if (text == null) {
      throw new IllegalArgumentException("text == null");
    }
    this.text = text;
    if (extInfo == null) {
      throw new IllegalArgumentException("extInfo == null");
    }
    this.extInfo = extInfo;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.type = type;
    builder.title = title;
    builder.link = link;
    builder.imgUrl = imgUrl;
    builder.text = text;
    builder.extInfo = extInfo;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (type != null) builder.append(", type=").append(type);
    if (title != null) builder.append(", title=").append(Internal.sanitize(title));
    if (link != null) builder.append(", link=").append(Internal.sanitize(link));
    if (imgUrl != null) builder.append(", imgUrl=").append(Internal.sanitize(imgUrl));
    if (text != null) builder.append(", text=").append(Internal.sanitize(text));
    if (extInfo != null) builder.append(", extInfo=").append(Internal.sanitize(extInfo));
    return builder.replace(0, 2, "HyperlinkInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<HyperlinkInfo, Builder> {
    public HLinkType type;

    public String title;

    public String link;

    public String imgUrl;

    public String text;

    public String extInfo;

    public Builder() {
      type = HLinkType.HLT_URL;
      title = "";
      link = "";
      imgUrl = "";
      text = "";
      extInfo = "";
    }

    public Builder type(HLinkType type) {
      this.type = type;
      return this;
    }

    /**
     * 链接标题
     */
    public Builder title(String title) {
      this.title = title;
      return this;
    }

    public Builder link(String link) {
      this.link = link;
      return this;
    }

    /**
     * 链接图片
     */
    public Builder imgUrl(String imgUrl) {
      this.imgUrl = imgUrl;
      return this;
    }

    /**
     * 链接内容
     */
    public Builder text(String text) {
      this.text = text;
      return this;
    }

    public Builder extInfo(String extInfo) {
      this.extInfo = extInfo;
      return this;
    }

    @Override
    public HyperlinkInfo build() {
      return new HyperlinkInfo(type, title, link, imgUrl, text, extInfo, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_HyperlinkInfo extends ProtoAdapter<HyperlinkInfo> {
    public ProtoAdapter_HyperlinkInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, HyperlinkInfo.class, "type.googleapis.com/haiyaa.HyperlinkInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(HyperlinkInfo value) {
      int result = 0;
      if (!Objects.equals(value.type, HLinkType.HLT_URL)) {
        result += HLinkType.ADAPTER.encodedSizeWithTag(1, value.type);
      }
      if (!Objects.equals(value.title, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.title);
      }
      if (!Objects.equals(value.link, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.link);
      }
      if (!Objects.equals(value.imgUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.imgUrl);
      }
      if (!Objects.equals(value.text, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.text);
      }
      if (!Objects.equals(value.extInfo, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(6, value.extInfo);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, HyperlinkInfo value) throws IOException {
      if (!Objects.equals(value.type, HLinkType.HLT_URL)) HLinkType.ADAPTER.encodeWithTag(writer, 1, value.type);
      if (!Objects.equals(value.title, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.title);
      if (!Objects.equals(value.link, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.link);
      if (!Objects.equals(value.imgUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.imgUrl);
      if (!Objects.equals(value.text, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.text);
      if (!Objects.equals(value.extInfo, "")) ProtoAdapter.STRING.encodeWithTag(writer, 6, value.extInfo);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public HyperlinkInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: {
            try {
              builder.type(HLinkType.ADAPTER.decode(reader));
            } catch (ProtoAdapter.EnumConstantNotFoundException e) {
              builder.addUnknownField(tag, FieldEncoding.VARINT, (long) e.value);
            }
            break;
          }
          case 2: builder.title(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.link(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.imgUrl(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.text(ProtoAdapter.STRING.decode(reader)); break;
          case 6: builder.extInfo(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public HyperlinkInfo redact(HyperlinkInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
