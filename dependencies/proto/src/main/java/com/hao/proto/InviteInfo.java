// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.InviteInfo in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class InviteInfo extends Message<InviteInfo, InviteInfo.Builder> {
  public static final ProtoAdapter<InviteInfo> ADAPTER = new ProtoAdapter_InviteInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 剩余次数
   */
  public int limitn;

  /**
   * 邀请记录
   */
  public List<UserInvite> ivts;

  public InviteInfo(int limitn, List<UserInvite> ivts) {
    this(limitn, ivts, ByteString.EMPTY);
  }

  public InviteInfo(int limitn, List<UserInvite> ivts, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.limitn = limitn;
    this.ivts = Internal.immutableCopyOf("ivts", ivts);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.limitn = limitn;
    builder.ivts = Internal.copyOf(ivts);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", limitn=").append(limitn);
    if (!ivts.isEmpty()) builder.append(", ivts=").append(ivts);
    return builder.replace(0, 2, "InviteInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<InviteInfo, Builder> {
    public int limitn;

    public List<UserInvite> ivts;

    public Builder() {
      limitn = 0;
      ivts = Internal.newMutableList();
    }

    /**
     * 剩余次数
     */
    public Builder limitn(int limitn) {
      this.limitn = limitn;
      return this;
    }

    /**
     * 邀请记录
     */
    public Builder ivts(List<UserInvite> ivts) {
      Internal.checkElementsNotNull(ivts);
      this.ivts = ivts;
      return this;
    }

    @Override
    public InviteInfo build() {
      return new InviteInfo(limitn, ivts, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_InviteInfo extends ProtoAdapter<InviteInfo> {
    public ProtoAdapter_InviteInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, InviteInfo.class, "type.googleapis.com/haiyaa.InviteInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(InviteInfo value) {
      int result = 0;
      if (!Objects.equals(value.limitn, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(1, value.limitn);
      }
      result += UserInvite.ADAPTER.asRepeated().encodedSizeWithTag(2, value.ivts);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, InviteInfo value) throws IOException {
      if (!Objects.equals(value.limitn, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 1, value.limitn);
      UserInvite.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.ivts);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public InviteInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.limitn(ProtoAdapter.INT32.decode(reader)); break;
          case 2: builder.ivts.add(UserInvite.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public InviteInfo redact(InviteInfo value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.ivts, UserInvite.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
