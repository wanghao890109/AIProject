// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.JoinRoom in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 用户尝试进入加锁的房间
 */
public final class JoinRoom extends Message<JoinRoom, JoinRoom.Builder> {
  public static final ProtoAdapter<JoinRoom> ADAPTER = new ProtoAdapter_JoinRoom();

  private static final long serialVersionUID = 0L;

  /**
   * 房间Id
   */
  public long roomId;

  /**
   * 锁密码
   */
  public String passwd;

  public JoinRoom(long roomId, String passwd) {
    this(roomId, passwd, ByteString.EMPTY);
  }

  public JoinRoom(long roomId, String passwd, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.roomId = roomId;
    if (passwd == null) {
      throw new IllegalArgumentException("passwd == null");
    }
    this.passwd = passwd;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.roomId = roomId;
    builder.passwd = passwd;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", roomId=").append(roomId);
    if (passwd != null) builder.append(", passwd=").append(Internal.sanitize(passwd));
    return builder.replace(0, 2, "JoinRoom{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<JoinRoom, Builder> {
    public long roomId;

    public String passwd;

    public Builder() {
      roomId = 0L;
      passwd = "";
    }

    /**
     * 房间Id
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 锁密码
     */
    public Builder passwd(String passwd) {
      this.passwd = passwd;
      return this;
    }

    @Override
    public JoinRoom build() {
      return new JoinRoom(roomId, passwd, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_JoinRoom extends ProtoAdapter<JoinRoom> {
    public ProtoAdapter_JoinRoom() {
      super(FieldEncoding.LENGTH_DELIMITED, JoinRoom.class, "type.googleapis.com/haiyaa.JoinRoom", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(JoinRoom value) {
      int result = 0;
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.roomId);
      }
      if (!Objects.equals(value.passwd, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.passwd);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, JoinRoom value) throws IOException {
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.roomId);
      if (!Objects.equals(value.passwd, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.passwd);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public JoinRoom decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.passwd(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public JoinRoom redact(JoinRoom value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
