// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.LoveLettleS in gift.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class LoveLettleS extends Message<LoveLettleS, LoveLettleS.Builder> {
  public static final ProtoAdapter<LoveLettleS> ADAPTER = new ProtoAdapter_LoveLettleS();

  private static final long serialVersionUID = 0L;

  public List<LoveLettle> lettles;

  public LoveLettleS(List<LoveLettle> lettles) {
    this(lettles, ByteString.EMPTY);
  }

  public LoveLettleS(List<LoveLettle> lettles, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.lettles = Internal.immutableCopyOf("lettles", lettles);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.lettles = Internal.copyOf(lettles);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!lettles.isEmpty()) builder.append(", lettles=").append(lettles);
    return builder.replace(0, 2, "LoveLettleS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<LoveLettleS, Builder> {
    public List<LoveLettle> lettles;

    public Builder() {
      lettles = Internal.newMutableList();
    }

    public Builder lettles(List<LoveLettle> lettles) {
      Internal.checkElementsNotNull(lettles);
      this.lettles = lettles;
      return this;
    }

    @Override
    public LoveLettleS build() {
      return new LoveLettleS(lettles, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_LoveLettleS extends ProtoAdapter<LoveLettleS> {
    public ProtoAdapter_LoveLettleS() {
      super(FieldEncoding.LENGTH_DELIMITED, LoveLettleS.class, "type.googleapis.com/haiyaa.LoveLettleS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(LoveLettleS value) {
      int result = 0;
      result += LoveLettle.ADAPTER.asRepeated().encodedSizeWithTag(1, value.lettles);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, LoveLettleS value) throws IOException {
      LoveLettle.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.lettles);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public LoveLettleS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.lettles.add(LoveLettle.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public LoveLettleS redact(LoveLettleS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.lettles, LoveLettle.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
