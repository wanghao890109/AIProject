// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.MicroGame in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 房间中的麦上表情
 */
public final class MicroGame extends Message<MicroGame, MicroGame.Builder> {
  public static final ProtoAdapter<MicroGame> ADAPTER = new ProtoAdapter_MicroGame();

  private static final long serialVersionUID = 0L;

  /**
   * 房间Id
   */
  public long roomId;

  /**
   * 用户id
   */
  public long userId;

  /**
   * 表情id
   */
  public int gameId;

  /**
   * MicroId
   */
  public int micoId;

  /**
   * 结果
   */
  public int result;

  /**
   * 用户头像
   */
  public UserBase user;

  /**
   * 自定义随机区间
   */
  public int minRand;

  /**
   * 自定义随机区间
   */
  public int maxRand;

  public MicroGame(long roomId, long userId, int gameId, int micoId, int result, UserBase user,
      int minRand, int maxRand) {
    this(roomId, userId, gameId, micoId, result, user, minRand, maxRand, ByteString.EMPTY);
  }

  public MicroGame(long roomId, long userId, int gameId, int micoId, int result, UserBase user,
      int minRand, int maxRand, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.roomId = roomId;
    this.userId = userId;
    this.gameId = gameId;
    this.micoId = micoId;
    this.result = result;
    this.user = user;
    this.minRand = minRand;
    this.maxRand = maxRand;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.roomId = roomId;
    builder.userId = userId;
    builder.gameId = gameId;
    builder.micoId = micoId;
    builder.result = result;
    builder.user = user;
    builder.minRand = minRand;
    builder.maxRand = maxRand;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", roomId=").append(roomId);
    builder.append(", userId=").append(userId);
    builder.append(", gameId=").append(gameId);
    builder.append(", micoId=").append(micoId);
    builder.append(", result=").append(result);
    if (user != null) builder.append(", user=").append(user);
    builder.append(", minRand=").append(minRand);
    builder.append(", maxRand=").append(maxRand);
    return builder.replace(0, 2, "MicroGame{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<MicroGame, Builder> {
    public long roomId;

    public long userId;

    public int gameId;

    public int micoId;

    public int result;

    public UserBase user;

    public int minRand;

    public int maxRand;

    public Builder() {
      roomId = 0L;
      userId = 0L;
      gameId = 0;
      micoId = 0;
      result = 0;
      minRand = 0;
      maxRand = 0;
    }

    /**
     * 房间Id
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 用户id
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 表情id
     */
    public Builder gameId(int gameId) {
      this.gameId = gameId;
      return this;
    }

    /**
     * MicroId
     */
    public Builder micoId(int micoId) {
      this.micoId = micoId;
      return this;
    }

    /**
     * 结果
     */
    public Builder result(int result) {
      this.result = result;
      return this;
    }

    /**
     * 用户头像
     */
    public Builder user(UserBase user) {
      this.user = user;
      return this;
    }

    /**
     * 自定义随机区间
     */
    public Builder minRand(int minRand) {
      this.minRand = minRand;
      return this;
    }

    /**
     * 自定义随机区间
     */
    public Builder maxRand(int maxRand) {
      this.maxRand = maxRand;
      return this;
    }

    @Override
    public MicroGame build() {
      return new MicroGame(roomId, userId, gameId, micoId, result, user, minRand, maxRand, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_MicroGame extends ProtoAdapter<MicroGame> {
    public ProtoAdapter_MicroGame() {
      super(FieldEncoding.LENGTH_DELIMITED, MicroGame.class, "type.googleapis.com/haiyaa.MicroGame", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(MicroGame value) {
      int result_ = 0;
      if (!Objects.equals(value.roomId, 0L)) {
        result_ += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.roomId);
      }
      if (!Objects.equals(value.userId, 0L)) {
        result_ += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.userId);
      }
      if (!Objects.equals(value.gameId, 0)) {
        result_ += ProtoAdapter.INT32.encodedSizeWithTag(3, value.gameId);
      }
      if (!Objects.equals(value.micoId, 0)) {
        result_ += ProtoAdapter.INT32.encodedSizeWithTag(5, value.micoId);
      }
      if (!Objects.equals(value.result, 0)) {
        result_ += ProtoAdapter.INT32.encodedSizeWithTag(6, value.result);
      }
      if (!Objects.equals(value.user, null)) {
        result_ += UserBase.ADAPTER.encodedSizeWithTag(7, value.user);
      }
      if (!Objects.equals(value.minRand, 0)) {
        result_ += ProtoAdapter.INT32.encodedSizeWithTag(8, value.minRand);
      }
      if (!Objects.equals(value.maxRand, 0)) {
        result_ += ProtoAdapter.INT32.encodedSizeWithTag(9, value.maxRand);
      }
      result_ += value.unknownFields().size();
      return result_;
    }

    @Override
    public void encode(ProtoWriter writer, MicroGame value) throws IOException {
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.roomId);
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.userId);
      if (!Objects.equals(value.gameId, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.gameId);
      if (!Objects.equals(value.micoId, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 5, value.micoId);
      if (!Objects.equals(value.result, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 6, value.result);
      if (!Objects.equals(value.user, null)) UserBase.ADAPTER.encodeWithTag(writer, 7, value.user);
      if (!Objects.equals(value.minRand, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 8, value.minRand);
      if (!Objects.equals(value.maxRand, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 9, value.maxRand);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public MicroGame decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.gameId(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.micoId(ProtoAdapter.INT32.decode(reader)); break;
          case 6: builder.result(ProtoAdapter.INT32.decode(reader)); break;
          case 7: builder.user(UserBase.ADAPTER.decode(reader)); break;
          case 8: builder.minRand(ProtoAdapter.INT32.decode(reader)); break;
          case 9: builder.maxRand(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public MicroGame redact(MicroGame value) {
      Builder builder = value.newBuilder();
      if (builder.user != null) builder.user = UserBase.ADAPTER.redact(builder.user);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
