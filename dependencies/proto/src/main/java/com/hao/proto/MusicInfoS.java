// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.MusicInfoS in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class MusicInfoS extends Message<MusicInfoS, MusicInfoS.Builder> {
  public static final ProtoAdapter<MusicInfoS> ADAPTER = new ProtoAdapter_MusicInfoS();

  private static final long serialVersionUID = 0L;

  public String strId;

  public List<MusicInfo> musics;

  public MusicInfoS(String strId, List<MusicInfo> musics) {
    this(strId, musics, ByteString.EMPTY);
  }

  public MusicInfoS(String strId, List<MusicInfo> musics, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (strId == null) {
      throw new IllegalArgumentException("strId == null");
    }
    this.strId = strId;
    this.musics = Internal.immutableCopyOf("musics", musics);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.strId = strId;
    builder.musics = Internal.copyOf(musics);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (strId != null) builder.append(", strId=").append(Internal.sanitize(strId));
    if (!musics.isEmpty()) builder.append(", musics=").append(musics);
    return builder.replace(0, 2, "MusicInfoS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<MusicInfoS, Builder> {
    public String strId;

    public List<MusicInfo> musics;

    public Builder() {
      strId = "";
      musics = Internal.newMutableList();
    }

    public Builder strId(String strId) {
      this.strId = strId;
      return this;
    }

    public Builder musics(List<MusicInfo> musics) {
      Internal.checkElementsNotNull(musics);
      this.musics = musics;
      return this;
    }

    @Override
    public MusicInfoS build() {
      return new MusicInfoS(strId, musics, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_MusicInfoS extends ProtoAdapter<MusicInfoS> {
    public ProtoAdapter_MusicInfoS() {
      super(FieldEncoding.LENGTH_DELIMITED, MusicInfoS.class, "type.googleapis.com/haiyaa.MusicInfoS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(MusicInfoS value) {
      int result = 0;
      if (!Objects.equals(value.strId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.strId);
      }
      result += MusicInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.musics);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, MusicInfoS value) throws IOException {
      if (!Objects.equals(value.strId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.strId);
      MusicInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.musics);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public MusicInfoS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.musics.add(MusicInfo.ADAPTER.decode(reader)); break;
          case 2: builder.strId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public MusicInfoS redact(MusicInfoS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.musics, MusicInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
