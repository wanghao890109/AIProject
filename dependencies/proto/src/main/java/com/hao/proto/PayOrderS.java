// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.PayOrderS in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class PayOrderS extends Message<PayOrderS, PayOrderS.Builder> {
  public static final ProtoAdapter<PayOrderS> ADAPTER = new ProtoAdapter_PayOrderS();

  private static final long serialVersionUID = 0L;

  public List<PayOrder> orders;

  public PayOrderS(List<PayOrder> orders) {
    this(orders, ByteString.EMPTY);
  }

  public PayOrderS(List<PayOrder> orders, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.orders = Internal.immutableCopyOf("orders", orders);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.orders = Internal.copyOf(orders);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!orders.isEmpty()) builder.append(", orders=").append(orders);
    return builder.replace(0, 2, "PayOrderS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<PayOrderS, Builder> {
    public List<PayOrder> orders;

    public Builder() {
      orders = Internal.newMutableList();
    }

    public Builder orders(List<PayOrder> orders) {
      Internal.checkElementsNotNull(orders);
      this.orders = orders;
      return this;
    }

    @Override
    public PayOrderS build() {
      return new PayOrderS(orders, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PayOrderS extends ProtoAdapter<PayOrderS> {
    public ProtoAdapter_PayOrderS() {
      super(FieldEncoding.LENGTH_DELIMITED, PayOrderS.class, "type.googleapis.com/haiyaa.PayOrderS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(PayOrderS value) {
      int result = 0;
      result += PayOrder.ADAPTER.asRepeated().encodedSizeWithTag(1, value.orders);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, PayOrderS value) throws IOException {
      PayOrder.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.orders);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PayOrderS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.orders.add(PayOrder.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public PayOrderS redact(PayOrderS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.orders, PayOrder.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
