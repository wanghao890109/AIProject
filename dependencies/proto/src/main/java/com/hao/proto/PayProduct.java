// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.PayProduct in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 充值商品
 */
public final class PayProduct extends Message<PayProduct, PayProduct.Builder> {
  public static final ProtoAdapter<PayProduct> ADAPTER = new ProtoAdapter_PayProduct();

  private static final long serialVersionUID = 0L;

  /**
   * 商品Id
   */
  public String productId;

  /**
   * 礼物名称
   */
  public String name;

  /**
   * 支付渠道, GoodKind
   */
  public int kind;

  /**
   * 状态，是否上架 1、上架 2、下架，上架，且要在St-Ed时间范围内
   */
  public int status;

  /**
   * 商品价格
   */
  public double price;

  /**
   * 商品对应的金币
   */
  public int coin;

  /**
   * 商品资源地址
   */
  public String url;

  public PayProduct(String productId, String name, int kind, int status, double price, int coin,
      String url) {
    this(productId, name, kind, status, price, coin, url, ByteString.EMPTY);
  }

  public PayProduct(String productId, String name, int kind, int status, double price, int coin,
      String url, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (productId == null) {
      throw new IllegalArgumentException("productId == null");
    }
    this.productId = productId;
    if (name == null) {
      throw new IllegalArgumentException("name == null");
    }
    this.name = name;
    this.kind = kind;
    this.status = status;
    this.price = price;
    this.coin = coin;
    if (url == null) {
      throw new IllegalArgumentException("url == null");
    }
    this.url = url;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.productId = productId;
    builder.name = name;
    builder.kind = kind;
    builder.status = status;
    builder.price = price;
    builder.coin = coin;
    builder.url = url;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (productId != null) builder.append(", productId=").append(Internal.sanitize(productId));
    if (name != null) builder.append(", name=").append(Internal.sanitize(name));
    builder.append(", kind=").append(kind);
    builder.append(", status=").append(status);
    builder.append(", price=").append(price);
    builder.append(", coin=").append(coin);
    if (url != null) builder.append(", url=").append(Internal.sanitize(url));
    return builder.replace(0, 2, "PayProduct{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<PayProduct, Builder> {
    public String productId;

    public String name;

    public int kind;

    public int status;

    public double price;

    public int coin;

    public String url;

    public Builder() {
      productId = "";
      name = "";
      kind = 0;
      status = 0;
      price = 0.0;
      coin = 0;
      url = "";
    }

    /**
     * 商品Id
     */
    public Builder productId(String productId) {
      this.productId = productId;
      return this;
    }

    /**
     * 礼物名称
     */
    public Builder name(String name) {
      this.name = name;
      return this;
    }

    /**
     * 支付渠道, GoodKind
     */
    public Builder kind(int kind) {
      this.kind = kind;
      return this;
    }

    /**
     * 状态，是否上架 1、上架 2、下架，上架，且要在St-Ed时间范围内
     */
    public Builder status(int status) {
      this.status = status;
      return this;
    }

    /**
     * 商品价格
     */
    public Builder price(double price) {
      this.price = price;
      return this;
    }

    /**
     * 商品对应的金币
     */
    public Builder coin(int coin) {
      this.coin = coin;
      return this;
    }

    /**
     * 商品资源地址
     */
    public Builder url(String url) {
      this.url = url;
      return this;
    }

    @Override
    public PayProduct build() {
      return new PayProduct(productId, name, kind, status, price, coin, url, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PayProduct extends ProtoAdapter<PayProduct> {
    public ProtoAdapter_PayProduct() {
      super(FieldEncoding.LENGTH_DELIMITED, PayProduct.class, "type.googleapis.com/haiyaa.PayProduct", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(PayProduct value) {
      int result = 0;
      if (!Objects.equals(value.productId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.productId);
      }
      if (!Objects.equals(value.name, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.name);
      }
      if (!Objects.equals(value.kind, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.kind);
      }
      if (!Objects.equals(value.status, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(5, value.status);
      }
      if (!Objects.equals(value.price, 0.0)) {
        result += ProtoAdapter.DOUBLE.encodedSizeWithTag(6, value.price);
      }
      if (!Objects.equals(value.coin, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(7, value.coin);
      }
      if (!Objects.equals(value.url, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(8, value.url);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, PayProduct value) throws IOException {
      if (!Objects.equals(value.productId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.productId);
      if (!Objects.equals(value.name, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.name);
      if (!Objects.equals(value.kind, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.kind);
      if (!Objects.equals(value.status, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 5, value.status);
      if (!Objects.equals(value.price, 0.0)) ProtoAdapter.DOUBLE.encodeWithTag(writer, 6, value.price);
      if (!Objects.equals(value.coin, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 7, value.coin);
      if (!Objects.equals(value.url, "")) ProtoAdapter.STRING.encodeWithTag(writer, 8, value.url);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PayProduct decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 2: builder.productId(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.name(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.kind(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.status(ProtoAdapter.INT32.decode(reader)); break;
          case 6: builder.price(ProtoAdapter.DOUBLE.decode(reader)); break;
          case 7: builder.coin(ProtoAdapter.INT32.decode(reader)); break;
          case 8: builder.url(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public PayProduct redact(PayProduct value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
