// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.PbError in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class PbError extends Message<PbError, PbError.Builder> {
  public static final ProtoAdapter<PbError> ADAPTER = new ProtoAdapter_PbError();

  private static final long serialVersionUID = 0L;

  /**
   * 错误码
   */
  public int errCode;

  /**
   * 错误文案
   */
  public String errMsg;

  public PbError(int errCode, String errMsg) {
    this(errCode, errMsg, ByteString.EMPTY);
  }

  public PbError(int errCode, String errMsg, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.errCode = errCode;
    if (errMsg == null) {
      throw new IllegalArgumentException("errMsg == null");
    }
    this.errMsg = errMsg;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.errCode = errCode;
    builder.errMsg = errMsg;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", errCode=").append(errCode);
    if (errMsg != null) builder.append(", errMsg=").append(Internal.sanitize(errMsg));
    return builder.replace(0, 2, "PbError{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<PbError, Builder> {
    public int errCode;

    public String errMsg;

    public Builder() {
      errCode = 0;
      errMsg = "";
    }

    /**
     * 错误码
     */
    public Builder errCode(int errCode) {
      this.errCode = errCode;
      return this;
    }

    /**
     * 错误文案
     */
    public Builder errMsg(String errMsg) {
      this.errMsg = errMsg;
      return this;
    }

    @Override
    public PbError build() {
      return new PbError(errCode, errMsg, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PbError extends ProtoAdapter<PbError> {
    public ProtoAdapter_PbError() {
      super(FieldEncoding.LENGTH_DELIMITED, PbError.class, "type.googleapis.com/haiyaa.PbError", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(PbError value) {
      int result = 0;
      if (!Objects.equals(value.errCode, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(1, value.errCode);
      }
      if (!Objects.equals(value.errMsg, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.errMsg);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, PbError value) throws IOException {
      if (!Objects.equals(value.errCode, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 1, value.errCode);
      if (!Objects.equals(value.errMsg, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.errMsg);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PbError decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.errCode(ProtoAdapter.INT32.decode(reader)); break;
          case 2: builder.errMsg(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public PbError redact(PbError value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
