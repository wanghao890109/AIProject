// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.PbHead in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class PbHead extends Message<PbHead, PbHead.Builder> {
  public static final ProtoAdapter<PbHead> ADAPTER = new ProtoAdapter_PbHead();

  private static final long serialVersionUID = 0L;

  /**
   * 命令组
   */
  public int appId;

  /**
   * 命令字
   */
  public int cmd;

  /**
   * UserId， 用户Id = ReqBase.UserId
   */
  public long uid;

  /**
   * BussnissId, Svr生成
   */
  public int bid;

  /**
   * MessageId，业务号，客户端发起，原样返回
   */
  public int mid;

  /**
   * 头参
   */
  public int pam;

  /**
   * 服务器当前时间，client如果需要则使用
   */
  public int svrTime;

  /**
   * Token
   */
  public String token;

  /**
   * 业务PB，消息体 = ReqBase.Data = RetBase.Data
   */
  public ByteString pbBody;

  /**
   * 设备信息, 需要则填，不需要不填
   */
  public DeviceInfo dev;

  public PbHead(int appId, int cmd, long uid, int bid, int mid, int pam, int svrTime, String token,
      ByteString pbBody, DeviceInfo dev) {
    this(appId, cmd, uid, bid, mid, pam, svrTime, token, pbBody, dev, ByteString.EMPTY);
  }

  public PbHead(int appId, int cmd, long uid, int bid, int mid, int pam, int svrTime, String token,
      ByteString pbBody, DeviceInfo dev, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.appId = appId;
    this.cmd = cmd;
    this.uid = uid;
    this.bid = bid;
    this.mid = mid;
    this.pam = pam;
    this.svrTime = svrTime;
    if (token == null) {
      throw new IllegalArgumentException("token == null");
    }
    this.token = token;
    if (pbBody == null) {
      throw new IllegalArgumentException("pbBody == null");
    }
    this.pbBody = pbBody;
    this.dev = dev;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.appId = appId;
    builder.cmd = cmd;
    builder.uid = uid;
    builder.bid = bid;
    builder.mid = mid;
    builder.pam = pam;
    builder.svrTime = svrTime;
    builder.token = token;
    builder.pbBody = pbBody;
    builder.dev = dev;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", appId=").append(appId);
    builder.append(", cmd=").append(cmd);
    builder.append(", uid=").append(uid);
    builder.append(", bid=").append(bid);
    builder.append(", mid=").append(mid);
    builder.append(", pam=").append(pam);
    builder.append(", svrTime=").append(svrTime);
    if (token != null) builder.append(", token=").append(Internal.sanitize(token));
    if (pbBody != null) builder.append(", pbBody=").append(pbBody);
    if (dev != null) builder.append(", dev=").append(dev);
    return builder.replace(0, 2, "PbHead{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<PbHead, Builder> {
    public int appId;

    public int cmd;

    public long uid;

    public int bid;

    public int mid;

    public int pam;

    public int svrTime;

    public String token;

    public ByteString pbBody;

    public DeviceInfo dev;

    public Builder() {
      appId = 0;
      cmd = 0;
      uid = 0L;
      bid = 0;
      mid = 0;
      pam = 0;
      svrTime = 0;
      token = "";
      pbBody = ByteString.EMPTY;
    }

    /**
     * 命令组
     */
    public Builder appId(int appId) {
      this.appId = appId;
      return this;
    }

    /**
     * 命令字
     */
    public Builder cmd(int cmd) {
      this.cmd = cmd;
      return this;
    }

    /**
     * UserId， 用户Id = ReqBase.UserId
     */
    public Builder uid(long uid) {
      this.uid = uid;
      return this;
    }

    /**
     * BussnissId, Svr生成
     */
    public Builder bid(int bid) {
      this.bid = bid;
      return this;
    }

    /**
     * MessageId，业务号，客户端发起，原样返回
     */
    public Builder mid(int mid) {
      this.mid = mid;
      return this;
    }

    /**
     * 头参
     */
    public Builder pam(int pam) {
      this.pam = pam;
      return this;
    }

    /**
     * 服务器当前时间，client如果需要则使用
     */
    public Builder svrTime(int svrTime) {
      this.svrTime = svrTime;
      return this;
    }

    /**
     * Token
     */
    public Builder token(String token) {
      this.token = token;
      return this;
    }

    /**
     * 业务PB，消息体 = ReqBase.Data = RetBase.Data
     */
    public Builder pbBody(ByteString pbBody) {
      this.pbBody = pbBody;
      return this;
    }

    /**
     * 设备信息, 需要则填，不需要不填
     */
    public Builder dev(DeviceInfo dev) {
      this.dev = dev;
      return this;
    }

    @Override
    public PbHead build() {
      return new PbHead(appId, cmd, uid, bid, mid, pam, svrTime, token, pbBody, dev, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PbHead extends ProtoAdapter<PbHead> {
    public ProtoAdapter_PbHead() {
      super(FieldEncoding.LENGTH_DELIMITED, PbHead.class, "type.googleapis.com/haiyaa.PbHead", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(PbHead value) {
      int result = 0;
      if (!Objects.equals(value.appId, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(1, value.appId);
      }
      if (!Objects.equals(value.cmd, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(2, value.cmd);
      }
      if (!Objects.equals(value.uid, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(3, value.uid);
      }
      if (!Objects.equals(value.bid, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(4, value.bid);
      }
      if (!Objects.equals(value.mid, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(5, value.mid);
      }
      if (!Objects.equals(value.pam, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(6, value.pam);
      }
      if (!Objects.equals(value.svrTime, 0)) {
        result += ProtoAdapter.UINT32.encodedSizeWithTag(7, value.svrTime);
      }
      if (!Objects.equals(value.token, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(8, value.token);
      }
      if (!Objects.equals(value.pbBody, ByteString.EMPTY)) {
        result += ProtoAdapter.BYTES.encodedSizeWithTag(9, value.pbBody);
      }
      if (!Objects.equals(value.dev, null)) {
        result += DeviceInfo.ADAPTER.encodedSizeWithTag(10, value.dev);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, PbHead value) throws IOException {
      if (!Objects.equals(value.appId, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 1, value.appId);
      if (!Objects.equals(value.cmd, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 2, value.cmd);
      if (!Objects.equals(value.uid, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 3, value.uid);
      if (!Objects.equals(value.bid, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 4, value.bid);
      if (!Objects.equals(value.mid, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 5, value.mid);
      if (!Objects.equals(value.pam, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 6, value.pam);
      if (!Objects.equals(value.svrTime, 0)) ProtoAdapter.UINT32.encodeWithTag(writer, 7, value.svrTime);
      if (!Objects.equals(value.token, "")) ProtoAdapter.STRING.encodeWithTag(writer, 8, value.token);
      if (!Objects.equals(value.pbBody, ByteString.EMPTY)) ProtoAdapter.BYTES.encodeWithTag(writer, 9, value.pbBody);
      if (!Objects.equals(value.dev, null)) DeviceInfo.ADAPTER.encodeWithTag(writer, 10, value.dev);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PbHead decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.appId(ProtoAdapter.UINT32.decode(reader)); break;
          case 2: builder.cmd(ProtoAdapter.UINT32.decode(reader)); break;
          case 3: builder.uid(ProtoAdapter.UINT64.decode(reader)); break;
          case 4: builder.bid(ProtoAdapter.UINT32.decode(reader)); break;
          case 5: builder.mid(ProtoAdapter.UINT32.decode(reader)); break;
          case 6: builder.pam(ProtoAdapter.UINT32.decode(reader)); break;
          case 7: builder.svrTime(ProtoAdapter.UINT32.decode(reader)); break;
          case 8: builder.token(ProtoAdapter.STRING.decode(reader)); break;
          case 9: builder.pbBody(ProtoAdapter.BYTES.decode(reader)); break;
          case 10: builder.dev(DeviceInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public PbHead redact(PbHead value) {
      Builder builder = value.newBuilder();
      if (builder.dev != null) builder.dev = DeviceInfo.ADAPTER.redact(builder.dev);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
