// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.PhotoInfo in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class PhotoInfo extends Message<PhotoInfo, PhotoInfo.Builder> {
  public static final ProtoAdapter<PhotoInfo> ADAPTER = new ProtoAdapter_PhotoInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 图片宽度;
   */
  public long width;

  /**
   * 图片高度;
   */
  public long height;

  /**
   * 文件大小;
   */
  public long size;

  /**
   * 图片地址
   */
  public String url;

  /**
   * 用户备注
   */
  public String note;

  /**
   * 如果图片有名字
   */
  public String name;

  /**
   * 原图地址
   */
  public String originUrl;

  /**
   * 跳转地址
   */
  public String jumptoUrl;

  /**
   * 图像Id，用于相册
   */
  public String photoId;

  public PhotoInfo(long width, long height, long size, String url, String note, String name,
      String originUrl, String jumptoUrl, String photoId) {
    this(width, height, size, url, note, name, originUrl, jumptoUrl, photoId, ByteString.EMPTY);
  }

  public PhotoInfo(long width, long height, long size, String url, String note, String name,
      String originUrl, String jumptoUrl, String photoId, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.width = width;
    this.height = height;
    this.size = size;
    if (url == null) {
      throw new IllegalArgumentException("url == null");
    }
    this.url = url;
    if (note == null) {
      throw new IllegalArgumentException("note == null");
    }
    this.note = note;
    if (name == null) {
      throw new IllegalArgumentException("name == null");
    }
    this.name = name;
    if (originUrl == null) {
      throw new IllegalArgumentException("originUrl == null");
    }
    this.originUrl = originUrl;
    if (jumptoUrl == null) {
      throw new IllegalArgumentException("jumptoUrl == null");
    }
    this.jumptoUrl = jumptoUrl;
    if (photoId == null) {
      throw new IllegalArgumentException("photoId == null");
    }
    this.photoId = photoId;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.width = width;
    builder.height = height;
    builder.size = size;
    builder.url = url;
    builder.note = note;
    builder.name = name;
    builder.originUrl = originUrl;
    builder.jumptoUrl = jumptoUrl;
    builder.photoId = photoId;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", width=").append(width);
    builder.append(", height=").append(height);
    builder.append(", size=").append(size);
    if (url != null) builder.append(", url=").append(Internal.sanitize(url));
    if (note != null) builder.append(", note=").append(Internal.sanitize(note));
    if (name != null) builder.append(", name=").append(Internal.sanitize(name));
    if (originUrl != null) builder.append(", originUrl=").append(Internal.sanitize(originUrl));
    if (jumptoUrl != null) builder.append(", jumptoUrl=").append(Internal.sanitize(jumptoUrl));
    if (photoId != null) builder.append(", photoId=").append(Internal.sanitize(photoId));
    return builder.replace(0, 2, "PhotoInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<PhotoInfo, Builder> {
    public long width;

    public long height;

    public long size;

    public String url;

    public String note;

    public String name;

    public String originUrl;

    public String jumptoUrl;

    public String photoId;

    public Builder() {
      width = 0L;
      height = 0L;
      size = 0L;
      url = "";
      note = "";
      name = "";
      originUrl = "";
      jumptoUrl = "";
      photoId = "";
    }

    /**
     * 图片宽度;
     */
    public Builder width(long width) {
      this.width = width;
      return this;
    }

    /**
     * 图片高度;
     */
    public Builder height(long height) {
      this.height = height;
      return this;
    }

    /**
     * 文件大小;
     */
    public Builder size(long size) {
      this.size = size;
      return this;
    }

    /**
     * 图片地址
     */
    public Builder url(String url) {
      this.url = url;
      return this;
    }

    /**
     * 用户备注
     */
    public Builder note(String note) {
      this.note = note;
      return this;
    }

    /**
     * 如果图片有名字
     */
    public Builder name(String name) {
      this.name = name;
      return this;
    }

    /**
     * 原图地址
     */
    public Builder originUrl(String originUrl) {
      this.originUrl = originUrl;
      return this;
    }

    /**
     * 跳转地址
     */
    public Builder jumptoUrl(String jumptoUrl) {
      this.jumptoUrl = jumptoUrl;
      return this;
    }

    /**
     * 图像Id，用于相册
     */
    public Builder photoId(String photoId) {
      this.photoId = photoId;
      return this;
    }

    @Override
    public PhotoInfo build() {
      return new PhotoInfo(width, height, size, url, note, name, originUrl, jumptoUrl, photoId, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_PhotoInfo extends ProtoAdapter<PhotoInfo> {
    public ProtoAdapter_PhotoInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, PhotoInfo.class, "type.googleapis.com/haiyaa.PhotoInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(PhotoInfo value) {
      int result = 0;
      if (!Objects.equals(value.width, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(1, value.width);
      }
      if (!Objects.equals(value.height, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(2, value.height);
      }
      if (!Objects.equals(value.size, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(3, value.size);
      }
      if (!Objects.equals(value.url, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.url);
      }
      if (!Objects.equals(value.note, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.note);
      }
      if (!Objects.equals(value.name, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(6, value.name);
      }
      if (!Objects.equals(value.originUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(7, value.originUrl);
      }
      if (!Objects.equals(value.jumptoUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(8, value.jumptoUrl);
      }
      if (!Objects.equals(value.photoId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(9, value.photoId);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, PhotoInfo value) throws IOException {
      if (!Objects.equals(value.width, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 1, value.width);
      if (!Objects.equals(value.height, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 2, value.height);
      if (!Objects.equals(value.size, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 3, value.size);
      if (!Objects.equals(value.url, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.url);
      if (!Objects.equals(value.note, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.note);
      if (!Objects.equals(value.name, "")) ProtoAdapter.STRING.encodeWithTag(writer, 6, value.name);
      if (!Objects.equals(value.originUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 7, value.originUrl);
      if (!Objects.equals(value.jumptoUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 8, value.jumptoUrl);
      if (!Objects.equals(value.photoId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 9, value.photoId);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public PhotoInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.width(ProtoAdapter.INT64.decode(reader)); break;
          case 2: builder.height(ProtoAdapter.INT64.decode(reader)); break;
          case 3: builder.size(ProtoAdapter.INT64.decode(reader)); break;
          case 4: builder.url(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.note(ProtoAdapter.STRING.decode(reader)); break;
          case 6: builder.name(ProtoAdapter.STRING.decode(reader)); break;
          case 7: builder.originUrl(ProtoAdapter.STRING.decode(reader)); break;
          case 8: builder.jumptoUrl(ProtoAdapter.STRING.decode(reader)); break;
          case 9: builder.photoId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public PhotoInfo redact(PhotoInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
