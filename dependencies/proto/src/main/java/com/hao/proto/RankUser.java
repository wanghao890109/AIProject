// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RankUser in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class RankUser extends Message<RankUser, RankUser.Builder> {
  public static final ProtoAdapter<RankUser> ADAPTER = new ProtoAdapter_RankUser();

  private static final long serialVersionUID = 0L;

  /**
   * 排名id
   */
  public int rankId;

  /**
   * 用户id
   */
  public long userId;

  /**
   * 榜单值
   */
  public int rankVal;

  /**
   * 距离上一名或距离上榜差值
   */
  public int diffVal;

  /**
   * 当前所在的房间Id, 我关注的人也要加是否在房间中
   */
  public long roomId;

  /**
   * 用户头像，未必有, 如果是麦位状态变化，也忽略麦上人的因素
   */
  public UserBase user;

  public RankUser(int rankId, long userId, int rankVal, int diffVal, long roomId, UserBase user) {
    this(rankId, userId, rankVal, diffVal, roomId, user, ByteString.EMPTY);
  }

  public RankUser(int rankId, long userId, int rankVal, int diffVal, long roomId, UserBase user,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.rankId = rankId;
    this.userId = userId;
    this.rankVal = rankVal;
    this.diffVal = diffVal;
    this.roomId = roomId;
    this.user = user;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.rankId = rankId;
    builder.userId = userId;
    builder.rankVal = rankVal;
    builder.diffVal = diffVal;
    builder.roomId = roomId;
    builder.user = user;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", rankId=").append(rankId);
    builder.append(", userId=").append(userId);
    builder.append(", rankVal=").append(rankVal);
    builder.append(", diffVal=").append(diffVal);
    builder.append(", roomId=").append(roomId);
    if (user != null) builder.append(", user=").append(user);
    return builder.replace(0, 2, "RankUser{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RankUser, Builder> {
    public int rankId;

    public long userId;

    public int rankVal;

    public int diffVal;

    public long roomId;

    public UserBase user;

    public Builder() {
      rankId = 0;
      userId = 0L;
      rankVal = 0;
      diffVal = 0;
      roomId = 0L;
    }

    /**
     * 排名id
     */
    public Builder rankId(int rankId) {
      this.rankId = rankId;
      return this;
    }

    /**
     * 用户id
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 榜单值
     */
    public Builder rankVal(int rankVal) {
      this.rankVal = rankVal;
      return this;
    }

    /**
     * 距离上一名或距离上榜差值
     */
    public Builder diffVal(int diffVal) {
      this.diffVal = diffVal;
      return this;
    }

    /**
     * 当前所在的房间Id, 我关注的人也要加是否在房间中
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 用户头像，未必有, 如果是麦位状态变化，也忽略麦上人的因素
     */
    public Builder user(UserBase user) {
      this.user = user;
      return this;
    }

    @Override
    public RankUser build() {
      return new RankUser(rankId, userId, rankVal, diffVal, roomId, user, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RankUser extends ProtoAdapter<RankUser> {
    public ProtoAdapter_RankUser() {
      super(FieldEncoding.LENGTH_DELIMITED, RankUser.class, "type.googleapis.com/haiyaa.RankUser", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RankUser value) {
      int result = 0;
      if (!Objects.equals(value.rankId, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(1, value.rankId);
      }
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.userId);
      }
      if (!Objects.equals(value.rankVal, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.rankVal);
      }
      if (!Objects.equals(value.diffVal, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.diffVal);
      }
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(5, value.roomId);
      }
      if (!Objects.equals(value.user, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(6, value.user);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RankUser value) throws IOException {
      if (!Objects.equals(value.rankId, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 1, value.rankId);
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.userId);
      if (!Objects.equals(value.rankVal, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.rankVal);
      if (!Objects.equals(value.diffVal, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.diffVal);
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 5, value.roomId);
      if (!Objects.equals(value.user, null)) UserBase.ADAPTER.encodeWithTag(writer, 6, value.user);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RankUser decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.rankId(ProtoAdapter.INT32.decode(reader)); break;
          case 2: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.rankVal(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.diffVal(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 6: builder.user(UserBase.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RankUser redact(RankUser value) {
      Builder builder = value.newBuilder();
      if (builder.user != null) builder.user = UserBase.ADAPTER.redact(builder.user);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
