// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RankUserS in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class RankUserS extends Message<RankUserS, RankUserS.Builder> {
  public static final ProtoAdapter<RankUserS> ADAPTER = new ProtoAdapter_RankUserS();

  private static final long serialVersionUID = 0L;

  /**
   * 榜单，固定, 优化
   */
  public List<RankUser> rankers;

  /**
   * 用户自己
   */
  public RankUser self;

  public RankUserS(List<RankUser> rankers, RankUser self) {
    this(rankers, self, ByteString.EMPTY);
  }

  public RankUserS(List<RankUser> rankers, RankUser self, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.rankers = Internal.immutableCopyOf("rankers", rankers);
    this.self = self;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.rankers = Internal.copyOf(rankers);
    builder.self = self;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!rankers.isEmpty()) builder.append(", rankers=").append(rankers);
    if (self != null) builder.append(", self=").append(self);
    return builder.replace(0, 2, "RankUserS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RankUserS, Builder> {
    public List<RankUser> rankers;

    public RankUser self;

    public Builder() {
      rankers = Internal.newMutableList();
    }

    /**
     * 榜单，固定, 优化
     */
    public Builder rankers(List<RankUser> rankers) {
      Internal.checkElementsNotNull(rankers);
      this.rankers = rankers;
      return this;
    }

    /**
     * 用户自己
     */
    public Builder self(RankUser self) {
      this.self = self;
      return this;
    }

    @Override
    public RankUserS build() {
      return new RankUserS(rankers, self, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RankUserS extends ProtoAdapter<RankUserS> {
    public ProtoAdapter_RankUserS() {
      super(FieldEncoding.LENGTH_DELIMITED, RankUserS.class, "type.googleapis.com/haiyaa.RankUserS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RankUserS value) {
      int result = 0;
      result += RankUser.ADAPTER.asRepeated().encodedSizeWithTag(1, value.rankers);
      if (!Objects.equals(value.self, null)) {
        result += RankUser.ADAPTER.encodedSizeWithTag(2, value.self);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RankUserS value) throws IOException {
      RankUser.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.rankers);
      if (!Objects.equals(value.self, null)) RankUser.ADAPTER.encodeWithTag(writer, 2, value.self);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RankUserS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.rankers.add(RankUser.ADAPTER.decode(reader)); break;
          case 2: builder.self(RankUser.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RankUserS redact(RankUserS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.rankers, RankUser.ADAPTER);
      if (builder.self != null) builder.self = RankUser.ADAPTER.redact(builder.self);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
