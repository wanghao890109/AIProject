// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomChange in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class RoomChange extends Message<RoomChange, RoomChange.Builder> {
  public static final ProtoAdapter<RoomChange> ADAPTER = new ProtoAdapter_RoomChange();

  private static final long serialVersionUID = 0L;

  /**
   * 房间基础信息
   */
  public RoomBase rbase;

  /**
   * 房间麦位信息
   */
  public RoomMicroS micros;

  public RoomChange(RoomBase rbase, RoomMicroS micros) {
    this(rbase, micros, ByteString.EMPTY);
  }

  public RoomChange(RoomBase rbase, RoomMicroS micros, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.rbase = rbase;
    this.micros = micros;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.rbase = rbase;
    builder.micros = micros;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (rbase != null) builder.append(", rbase=").append(rbase);
    if (micros != null) builder.append(", micros=").append(micros);
    return builder.replace(0, 2, "RoomChange{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomChange, Builder> {
    public RoomBase rbase;

    public RoomMicroS micros;

    public Builder() {
    }

    /**
     * 房间基础信息
     */
    public Builder rbase(RoomBase rbase) {
      this.rbase = rbase;
      return this;
    }

    /**
     * 房间麦位信息
     */
    public Builder micros(RoomMicroS micros) {
      this.micros = micros;
      return this;
    }

    @Override
    public RoomChange build() {
      return new RoomChange(rbase, micros, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomChange extends ProtoAdapter<RoomChange> {
    public ProtoAdapter_RoomChange() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomChange.class, "type.googleapis.com/haiyaa.RoomChange", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomChange value) {
      int result = 0;
      if (!Objects.equals(value.rbase, null)) {
        result += RoomBase.ADAPTER.encodedSizeWithTag(1, value.rbase);
      }
      if (!Objects.equals(value.micros, null)) {
        result += RoomMicroS.ADAPTER.encodedSizeWithTag(2, value.micros);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomChange value) throws IOException {
      if (!Objects.equals(value.rbase, null)) RoomBase.ADAPTER.encodeWithTag(writer, 1, value.rbase);
      if (!Objects.equals(value.micros, null)) RoomMicroS.ADAPTER.encodeWithTag(writer, 2, value.micros);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomChange decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.rbase(RoomBase.ADAPTER.decode(reader)); break;
          case 2: builder.micros(RoomMicroS.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomChange redact(RoomChange value) {
      Builder builder = value.newBuilder();
      if (builder.rbase != null) builder.rbase = RoomBase.ADAPTER.redact(builder.rbase);
      if (builder.micros != null) builder.micros = RoomMicroS.ADAPTER.redact(builder.micros);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
