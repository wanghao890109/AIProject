// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomGameS in game.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class RoomGameS extends Message<RoomGameS, RoomGameS.Builder> {
  public static final ProtoAdapter<RoomGameS> ADAPTER = new ProtoAdapter_RoomGameS();

  private static final long serialVersionUID = 0L;

  public List<RoomGame> games;

  public RoomGameS(List<RoomGame> games) {
    this(games, ByteString.EMPTY);
  }

  public RoomGameS(List<RoomGame> games, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.games = Internal.immutableCopyOf("games", games);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.games = Internal.copyOf(games);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!games.isEmpty()) builder.append(", games=").append(games);
    return builder.replace(0, 2, "RoomGameS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomGameS, Builder> {
    public List<RoomGame> games;

    public Builder() {
      games = Internal.newMutableList();
    }

    public Builder games(List<RoomGame> games) {
      Internal.checkElementsNotNull(games);
      this.games = games;
      return this;
    }

    @Override
    public RoomGameS build() {
      return new RoomGameS(games, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomGameS extends ProtoAdapter<RoomGameS> {
    public ProtoAdapter_RoomGameS() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomGameS.class, "type.googleapis.com/haiyaa.RoomGameS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomGameS value) {
      int result = 0;
      result += RoomGame.ADAPTER.asRepeated().encodedSizeWithTag(1, value.games);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomGameS value) throws IOException {
      RoomGame.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.games);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomGameS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.games.add(RoomGame.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomGameS redact(RoomGameS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.games, RoomGame.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
