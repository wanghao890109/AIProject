// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomInfoS in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class RoomInfoS extends Message<RoomInfoS, RoomInfoS.Builder> {
  public static final ProtoAdapter<RoomInfoS> ADAPTER = new ProtoAdapter_RoomInfoS();

  private static final long serialVersionUID = 0L;

  public List<RoomInfo> rooms;

  public RoomInfoS(List<RoomInfo> rooms) {
    this(rooms, ByteString.EMPTY);
  }

  public RoomInfoS(List<RoomInfo> rooms, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.rooms = Internal.immutableCopyOf("rooms", rooms);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.rooms = Internal.copyOf(rooms);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!rooms.isEmpty()) builder.append(", rooms=").append(rooms);
    return builder.replace(0, 2, "RoomInfoS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomInfoS, Builder> {
    public List<RoomInfo> rooms;

    public Builder() {
      rooms = Internal.newMutableList();
    }

    public Builder rooms(List<RoomInfo> rooms) {
      Internal.checkElementsNotNull(rooms);
      this.rooms = rooms;
      return this;
    }

    @Override
    public RoomInfoS build() {
      return new RoomInfoS(rooms, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomInfoS extends ProtoAdapter<RoomInfoS> {
    public ProtoAdapter_RoomInfoS() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomInfoS.class, "type.googleapis.com/haiyaa.RoomInfoS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomInfoS value) {
      int result = 0;
      result += RoomInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.rooms);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomInfoS value) throws IOException {
      RoomInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.rooms);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomInfoS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.rooms.add(RoomInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomInfoS redact(RoomInfoS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.rooms, RoomInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
