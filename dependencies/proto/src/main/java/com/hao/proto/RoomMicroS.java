// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomMicroS in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class RoomMicroS extends Message<RoomMicroS, RoomMicroS.Builder> {
  public static final ProtoAdapter<RoomMicroS> ADAPTER = new ProtoAdapter_RoomMicroS();

  private static final long serialVersionUID = 0L;

  /**
   * 房间Id
   */
  public long roomId;

  public List<RoomMicro> micros;

  /**
   * 麦位序列号
   */
  public long serialno;

  public RoomMicroS(long roomId, List<RoomMicro> micros, long serialno) {
    this(roomId, micros, serialno, ByteString.EMPTY);
  }

  public RoomMicroS(long roomId, List<RoomMicro> micros, long serialno, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.roomId = roomId;
    this.micros = Internal.immutableCopyOf("micros", micros);
    this.serialno = serialno;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.roomId = roomId;
    builder.micros = Internal.copyOf(micros);
    builder.serialno = serialno;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", roomId=").append(roomId);
    if (!micros.isEmpty()) builder.append(", micros=").append(micros);
    builder.append(", serialno=").append(serialno);
    return builder.replace(0, 2, "RoomMicroS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomMicroS, Builder> {
    public long roomId;

    public List<RoomMicro> micros;

    public long serialno;

    public Builder() {
      roomId = 0L;
      micros = Internal.newMutableList();
      serialno = 0L;
    }

    /**
     * 房间Id
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    public Builder micros(List<RoomMicro> micros) {
      Internal.checkElementsNotNull(micros);
      this.micros = micros;
      return this;
    }

    /**
     * 麦位序列号
     */
    public Builder serialno(long serialno) {
      this.serialno = serialno;
      return this;
    }

    @Override
    public RoomMicroS build() {
      return new RoomMicroS(roomId, micros, serialno, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomMicroS extends ProtoAdapter<RoomMicroS> {
    public ProtoAdapter_RoomMicroS() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomMicroS.class, "type.googleapis.com/haiyaa.RoomMicroS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomMicroS value) {
      int result = 0;
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.roomId);
      }
      result += RoomMicro.ADAPTER.asRepeated().encodedSizeWithTag(2, value.micros);
      if (!Objects.equals(value.serialno, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(3, value.serialno);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomMicroS value) throws IOException {
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.roomId);
      RoomMicro.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.micros);
      if (!Objects.equals(value.serialno, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 3, value.serialno);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomMicroS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.micros.add(RoomMicro.ADAPTER.decode(reader)); break;
          case 3: builder.serialno(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomMicroS redact(RoomMicroS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.micros, RoomMicro.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
