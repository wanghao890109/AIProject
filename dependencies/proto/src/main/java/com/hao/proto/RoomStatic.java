// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomStatic in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 房间中的统计数据
 */
public final class RoomStatic extends Message<RoomStatic, RoomStatic.Builder> {
  public static final ProtoAdapter<RoomStatic> ADAPTER = new ProtoAdapter_RoomStatic();

  private static final long serialVersionUID = 0L;

  /**
   * 房间Id
   */
  public long roomId;

  /**
   * 当前人数
   */
  public int curNum;

  /**
   * 当前热度
   */
  public int curHot;

  /**
   * 热度排名
   */
  public int hotRank;

  public RoomStatic(long roomId, int curNum, int curHot, int hotRank) {
    this(roomId, curNum, curHot, hotRank, ByteString.EMPTY);
  }

  public RoomStatic(long roomId, int curNum, int curHot, int hotRank, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.roomId = roomId;
    this.curNum = curNum;
    this.curHot = curHot;
    this.hotRank = hotRank;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.roomId = roomId;
    builder.curNum = curNum;
    builder.curHot = curHot;
    builder.hotRank = hotRank;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", roomId=").append(roomId);
    builder.append(", curNum=").append(curNum);
    builder.append(", curHot=").append(curHot);
    builder.append(", hotRank=").append(hotRank);
    return builder.replace(0, 2, "RoomStatic{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomStatic, Builder> {
    public long roomId;

    public int curNum;

    public int curHot;

    public int hotRank;

    public Builder() {
      roomId = 0L;
      curNum = 0;
      curHot = 0;
      hotRank = 0;
    }

    /**
     * 房间Id
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 当前人数
     */
    public Builder curNum(int curNum) {
      this.curNum = curNum;
      return this;
    }

    /**
     * 当前热度
     */
    public Builder curHot(int curHot) {
      this.curHot = curHot;
      return this;
    }

    /**
     * 热度排名
     */
    public Builder hotRank(int hotRank) {
      this.hotRank = hotRank;
      return this;
    }

    @Override
    public RoomStatic build() {
      return new RoomStatic(roomId, curNum, curHot, hotRank, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomStatic extends ProtoAdapter<RoomStatic> {
    public ProtoAdapter_RoomStatic() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomStatic.class, "type.googleapis.com/haiyaa.RoomStatic", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomStatic value) {
      int result = 0;
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.roomId);
      }
      if (!Objects.equals(value.curNum, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.curNum);
      }
      if (!Objects.equals(value.curHot, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.curHot);
      }
      if (!Objects.equals(value.hotRank, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.hotRank);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomStatic value) throws IOException {
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.roomId);
      if (!Objects.equals(value.curNum, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.curNum);
      if (!Objects.equals(value.curHot, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.curHot);
      if (!Objects.equals(value.hotRank, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.hotRank);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomStatic decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.curNum(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.curHot(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.hotRank(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomStatic redact(RoomStatic value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
