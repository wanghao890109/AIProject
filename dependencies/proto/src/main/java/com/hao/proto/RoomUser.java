// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomUser in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class RoomUser extends Message<RoomUser, RoomUser.Builder> {
  public static final ProtoAdapter<RoomUser> ADAPTER = new ProtoAdapter_RoomUser();

  private static final long serialVersionUID = 0L;

  /**
   * 房间Id
   */
  public long roomId;

  /**
   * 用户id
   */
  public long userId;

  /**
   * 麦位Id, 0-MaxMicro 表示在麦上
   */
  public int microId;

  /**
   * 房间角色, Enum RoomRole
   */
  public int roomRole;

  /**
   * 用户头像，未必有, 如果是麦位状态变化，也忽略麦上人的因素
   */
  public UserBase user;

  public RoomUser(long roomId, long userId, int microId, int roomRole, UserBase user) {
    this(roomId, userId, microId, roomRole, user, ByteString.EMPTY);
  }

  public RoomUser(long roomId, long userId, int microId, int roomRole, UserBase user,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.roomId = roomId;
    this.userId = userId;
    this.microId = microId;
    this.roomRole = roomRole;
    this.user = user;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.roomId = roomId;
    builder.userId = userId;
    builder.microId = microId;
    builder.roomRole = roomRole;
    builder.user = user;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", roomId=").append(roomId);
    builder.append(", userId=").append(userId);
    builder.append(", microId=").append(microId);
    builder.append(", roomRole=").append(roomRole);
    if (user != null) builder.append(", user=").append(user);
    return builder.replace(0, 2, "RoomUser{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomUser, Builder> {
    public long roomId;

    public long userId;

    public int microId;

    public int roomRole;

    public UserBase user;

    public Builder() {
      roomId = 0L;
      userId = 0L;
      microId = 0;
      roomRole = 0;
    }

    /**
     * 房间Id
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 用户id
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 麦位Id, 0-MaxMicro 表示在麦上
     */
    public Builder microId(int microId) {
      this.microId = microId;
      return this;
    }

    /**
     * 房间角色, Enum RoomRole
     */
    public Builder roomRole(int roomRole) {
      this.roomRole = roomRole;
      return this;
    }

    /**
     * 用户头像，未必有, 如果是麦位状态变化，也忽略麦上人的因素
     */
    public Builder user(UserBase user) {
      this.user = user;
      return this;
    }

    @Override
    public RoomUser build() {
      return new RoomUser(roomId, userId, microId, roomRole, user, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomUser extends ProtoAdapter<RoomUser> {
    public ProtoAdapter_RoomUser() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomUser.class, "type.googleapis.com/haiyaa.RoomUser", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomUser value) {
      int result = 0;
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.roomId);
      }
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(2, value.userId);
      }
      if (!Objects.equals(value.microId, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.microId);
      }
      if (!Objects.equals(value.roomRole, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(5, value.roomRole);
      }
      if (!Objects.equals(value.user, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(6, value.user);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomUser value) throws IOException {
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.roomId);
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 2, value.userId);
      if (!Objects.equals(value.microId, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.microId);
      if (!Objects.equals(value.roomRole, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 5, value.roomRole);
      if (!Objects.equals(value.user, null)) UserBase.ADAPTER.encodeWithTag(writer, 6, value.user);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomUser decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 3: builder.microId(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.roomRole(ProtoAdapter.INT32.decode(reader)); break;
          case 6: builder.user(UserBase.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomUser redact(RoomUser value) {
      Builder builder = value.newBuilder();
      if (builder.user != null) builder.user = UserBase.ADAPTER.redact(builder.user);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
