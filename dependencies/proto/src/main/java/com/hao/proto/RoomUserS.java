// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.RoomUserS in room.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class RoomUserS extends Message<RoomUserS, RoomUserS.Builder> {
  public static final ProtoAdapter<RoomUserS> ADAPTER = new ProtoAdapter_RoomUserS();

  private static final long serialVersionUID = 0L;

  public int total;

  public int mgrNum;

  public List<RoomUser> users;

  public RoomUserS(int total, int mgrNum, List<RoomUser> users) {
    this(total, mgrNum, users, ByteString.EMPTY);
  }

  public RoomUserS(int total, int mgrNum, List<RoomUser> users, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.total = total;
    this.mgrNum = mgrNum;
    this.users = Internal.immutableCopyOf("users", users);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.total = total;
    builder.mgrNum = mgrNum;
    builder.users = Internal.copyOf(users);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", total=").append(total);
    builder.append(", mgrNum=").append(mgrNum);
    if (!users.isEmpty()) builder.append(", users=").append(users);
    return builder.replace(0, 2, "RoomUserS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<RoomUserS, Builder> {
    public int total;

    public int mgrNum;

    public List<RoomUser> users;

    public Builder() {
      total = 0;
      mgrNum = 0;
      users = Internal.newMutableList();
    }

    public Builder total(int total) {
      this.total = total;
      return this;
    }

    public Builder mgrNum(int mgrNum) {
      this.mgrNum = mgrNum;
      return this;
    }

    public Builder users(List<RoomUser> users) {
      Internal.checkElementsNotNull(users);
      this.users = users;
      return this;
    }

    @Override
    public RoomUserS build() {
      return new RoomUserS(total, mgrNum, users, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_RoomUserS extends ProtoAdapter<RoomUserS> {
    public ProtoAdapter_RoomUserS() {
      super(FieldEncoding.LENGTH_DELIMITED, RoomUserS.class, "type.googleapis.com/haiyaa.RoomUserS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(RoomUserS value) {
      int result = 0;
      if (!Objects.equals(value.total, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(1, value.total);
      }
      if (!Objects.equals(value.mgrNum, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.mgrNum);
      }
      result += RoomUser.ADAPTER.asRepeated().encodedSizeWithTag(3, value.users);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, RoomUserS value) throws IOException {
      if (!Objects.equals(value.total, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 1, value.total);
      if (!Objects.equals(value.mgrNum, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.mgrNum);
      RoomUser.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.users);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public RoomUserS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.total(ProtoAdapter.INT32.decode(reader)); break;
          case 2: builder.mgrNum(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.users.add(RoomUser.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public RoomUserS redact(RoomUserS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.users, RoomUser.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
