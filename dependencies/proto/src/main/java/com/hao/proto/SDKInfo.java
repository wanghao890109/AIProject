// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.SDKInfo in login.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class SDKInfo extends Message<SDKInfo, SDKInfo.Builder> {
  public static final ProtoAdapter<SDKInfo> ADAPTER = new ProtoAdapter_SDKInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 语音SDK Id
   */
  public String sdkId;

  /**
   * 语音SDK Token
   */
  public String sdkKey;

  public SDKInfo(String sdkId, String sdkKey) {
    this(sdkId, sdkKey, ByteString.EMPTY);
  }

  public SDKInfo(String sdkId, String sdkKey, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (sdkId == null) {
      throw new IllegalArgumentException("sdkId == null");
    }
    this.sdkId = sdkId;
    if (sdkKey == null) {
      throw new IllegalArgumentException("sdkKey == null");
    }
    this.sdkKey = sdkKey;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.sdkId = sdkId;
    builder.sdkKey = sdkKey;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (sdkId != null) builder.append(", sdkId=").append(Internal.sanitize(sdkId));
    if (sdkKey != null) builder.append(", sdkKey=").append(Internal.sanitize(sdkKey));
    return builder.replace(0, 2, "SDKInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<SDKInfo, Builder> {
    public String sdkId;

    public String sdkKey;

    public Builder() {
      sdkId = "";
      sdkKey = "";
    }

    /**
     * 语音SDK Id
     */
    public Builder sdkId(String sdkId) {
      this.sdkId = sdkId;
      return this;
    }

    /**
     * 语音SDK Token
     */
    public Builder sdkKey(String sdkKey) {
      this.sdkKey = sdkKey;
      return this;
    }

    @Override
    public SDKInfo build() {
      return new SDKInfo(sdkId, sdkKey, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_SDKInfo extends ProtoAdapter<SDKInfo> {
    public ProtoAdapter_SDKInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, SDKInfo.class, "type.googleapis.com/haiyaa.SDKInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(SDKInfo value) {
      int result = 0;
      if (!Objects.equals(value.sdkId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.sdkId);
      }
      if (!Objects.equals(value.sdkKey, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.sdkKey);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, SDKInfo value) throws IOException {
      if (!Objects.equals(value.sdkId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.sdkId);
      if (!Objects.equals(value.sdkKey, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.sdkKey);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public SDKInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.sdkId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.sdkKey(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public SDKInfo redact(SDKInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
