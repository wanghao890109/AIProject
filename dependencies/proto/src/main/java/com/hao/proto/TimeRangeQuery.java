// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.TimeRangeQuery in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class TimeRangeQuery extends Message<TimeRangeQuery, TimeRangeQuery.Builder> {
  public static final ProtoAdapter<TimeRangeQuery> ADAPTER = new ProtoAdapter_TimeRangeQuery();

  private static final long serialVersionUID = 0L;

  /**
   * 查询参数
   */
  public long userId;

  /**
   * 查询范围开始时间
   */
  public long begTime;

  /**
   * 查询范围结束时间
   */
  public long endTime;

  public TimeRangeQuery(long userId, long begTime, long endTime) {
    this(userId, begTime, endTime, ByteString.EMPTY);
  }

  public TimeRangeQuery(long userId, long begTime, long endTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.begTime = begTime;
    this.endTime = endTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.begTime = begTime;
    builder.endTime = endTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    builder.append(", begTime=").append(begTime);
    builder.append(", endTime=").append(endTime);
    return builder.replace(0, 2, "TimeRangeQuery{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TimeRangeQuery, Builder> {
    public long userId;

    public long begTime;

    public long endTime;

    public Builder() {
      userId = 0L;
      begTime = 0L;
      endTime = 0L;
    }

    /**
     * 查询参数
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 查询范围开始时间
     */
    public Builder begTime(long begTime) {
      this.begTime = begTime;
      return this;
    }

    /**
     * 查询范围结束时间
     */
    public Builder endTime(long endTime) {
      this.endTime = endTime;
      return this;
    }

    @Override
    public TimeRangeQuery build() {
      return new TimeRangeQuery(userId, begTime, endTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TimeRangeQuery extends ProtoAdapter<TimeRangeQuery> {
    public ProtoAdapter_TimeRangeQuery() {
      super(FieldEncoding.LENGTH_DELIMITED, TimeRangeQuery.class, "type.googleapis.com/haiyaa.TimeRangeQuery", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(TimeRangeQuery value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.begTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(2, value.begTime);
      }
      if (!Objects.equals(value.endTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(3, value.endTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, TimeRangeQuery value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.begTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 2, value.begTime);
      if (!Objects.equals(value.endTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 3, value.endTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TimeRangeQuery decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.begTime(ProtoAdapter.INT64.decode(reader)); break;
          case 3: builder.endTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public TimeRangeQuery redact(TimeRangeQuery value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
