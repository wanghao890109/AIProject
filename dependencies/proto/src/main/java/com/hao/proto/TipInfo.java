// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.TipInfo in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class TipInfo extends Message<TipInfo, TipInfo.Builder> {
  public static final ProtoAdapter<TipInfo> ADAPTER = new ProtoAdapter_TipInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 提示内容
   */
  public String text;

  /**
   * 提示内容
   */
  public String des;

  /**
   * ExtType;orderid;order-op;
   */
  public String extInfo;

  public TipInfo(String text, String des, String extInfo) {
    this(text, des, extInfo, ByteString.EMPTY);
  }

  public TipInfo(String text, String des, String extInfo, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (text == null) {
      throw new IllegalArgumentException("text == null");
    }
    this.text = text;
    if (des == null) {
      throw new IllegalArgumentException("des == null");
    }
    this.des = des;
    if (extInfo == null) {
      throw new IllegalArgumentException("extInfo == null");
    }
    this.extInfo = extInfo;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.text = text;
    builder.des = des;
    builder.extInfo = extInfo;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (text != null) builder.append(", text=").append(Internal.sanitize(text));
    if (des != null) builder.append(", des=").append(Internal.sanitize(des));
    if (extInfo != null) builder.append(", extInfo=").append(Internal.sanitize(extInfo));
    return builder.replace(0, 2, "TipInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TipInfo, Builder> {
    public String text;

    public String des;

    public String extInfo;

    public Builder() {
      text = "";
      des = "";
      extInfo = "";
    }

    /**
     * 提示内容
     */
    public Builder text(String text) {
      this.text = text;
      return this;
    }

    /**
     * 提示内容
     */
    public Builder des(String des) {
      this.des = des;
      return this;
    }

    /**
     * ExtType;orderid;order-op;
     */
    public Builder extInfo(String extInfo) {
      this.extInfo = extInfo;
      return this;
    }

    @Override
    public TipInfo build() {
      return new TipInfo(text, des, extInfo, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TipInfo extends ProtoAdapter<TipInfo> {
    public ProtoAdapter_TipInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, TipInfo.class, "type.googleapis.com/haiyaa.TipInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(TipInfo value) {
      int result = 0;
      if (!Objects.equals(value.text, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.text);
      }
      if (!Objects.equals(value.des, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.des);
      }
      if (!Objects.equals(value.extInfo, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.extInfo);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, TipInfo value) throws IOException {
      if (!Objects.equals(value.text, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.text);
      if (!Objects.equals(value.des, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.des);
      if (!Objects.equals(value.extInfo, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.extInfo);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TipInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.text(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.des(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.extInfo(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public TipInfo redact(TipInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
