// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.TradeCancel in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 取消支付
 */
public final class TradeCancel extends Message<TradeCancel, TradeCancel.Builder> {
  public static final ProtoAdapter<TradeCancel> ADAPTER = new ProtoAdapter_TradeCancel();

  private static final long serialVersionUID = 0L;

  /**
   * 订单Id
   */
  public String orderId;

  /**
   * 备注
   */
  public String reason;

  public TradeCancel(String orderId, String reason) {
    this(orderId, reason, ByteString.EMPTY);
  }

  public TradeCancel(String orderId, String reason, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (orderId == null) {
      throw new IllegalArgumentException("orderId == null");
    }
    this.orderId = orderId;
    if (reason == null) {
      throw new IllegalArgumentException("reason == null");
    }
    this.reason = reason;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.orderId = orderId;
    builder.reason = reason;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (orderId != null) builder.append(", orderId=").append(Internal.sanitize(orderId));
    if (reason != null) builder.append(", reason=").append(Internal.sanitize(reason));
    return builder.replace(0, 2, "TradeCancel{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TradeCancel, Builder> {
    public String orderId;

    public String reason;

    public Builder() {
      orderId = "";
      reason = "";
    }

    /**
     * 订单Id
     */
    public Builder orderId(String orderId) {
      this.orderId = orderId;
      return this;
    }

    /**
     * 备注
     */
    public Builder reason(String reason) {
      this.reason = reason;
      return this;
    }

    @Override
    public TradeCancel build() {
      return new TradeCancel(orderId, reason, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TradeCancel extends ProtoAdapter<TradeCancel> {
    public ProtoAdapter_TradeCancel() {
      super(FieldEncoding.LENGTH_DELIMITED, TradeCancel.class, "type.googleapis.com/haiyaa.TradeCancel", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(TradeCancel value) {
      int result = 0;
      if (!Objects.equals(value.orderId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.orderId);
      }
      if (!Objects.equals(value.reason, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.reason);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, TradeCancel value) throws IOException {
      if (!Objects.equals(value.orderId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.orderId);
      if (!Objects.equals(value.reason, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.reason);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TradeCancel decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.orderId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.reason(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public TradeCancel redact(TradeCancel value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
