// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.TradePay in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 创建订单
 */
public final class TradePay extends Message<TradePay, TradePay.Builder> {
  public static final ProtoAdapter<TradePay> ADAPTER = new ProtoAdapter_TradePay();

  private static final long serialVersionUID = 0L;

  /**
   * 商品Id
   */
  public String productId;

  public TradePay(String productId) {
    this(productId, ByteString.EMPTY);
  }

  public TradePay(String productId, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (productId == null) {
      throw new IllegalArgumentException("productId == null");
    }
    this.productId = productId;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.productId = productId;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (productId != null) builder.append(", productId=").append(Internal.sanitize(productId));
    return builder.replace(0, 2, "TradePay{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TradePay, Builder> {
    public String productId;

    public Builder() {
      productId = "";
    }

    /**
     * 商品Id
     */
    public Builder productId(String productId) {
      this.productId = productId;
      return this;
    }

    @Override
    public TradePay build() {
      return new TradePay(productId, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TradePay extends ProtoAdapter<TradePay> {
    public ProtoAdapter_TradePay() {
      super(FieldEncoding.LENGTH_DELIMITED, TradePay.class, "type.googleapis.com/haiyaa.TradePay", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(TradePay value) {
      int result = 0;
      if (!Objects.equals(value.productId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.productId);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, TradePay value) throws IOException {
      if (!Objects.equals(value.productId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.productId);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TradePay decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.productId(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public TradePay redact(TradePay value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
