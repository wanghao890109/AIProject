// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.TradeReceipt in pay.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 支付完成验签加钻
 */
public final class TradeReceipt extends Message<TradeReceipt, TradeReceipt.Builder> {
  public static final ProtoAdapter<TradeReceipt> ADAPTER = new ProtoAdapter_TradeReceipt();

  private static final long serialVersionUID = 0L;

  /**
   * 订单Id
   */
  public String orderId;

  /**
   * 商品Id
   */
  public String productId;

  /**
   * apple-receipt / goole-token
   */
  public String receipt;

  public TradeReceipt(String orderId, String productId, String receipt) {
    this(orderId, productId, receipt, ByteString.EMPTY);
  }

  public TradeReceipt(String orderId, String productId, String receipt, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (orderId == null) {
      throw new IllegalArgumentException("orderId == null");
    }
    this.orderId = orderId;
    if (productId == null) {
      throw new IllegalArgumentException("productId == null");
    }
    this.productId = productId;
    if (receipt == null) {
      throw new IllegalArgumentException("receipt == null");
    }
    this.receipt = receipt;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.orderId = orderId;
    builder.productId = productId;
    builder.receipt = receipt;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (orderId != null) builder.append(", orderId=").append(Internal.sanitize(orderId));
    if (productId != null) builder.append(", productId=").append(Internal.sanitize(productId));
    if (receipt != null) builder.append(", receipt=").append(Internal.sanitize(receipt));
    return builder.replace(0, 2, "TradeReceipt{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TradeReceipt, Builder> {
    public String orderId;

    public String productId;

    public String receipt;

    public Builder() {
      orderId = "";
      productId = "";
      receipt = "";
    }

    /**
     * 订单Id
     */
    public Builder orderId(String orderId) {
      this.orderId = orderId;
      return this;
    }

    /**
     * 商品Id
     */
    public Builder productId(String productId) {
      this.productId = productId;
      return this;
    }

    /**
     * apple-receipt / goole-token
     */
    public Builder receipt(String receipt) {
      this.receipt = receipt;
      return this;
    }

    @Override
    public TradeReceipt build() {
      return new TradeReceipt(orderId, productId, receipt, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TradeReceipt extends ProtoAdapter<TradeReceipt> {
    public ProtoAdapter_TradeReceipt() {
      super(FieldEncoding.LENGTH_DELIMITED, TradeReceipt.class, "type.googleapis.com/haiyaa.TradeReceipt", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(TradeReceipt value) {
      int result = 0;
      if (!Objects.equals(value.orderId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.orderId);
      }
      if (!Objects.equals(value.productId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.productId);
      }
      if (!Objects.equals(value.receipt, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.receipt);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, TradeReceipt value) throws IOException {
      if (!Objects.equals(value.orderId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.orderId);
      if (!Objects.equals(value.productId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.productId);
      if (!Objects.equals(value.receipt, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.receipt);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TradeReceipt decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.orderId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.productId(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.receipt(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public TradeReceipt redact(TradeReceipt value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
