// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.TxSig in game.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 游戏频道频道列表
 */
public final class TxSig extends Message<TxSig, TxSig.Builder> {
  public static final ProtoAdapter<TxSig> ADAPTER = new ProtoAdapter_TxSig();

  private static final long serialVersionUID = 0L;

  public String appId;

  public String txSig;

  public TxSig(String appId, String txSig) {
    this(appId, txSig, ByteString.EMPTY);
  }

  public TxSig(String appId, String txSig, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (appId == null) {
      throw new IllegalArgumentException("appId == null");
    }
    this.appId = appId;
    if (txSig == null) {
      throw new IllegalArgumentException("txSig == null");
    }
    this.txSig = txSig;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.appId = appId;
    builder.txSig = txSig;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (appId != null) builder.append(", appId=").append(Internal.sanitize(appId));
    if (txSig != null) builder.append(", txSig=").append(Internal.sanitize(txSig));
    return builder.replace(0, 2, "TxSig{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<TxSig, Builder> {
    public String appId;

    public String txSig;

    public Builder() {
      appId = "";
      txSig = "";
    }

    public Builder appId(String appId) {
      this.appId = appId;
      return this;
    }

    public Builder txSig(String txSig) {
      this.txSig = txSig;
      return this;
    }

    @Override
    public TxSig build() {
      return new TxSig(appId, txSig, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_TxSig extends ProtoAdapter<TxSig> {
    public ProtoAdapter_TxSig() {
      super(FieldEncoding.LENGTH_DELIMITED, TxSig.class, "type.googleapis.com/haiyaa.TxSig", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(TxSig value) {
      int result = 0;
      if (!Objects.equals(value.appId, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.appId);
      }
      if (!Objects.equals(value.txSig, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.txSig);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, TxSig value) throws IOException {
      if (!Objects.equals(value.appId, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.appId);
      if (!Objects.equals(value.txSig, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.txSig);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public TxSig decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.appId(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.txSig(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public TxSig redact(TxSig value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
