// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UpgradeInfo in login.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class UpgradeInfo extends Message<UpgradeInfo, UpgradeInfo.Builder> {
  public static final ProtoAdapter<UpgradeInfo> ADAPTER = new ProtoAdapter_UpgradeInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 发布时间
   */
  public long pubTime;

  /**
   * 升级状态，1:最新版本 2:不做任何提示 3:提示升级 4:强制升级
   */
  public int status;

  /**
   * 更新提示文案
   */
  public String tips;

  /**
   * 下载地址
   */
  public String downUrl;

  /**
   * 包大小
   */
  public String apkSize;

  /**
   * 升级到的版本
   */
  public String version;

  /**
   * 包的md5
   */
  public String md5;

  /**
   * version + build 共同判断一个版本
   */
  public String buildNo;

  public UpgradeInfo(long pubTime, int status, String tips, String downUrl, String apkSize,
      String version, String md5, String buildNo) {
    this(pubTime, status, tips, downUrl, apkSize, version, md5, buildNo, ByteString.EMPTY);
  }

  public UpgradeInfo(long pubTime, int status, String tips, String downUrl, String apkSize,
      String version, String md5, String buildNo, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.pubTime = pubTime;
    this.status = status;
    if (tips == null) {
      throw new IllegalArgumentException("tips == null");
    }
    this.tips = tips;
    if (downUrl == null) {
      throw new IllegalArgumentException("downUrl == null");
    }
    this.downUrl = downUrl;
    if (apkSize == null) {
      throw new IllegalArgumentException("apkSize == null");
    }
    this.apkSize = apkSize;
    if (version == null) {
      throw new IllegalArgumentException("version == null");
    }
    this.version = version;
    if (md5 == null) {
      throw new IllegalArgumentException("md5 == null");
    }
    this.md5 = md5;
    if (buildNo == null) {
      throw new IllegalArgumentException("buildNo == null");
    }
    this.buildNo = buildNo;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.pubTime = pubTime;
    builder.status = status;
    builder.tips = tips;
    builder.downUrl = downUrl;
    builder.apkSize = apkSize;
    builder.version = version;
    builder.md5 = md5;
    builder.buildNo = buildNo;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", pubTime=").append(pubTime);
    builder.append(", status=").append(status);
    if (tips != null) builder.append(", tips=").append(Internal.sanitize(tips));
    if (downUrl != null) builder.append(", downUrl=").append(Internal.sanitize(downUrl));
    if (apkSize != null) builder.append(", apkSize=").append(Internal.sanitize(apkSize));
    if (version != null) builder.append(", version=").append(Internal.sanitize(version));
    if (md5 != null) builder.append(", md5=").append(Internal.sanitize(md5));
    if (buildNo != null) builder.append(", buildNo=").append(Internal.sanitize(buildNo));
    return builder.replace(0, 2, "UpgradeInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UpgradeInfo, Builder> {
    public long pubTime;

    public int status;

    public String tips;

    public String downUrl;

    public String apkSize;

    public String version;

    public String md5;

    public String buildNo;

    public Builder() {
      pubTime = 0L;
      status = 0;
      tips = "";
      downUrl = "";
      apkSize = "";
      version = "";
      md5 = "";
      buildNo = "";
    }

    /**
     * 发布时间
     */
    public Builder pubTime(long pubTime) {
      this.pubTime = pubTime;
      return this;
    }

    /**
     * 升级状态，1:最新版本 2:不做任何提示 3:提示升级 4:强制升级
     */
    public Builder status(int status) {
      this.status = status;
      return this;
    }

    /**
     * 更新提示文案
     */
    public Builder tips(String tips) {
      this.tips = tips;
      return this;
    }

    /**
     * 下载地址
     */
    public Builder downUrl(String downUrl) {
      this.downUrl = downUrl;
      return this;
    }

    /**
     * 包大小
     */
    public Builder apkSize(String apkSize) {
      this.apkSize = apkSize;
      return this;
    }

    /**
     * 升级到的版本
     */
    public Builder version(String version) {
      this.version = version;
      return this;
    }

    /**
     * 包的md5
     */
    public Builder md5(String md5) {
      this.md5 = md5;
      return this;
    }

    /**
     * version + build 共同判断一个版本
     */
    public Builder buildNo(String buildNo) {
      this.buildNo = buildNo;
      return this;
    }

    @Override
    public UpgradeInfo build() {
      return new UpgradeInfo(pubTime, status, tips, downUrl, apkSize, version, md5, buildNo, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UpgradeInfo extends ProtoAdapter<UpgradeInfo> {
    public ProtoAdapter_UpgradeInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, UpgradeInfo.class, "type.googleapis.com/haiyaa.UpgradeInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UpgradeInfo value) {
      int result = 0;
      if (!Objects.equals(value.pubTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(1, value.pubTime);
      }
      if (!Objects.equals(value.status, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.status);
      }
      if (!Objects.equals(value.tips, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.tips);
      }
      if (!Objects.equals(value.downUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(4, value.downUrl);
      }
      if (!Objects.equals(value.apkSize, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(5, value.apkSize);
      }
      if (!Objects.equals(value.version, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(6, value.version);
      }
      if (!Objects.equals(value.md5, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(7, value.md5);
      }
      if (!Objects.equals(value.buildNo, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(8, value.buildNo);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UpgradeInfo value) throws IOException {
      if (!Objects.equals(value.pubTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 1, value.pubTime);
      if (!Objects.equals(value.status, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.status);
      if (!Objects.equals(value.tips, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.tips);
      if (!Objects.equals(value.downUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 4, value.downUrl);
      if (!Objects.equals(value.apkSize, "")) ProtoAdapter.STRING.encodeWithTag(writer, 5, value.apkSize);
      if (!Objects.equals(value.version, "")) ProtoAdapter.STRING.encodeWithTag(writer, 6, value.version);
      if (!Objects.equals(value.md5, "")) ProtoAdapter.STRING.encodeWithTag(writer, 7, value.md5);
      if (!Objects.equals(value.buildNo, "")) ProtoAdapter.STRING.encodeWithTag(writer, 8, value.buildNo);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UpgradeInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.pubTime(ProtoAdapter.INT64.decode(reader)); break;
          case 2: builder.status(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.tips(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.downUrl(ProtoAdapter.STRING.decode(reader)); break;
          case 5: builder.apkSize(ProtoAdapter.STRING.decode(reader)); break;
          case 6: builder.version(ProtoAdapter.STRING.decode(reader)); break;
          case 7: builder.md5(ProtoAdapter.STRING.decode(reader)); break;
          case 8: builder.buildNo(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UpgradeInfo redact(UpgradeInfo value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
