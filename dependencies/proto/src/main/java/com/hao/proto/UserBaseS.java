// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserBaseS in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class UserBaseS extends Message<UserBaseS, UserBaseS.Builder> {
  public static final ProtoAdapter<UserBaseS> ADAPTER = new ProtoAdapter_UserBaseS();

  private static final long serialVersionUID = 0L;

  public List<UserBase> ubase;

  public UserBaseS(List<UserBase> ubase) {
    this(ubase, ByteString.EMPTY);
  }

  public UserBaseS(List<UserBase> ubase, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.ubase = Internal.immutableCopyOf("ubase", ubase);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.ubase = Internal.copyOf(ubase);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!ubase.isEmpty()) builder.append(", ubase=").append(ubase);
    return builder.replace(0, 2, "UserBaseS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserBaseS, Builder> {
    public List<UserBase> ubase;

    public Builder() {
      ubase = Internal.newMutableList();
    }

    public Builder ubase(List<UserBase> ubase) {
      Internal.checkElementsNotNull(ubase);
      this.ubase = ubase;
      return this;
    }

    @Override
    public UserBaseS build() {
      return new UserBaseS(ubase, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserBaseS extends ProtoAdapter<UserBaseS> {
    public ProtoAdapter_UserBaseS() {
      super(FieldEncoding.LENGTH_DELIMITED, UserBaseS.class, "type.googleapis.com/haiyaa.UserBaseS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserBaseS value) {
      int result = 0;
      result += UserBase.ADAPTER.asRepeated().encodedSizeWithTag(1, value.ubase);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserBaseS value) throws IOException {
      UserBase.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.ubase);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserBaseS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.ubase.add(UserBase.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserBaseS redact(UserBaseS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.ubase, UserBase.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
