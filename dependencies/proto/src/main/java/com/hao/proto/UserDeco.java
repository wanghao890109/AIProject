// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserDeco in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

/**
 * 用户状态(装饰)和道具(背包系统),GiftInfo->DecoInfo, 勋章，这里需要重新调整
 */
public final class UserDeco extends Message<UserDeco, UserDeco.Builder> {
  public static final ProtoAdapter<UserDeco> ADAPTER = new ProtoAdapter_UserDeco();

  private static final long serialVersionUID = 0L;

  public long userId;

  public List<DecoInUse> decos;

  public UserDeco(long userId, List<DecoInUse> decos) {
    this(userId, decos, ByteString.EMPTY);
  }

  public UserDeco(long userId, List<DecoInUse> decos, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.decos = Internal.immutableCopyOf("decos", decos);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.decos = Internal.copyOf(decos);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    if (!decos.isEmpty()) builder.append(", decos=").append(decos);
    return builder.replace(0, 2, "UserDeco{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserDeco, Builder> {
    public long userId;

    public List<DecoInUse> decos;

    public Builder() {
      userId = 0L;
      decos = Internal.newMutableList();
    }

    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    public Builder decos(List<DecoInUse> decos) {
      Internal.checkElementsNotNull(decos);
      this.decos = decos;
      return this;
    }

    @Override
    public UserDeco build() {
      return new UserDeco(userId, decos, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserDeco extends ProtoAdapter<UserDeco> {
    public ProtoAdapter_UserDeco() {
      super(FieldEncoding.LENGTH_DELIMITED, UserDeco.class, "type.googleapis.com/haiyaa.UserDeco", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserDeco value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      result += DecoInUse.ADAPTER.asRepeated().encodedSizeWithTag(2, value.decos);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserDeco value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      DecoInUse.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.decos);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserDeco decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.decos.add(DecoInUse.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserDeco redact(UserDeco value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.decos, DecoInUse.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
