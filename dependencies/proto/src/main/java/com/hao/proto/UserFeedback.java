// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserFeedback in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class UserFeedback extends Message<UserFeedback, UserFeedback.Builder> {
  public static final ProtoAdapter<UserFeedback> ADAPTER = new ProtoAdapter_UserFeedback();

  private static final long serialVersionUID = 0L;

  /**
   * 反馈内容,建议或意见
   */
  public String content;

  /**
   * 反馈的图片
   */
  public String imgurl;

  /**
   * 反馈人的头像
   */
  public UserBase user;

  /**
   * 反馈时间
   */
  public long ctTime;

  public UserFeedback(String content, String imgurl, UserBase user, long ctTime) {
    this(content, imgurl, user, ctTime, ByteString.EMPTY);
  }

  public UserFeedback(String content, String imgurl, UserBase user, long ctTime,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (content == null) {
      throw new IllegalArgumentException("content == null");
    }
    this.content = content;
    if (imgurl == null) {
      throw new IllegalArgumentException("imgurl == null");
    }
    this.imgurl = imgurl;
    this.user = user;
    this.ctTime = ctTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.content = content;
    builder.imgurl = imgurl;
    builder.user = user;
    builder.ctTime = ctTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (content != null) builder.append(", content=").append(Internal.sanitize(content));
    if (imgurl != null) builder.append(", imgurl=").append(Internal.sanitize(imgurl));
    if (user != null) builder.append(", user=").append(user);
    builder.append(", ctTime=").append(ctTime);
    return builder.replace(0, 2, "UserFeedback{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserFeedback, Builder> {
    public String content;

    public String imgurl;

    public UserBase user;

    public long ctTime;

    public Builder() {
      content = "";
      imgurl = "";
      ctTime = 0L;
    }

    /**
     * 反馈内容,建议或意见
     */
    public Builder content(String content) {
      this.content = content;
      return this;
    }

    /**
     * 反馈的图片
     */
    public Builder imgurl(String imgurl) {
      this.imgurl = imgurl;
      return this;
    }

    /**
     * 反馈人的头像
     */
    public Builder user(UserBase user) {
      this.user = user;
      return this;
    }

    /**
     * 反馈时间
     */
    public Builder ctTime(long ctTime) {
      this.ctTime = ctTime;
      return this;
    }

    @Override
    public UserFeedback build() {
      return new UserFeedback(content, imgurl, user, ctTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserFeedback extends ProtoAdapter<UserFeedback> {
    public ProtoAdapter_UserFeedback() {
      super(FieldEncoding.LENGTH_DELIMITED, UserFeedback.class, "type.googleapis.com/haiyaa.UserFeedback", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserFeedback value) {
      int result = 0;
      if (!Objects.equals(value.content, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.content);
      }
      if (!Objects.equals(value.imgurl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.imgurl);
      }
      if (!Objects.equals(value.user, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(3, value.user);
      }
      if (!Objects.equals(value.ctTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(4, value.ctTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserFeedback value) throws IOException {
      if (!Objects.equals(value.content, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.content);
      if (!Objects.equals(value.imgurl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.imgurl);
      if (!Objects.equals(value.user, null)) UserBase.ADAPTER.encodeWithTag(writer, 3, value.user);
      if (!Objects.equals(value.ctTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 4, value.ctTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserFeedback decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.content(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.imgurl(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.user(UserBase.ADAPTER.decode(reader)); break;
          case 4: builder.ctTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserFeedback redact(UserFeedback value) {
      Builder builder = value.newBuilder();
      if (builder.user != null) builder.user = UserBase.ADAPTER.redact(builder.user);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
