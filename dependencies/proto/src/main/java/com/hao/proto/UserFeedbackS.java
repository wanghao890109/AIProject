// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserFeedbackS in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class UserFeedbackS extends Message<UserFeedbackS, UserFeedbackS.Builder> {
  public static final ProtoAdapter<UserFeedbackS> ADAPTER = new ProtoAdapter_UserFeedbackS();

  private static final long serialVersionUID = 0L;

  public List<UserFeedback> feeds;

  public UserFeedbackS(List<UserFeedback> feeds) {
    this(feeds, ByteString.EMPTY);
  }

  public UserFeedbackS(List<UserFeedback> feeds, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.feeds = Internal.immutableCopyOf("feeds", feeds);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.feeds = Internal.copyOf(feeds);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!feeds.isEmpty()) builder.append(", feeds=").append(feeds);
    return builder.replace(0, 2, "UserFeedbackS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserFeedbackS, Builder> {
    public List<UserFeedback> feeds;

    public Builder() {
      feeds = Internal.newMutableList();
    }

    public Builder feeds(List<UserFeedback> feeds) {
      Internal.checkElementsNotNull(feeds);
      this.feeds = feeds;
      return this;
    }

    @Override
    public UserFeedbackS build() {
      return new UserFeedbackS(feeds, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserFeedbackS extends ProtoAdapter<UserFeedbackS> {
    public ProtoAdapter_UserFeedbackS() {
      super(FieldEncoding.LENGTH_DELIMITED, UserFeedbackS.class, "type.googleapis.com/haiyaa.UserFeedbackS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserFeedbackS value) {
      int result = 0;
      result += UserFeedback.ADAPTER.asRepeated().encodedSizeWithTag(1, value.feeds);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserFeedbackS value) throws IOException {
      UserFeedback.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.feeds);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserFeedbackS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.feeds.add(UserFeedback.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserFeedbackS redact(UserFeedbackS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.feeds, UserFeedback.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
