// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserInfo in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class UserInfo extends Message<UserInfo, UserInfo.Builder> {
  public static final ProtoAdapter<UserInfo> ADAPTER = new ProtoAdapter_UserInfo();

  private static final long serialVersionUID = 0L;

  /**
   * 账号Id
   */
  public long userId;

  /**
   * 客态 + 主态
   */
  public UserBase userBase;

  /**
   * 勋章类
   */
  public DecoInfoS userDeco;

  /**
   * 客态
   */
  public C2CInfo userC2C;

  /**
   * 客态 + 主态
   */
  public SocialInfo userSocial;

  /**
   * 主态
   */
  public WelthInfo userWelth;

  /**
   * 主态
   */
  public RegisterInfo userReg;

  public UserInfo(long userId, UserBase userBase, DecoInfoS userDeco, C2CInfo userC2C,
      SocialInfo userSocial, WelthInfo userWelth, RegisterInfo userReg) {
    this(userId, userBase, userDeco, userC2C, userSocial, userWelth, userReg, ByteString.EMPTY);
  }

  public UserInfo(long userId, UserBase userBase, DecoInfoS userDeco, C2CInfo userC2C,
      SocialInfo userSocial, WelthInfo userWelth, RegisterInfo userReg, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.userBase = userBase;
    this.userDeco = userDeco;
    this.userC2C = userC2C;
    this.userSocial = userSocial;
    this.userWelth = userWelth;
    this.userReg = userReg;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.userBase = userBase;
    builder.userDeco = userDeco;
    builder.userC2C = userC2C;
    builder.userSocial = userSocial;
    builder.userWelth = userWelth;
    builder.userReg = userReg;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    if (userBase != null) builder.append(", userBase=").append(userBase);
    if (userDeco != null) builder.append(", userDeco=").append(userDeco);
    if (userC2C != null) builder.append(", userC2C=").append(userC2C);
    if (userSocial != null) builder.append(", userSocial=").append(userSocial);
    if (userWelth != null) builder.append(", userWelth=").append(userWelth);
    if (userReg != null) builder.append(", userReg=").append(userReg);
    return builder.replace(0, 2, "UserInfo{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserInfo, Builder> {
    public long userId;

    public UserBase userBase;

    public DecoInfoS userDeco;

    public C2CInfo userC2C;

    public SocialInfo userSocial;

    public WelthInfo userWelth;

    public RegisterInfo userReg;

    public Builder() {
      userId = 0L;
    }

    /**
     * 账号Id
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 客态 + 主态
     */
    public Builder userBase(UserBase userBase) {
      this.userBase = userBase;
      return this;
    }

    /**
     * 勋章类
     */
    public Builder userDeco(DecoInfoS userDeco) {
      this.userDeco = userDeco;
      return this;
    }

    /**
     * 客态
     */
    public Builder userC2C(C2CInfo userC2C) {
      this.userC2C = userC2C;
      return this;
    }

    /**
     * 客态 + 主态
     */
    public Builder userSocial(SocialInfo userSocial) {
      this.userSocial = userSocial;
      return this;
    }

    /**
     * 主态
     */
    public Builder userWelth(WelthInfo userWelth) {
      this.userWelth = userWelth;
      return this;
    }

    /**
     * 主态
     */
    public Builder userReg(RegisterInfo userReg) {
      this.userReg = userReg;
      return this;
    }

    @Override
    public UserInfo build() {
      return new UserInfo(userId, userBase, userDeco, userC2C, userSocial, userWelth, userReg, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserInfo extends ProtoAdapter<UserInfo> {
    public ProtoAdapter_UserInfo() {
      super(FieldEncoding.LENGTH_DELIMITED, UserInfo.class, "type.googleapis.com/haiyaa.UserInfo", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserInfo value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.userBase, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(2, value.userBase);
      }
      if (!Objects.equals(value.userDeco, null)) {
        result += DecoInfoS.ADAPTER.encodedSizeWithTag(3, value.userDeco);
      }
      if (!Objects.equals(value.userC2C, null)) {
        result += C2CInfo.ADAPTER.encodedSizeWithTag(5, value.userC2C);
      }
      if (!Objects.equals(value.userSocial, null)) {
        result += SocialInfo.ADAPTER.encodedSizeWithTag(6, value.userSocial);
      }
      if (!Objects.equals(value.userWelth, null)) {
        result += WelthInfo.ADAPTER.encodedSizeWithTag(7, value.userWelth);
      }
      if (!Objects.equals(value.userReg, null)) {
        result += RegisterInfo.ADAPTER.encodedSizeWithTag(8, value.userReg);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserInfo value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.userBase, null)) UserBase.ADAPTER.encodeWithTag(writer, 2, value.userBase);
      if (!Objects.equals(value.userDeco, null)) DecoInfoS.ADAPTER.encodeWithTag(writer, 3, value.userDeco);
      if (!Objects.equals(value.userC2C, null)) C2CInfo.ADAPTER.encodeWithTag(writer, 5, value.userC2C);
      if (!Objects.equals(value.userSocial, null)) SocialInfo.ADAPTER.encodeWithTag(writer, 6, value.userSocial);
      if (!Objects.equals(value.userWelth, null)) WelthInfo.ADAPTER.encodeWithTag(writer, 7, value.userWelth);
      if (!Objects.equals(value.userReg, null)) RegisterInfo.ADAPTER.encodeWithTag(writer, 8, value.userReg);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserInfo decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.userBase(UserBase.ADAPTER.decode(reader)); break;
          case 3: builder.userDeco(DecoInfoS.ADAPTER.decode(reader)); break;
          case 5: builder.userC2C(C2CInfo.ADAPTER.decode(reader)); break;
          case 6: builder.userSocial(SocialInfo.ADAPTER.decode(reader)); break;
          case 7: builder.userWelth(WelthInfo.ADAPTER.decode(reader)); break;
          case 8: builder.userReg(RegisterInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserInfo redact(UserInfo value) {
      Builder builder = value.newBuilder();
      if (builder.userBase != null) builder.userBase = UserBase.ADAPTER.redact(builder.userBase);
      if (builder.userDeco != null) builder.userDeco = DecoInfoS.ADAPTER.redact(builder.userDeco);
      if (builder.userC2C != null) builder.userC2C = C2CInfo.ADAPTER.redact(builder.userC2C);
      if (builder.userSocial != null) builder.userSocial = SocialInfo.ADAPTER.redact(builder.userSocial);
      if (builder.userWelth != null) builder.userWelth = WelthInfo.ADAPTER.redact(builder.userWelth);
      if (builder.userReg != null) builder.userReg = RegisterInfo.ADAPTER.redact(builder.userReg);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
