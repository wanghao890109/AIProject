// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserInfoS in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class UserInfoS extends Message<UserInfoS, UserInfoS.Builder> {
  public static final ProtoAdapter<UserInfoS> ADAPTER = new ProtoAdapter_UserInfoS();

  private static final long serialVersionUID = 0L;

  /**
   * 账号信息
   */
  public List<UserInfo> userInfos;

  public UserInfoS(List<UserInfo> userInfos) {
    this(userInfos, ByteString.EMPTY);
  }

  public UserInfoS(List<UserInfo> userInfos, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userInfos = Internal.immutableCopyOf("userInfos", userInfos);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userInfos = Internal.copyOf(userInfos);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!userInfos.isEmpty()) builder.append(", userInfos=").append(userInfos);
    return builder.replace(0, 2, "UserInfoS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserInfoS, Builder> {
    public List<UserInfo> userInfos;

    public Builder() {
      userInfos = Internal.newMutableList();
    }

    /**
     * 账号信息
     */
    public Builder userInfos(List<UserInfo> userInfos) {
      Internal.checkElementsNotNull(userInfos);
      this.userInfos = userInfos;
      return this;
    }

    @Override
    public UserInfoS build() {
      return new UserInfoS(userInfos, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserInfoS extends ProtoAdapter<UserInfoS> {
    public ProtoAdapter_UserInfoS() {
      super(FieldEncoding.LENGTH_DELIMITED, UserInfoS.class, "type.googleapis.com/haiyaa.UserInfoS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserInfoS value) {
      int result = 0;
      result += UserInfo.ADAPTER.asRepeated().encodedSizeWithTag(1, value.userInfos);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserInfoS value) throws IOException {
      UserInfo.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.userInfos);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserInfoS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userInfos.add(UserInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserInfoS redact(UserInfoS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.userInfos, UserInfo.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
