// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserInviteS in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class UserInviteS extends Message<UserInviteS, UserInviteS.Builder> {
  public static final ProtoAdapter<UserInviteS> ADAPTER = new ProtoAdapter_UserInviteS();

  private static final long serialVersionUID = 0L;

  /**
   * 邀请列表
   */
  public List<UserInvite> invites;

  public UserInviteS(List<UserInvite> invites) {
    this(invites, ByteString.EMPTY);
  }

  public UserInviteS(List<UserInvite> invites, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.invites = Internal.immutableCopyOf("invites", invites);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.invites = Internal.copyOf(invites);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!invites.isEmpty()) builder.append(", invites=").append(invites);
    return builder.replace(0, 2, "UserInviteS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserInviteS, Builder> {
    public List<UserInvite> invites;

    public Builder() {
      invites = Internal.newMutableList();
    }

    /**
     * 邀请列表
     */
    public Builder invites(List<UserInvite> invites) {
      Internal.checkElementsNotNull(invites);
      this.invites = invites;
      return this;
    }

    @Override
    public UserInviteS build() {
      return new UserInviteS(invites, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserInviteS extends ProtoAdapter<UserInviteS> {
    public ProtoAdapter_UserInviteS() {
      super(FieldEncoding.LENGTH_DELIMITED, UserInviteS.class, "type.googleapis.com/haiyaa.UserInviteS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserInviteS value) {
      int result = 0;
      result += UserInvite.ADAPTER.asRepeated().encodedSizeWithTag(1, value.invites);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserInviteS value) throws IOException {
      UserInvite.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.invites);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserInviteS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.invites.add(UserInvite.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserInviteS redact(UserInviteS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.invites, UserInvite.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
