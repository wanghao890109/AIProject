// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserRelation in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 不同区域，展示信息内容不一样，用于关系列表中
 */
public final class UserRelation extends Message<UserRelation, UserRelation.Builder> {
  public static final ProtoAdapter<UserRelation> ADAPTER = new ProtoAdapter_UserRelation();

  private static final long serialVersionUID = 0L;

  /**
   * 头像信息
   */
  public UserBase userBase;

  /**
   * C2CInfo   userC2C         = 2; 	// 关系信息
   * 今日是否已点过爱心
   */
  public boolean todaylove;

  /**
   * 用户关系 1 已关注我 2 我已关注 3 已互关，如果是互关好友，会有一定的爱心值
   */
  public int relation;

  /**
   * 关系值=亲密度，互关好友才有
   */
  public long relationVal;

  /**
   * 缔结关系时间
   */
  public long relationTime;

  public UserRelation(UserBase userBase, boolean todaylove, int relation, long relationVal,
      long relationTime) {
    this(userBase, todaylove, relation, relationVal, relationTime, ByteString.EMPTY);
  }

  public UserRelation(UserBase userBase, boolean todaylove, int relation, long relationVal,
      long relationTime, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userBase = userBase;
    this.todaylove = todaylove;
    this.relation = relation;
    this.relationVal = relationVal;
    this.relationTime = relationTime;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userBase = userBase;
    builder.todaylove = todaylove;
    builder.relation = relation;
    builder.relationVal = relationVal;
    builder.relationTime = relationTime;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (userBase != null) builder.append(", userBase=").append(userBase);
    builder.append(", todaylove=").append(todaylove);
    builder.append(", relation=").append(relation);
    builder.append(", relationVal=").append(relationVal);
    builder.append(", relationTime=").append(relationTime);
    return builder.replace(0, 2, "UserRelation{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserRelation, Builder> {
    public UserBase userBase;

    public boolean todaylove;

    public int relation;

    public long relationVal;

    public long relationTime;

    public Builder() {
      todaylove = false;
      relation = 0;
      relationVal = 0L;
      relationTime = 0L;
    }

    /**
     * 头像信息
     */
    public Builder userBase(UserBase userBase) {
      this.userBase = userBase;
      return this;
    }

    /**
     * C2CInfo   userC2C         = 2; 	// 关系信息
     * 今日是否已点过爱心
     */
    public Builder todaylove(boolean todaylove) {
      this.todaylove = todaylove;
      return this;
    }

    /**
     * 用户关系 1 已关注我 2 我已关注 3 已互关，如果是互关好友，会有一定的爱心值
     */
    public Builder relation(int relation) {
      this.relation = relation;
      return this;
    }

    /**
     * 关系值=亲密度，互关好友才有
     */
    public Builder relationVal(long relationVal) {
      this.relationVal = relationVal;
      return this;
    }

    /**
     * 缔结关系时间
     */
    public Builder relationTime(long relationTime) {
      this.relationTime = relationTime;
      return this;
    }

    @Override
    public UserRelation build() {
      return new UserRelation(userBase, todaylove, relation, relationVal, relationTime, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserRelation extends ProtoAdapter<UserRelation> {
    public ProtoAdapter_UserRelation() {
      super(FieldEncoding.LENGTH_DELIMITED, UserRelation.class, "type.googleapis.com/haiyaa.UserRelation", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserRelation value) {
      int result = 0;
      if (!Objects.equals(value.userBase, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(1, value.userBase);
      }
      if (!Objects.equals(value.todaylove, false)) {
        result += ProtoAdapter.BOOL.encodedSizeWithTag(2, value.todaylove);
      }
      if (!Objects.equals(value.relation, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.relation);
      }
      if (!Objects.equals(value.relationVal, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(4, value.relationVal);
      }
      if (!Objects.equals(value.relationTime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(5, value.relationTime);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserRelation value) throws IOException {
      if (!Objects.equals(value.userBase, null)) UserBase.ADAPTER.encodeWithTag(writer, 1, value.userBase);
      if (!Objects.equals(value.todaylove, false)) ProtoAdapter.BOOL.encodeWithTag(writer, 2, value.todaylove);
      if (!Objects.equals(value.relation, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.relation);
      if (!Objects.equals(value.relationVal, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 4, value.relationVal);
      if (!Objects.equals(value.relationTime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 5, value.relationTime);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserRelation decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userBase(UserBase.ADAPTER.decode(reader)); break;
          case 2: builder.todaylove(ProtoAdapter.BOOL.decode(reader)); break;
          case 3: builder.relation(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.relationVal(ProtoAdapter.INT64.decode(reader)); break;
          case 5: builder.relationTime(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserRelation redact(UserRelation value) {
      Builder builder = value.newBuilder();
      if (builder.userBase != null) builder.userBase = UserBase.ADAPTER.redact(builder.userBase);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
