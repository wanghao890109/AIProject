// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserReport in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class UserReport extends Message<UserReport, UserReport.Builder> {
  public static final ProtoAdapter<UserReport> ADAPTER = new ProtoAdapter_UserReport();

  private static final long serialVersionUID = 0L;

  /**
   * 被举报人
   */
  public long userId;

  /**
   * 举报类型
   */
  public int rpttype;

  /**
   * 举报描述
   */
  public String reason;

  /**
   * 举报人的头像
   */
  public UserBase fuser;

  /**
   * 被举报人的头像
   */
  public UserBase touser;

  /**
   * 反馈时间
   */
  public long rpttime;

  /**
   * 日志地址
   */
  public String logUrl;

  public UserReport(long userId, int rpttype, String reason, UserBase fuser, UserBase touser,
      long rpttime, String logUrl) {
    this(userId, rpttype, reason, fuser, touser, rpttime, logUrl, ByteString.EMPTY);
  }

  public UserReport(long userId, int rpttype, String reason, UserBase fuser, UserBase touser,
      long rpttime, String logUrl, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.rpttype = rpttype;
    if (reason == null) {
      throw new IllegalArgumentException("reason == null");
    }
    this.reason = reason;
    this.fuser = fuser;
    this.touser = touser;
    this.rpttime = rpttime;
    if (logUrl == null) {
      throw new IllegalArgumentException("logUrl == null");
    }
    this.logUrl = logUrl;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.rpttype = rpttype;
    builder.reason = reason;
    builder.fuser = fuser;
    builder.touser = touser;
    builder.rpttime = rpttime;
    builder.logUrl = logUrl;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    builder.append(", rpttype=").append(rpttype);
    if (reason != null) builder.append(", reason=").append(Internal.sanitize(reason));
    if (fuser != null) builder.append(", fuser=").append(fuser);
    if (touser != null) builder.append(", touser=").append(touser);
    builder.append(", rpttime=").append(rpttime);
    if (logUrl != null) builder.append(", logUrl=").append(Internal.sanitize(logUrl));
    return builder.replace(0, 2, "UserReport{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserReport, Builder> {
    public long userId;

    public int rpttype;

    public String reason;

    public UserBase fuser;

    public UserBase touser;

    public long rpttime;

    public String logUrl;

    public Builder() {
      userId = 0L;
      rpttype = 0;
      reason = "";
      rpttime = 0L;
      logUrl = "";
    }

    /**
     * 被举报人
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 举报类型
     */
    public Builder rpttype(int rpttype) {
      this.rpttype = rpttype;
      return this;
    }

    /**
     * 举报描述
     */
    public Builder reason(String reason) {
      this.reason = reason;
      return this;
    }

    /**
     * 举报人的头像
     */
    public Builder fuser(UserBase fuser) {
      this.fuser = fuser;
      return this;
    }

    /**
     * 被举报人的头像
     */
    public Builder touser(UserBase touser) {
      this.touser = touser;
      return this;
    }

    /**
     * 反馈时间
     */
    public Builder rpttime(long rpttime) {
      this.rpttime = rpttime;
      return this;
    }

    /**
     * 日志地址
     */
    public Builder logUrl(String logUrl) {
      this.logUrl = logUrl;
      return this;
    }

    @Override
    public UserReport build() {
      return new UserReport(userId, rpttype, reason, fuser, touser, rpttime, logUrl, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserReport extends ProtoAdapter<UserReport> {
    public ProtoAdapter_UserReport() {
      super(FieldEncoding.LENGTH_DELIMITED, UserReport.class, "type.googleapis.com/haiyaa.UserReport", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserReport value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.rpttype, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.rpttype);
      }
      if (!Objects.equals(value.reason, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(3, value.reason);
      }
      if (!Objects.equals(value.fuser, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(4, value.fuser);
      }
      if (!Objects.equals(value.touser, null)) {
        result += UserBase.ADAPTER.encodedSizeWithTag(5, value.touser);
      }
      if (!Objects.equals(value.rpttime, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(6, value.rpttime);
      }
      if (!Objects.equals(value.logUrl, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(7, value.logUrl);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserReport value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.rpttype, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.rpttype);
      if (!Objects.equals(value.reason, "")) ProtoAdapter.STRING.encodeWithTag(writer, 3, value.reason);
      if (!Objects.equals(value.fuser, null)) UserBase.ADAPTER.encodeWithTag(writer, 4, value.fuser);
      if (!Objects.equals(value.touser, null)) UserBase.ADAPTER.encodeWithTag(writer, 5, value.touser);
      if (!Objects.equals(value.rpttime, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 6, value.rpttime);
      if (!Objects.equals(value.logUrl, "")) ProtoAdapter.STRING.encodeWithTag(writer, 7, value.logUrl);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserReport decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.rpttype(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.reason(ProtoAdapter.STRING.decode(reader)); break;
          case 4: builder.fuser(UserBase.ADAPTER.decode(reader)); break;
          case 5: builder.touser(UserBase.ADAPTER.decode(reader)); break;
          case 6: builder.rpttime(ProtoAdapter.INT64.decode(reader)); break;
          case 7: builder.logUrl(ProtoAdapter.STRING.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserReport redact(UserReport value) {
      Builder builder = value.newBuilder();
      if (builder.fuser != null) builder.fuser = UserBase.ADAPTER.redact(builder.fuser);
      if (builder.touser != null) builder.touser = UserBase.ADAPTER.redact(builder.touser);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
