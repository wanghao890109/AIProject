// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserReportS in event.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class UserReportS extends Message<UserReportS, UserReportS.Builder> {
  public static final ProtoAdapter<UserReportS> ADAPTER = new ProtoAdapter_UserReportS();

  private static final long serialVersionUID = 0L;

  public List<UserReport> reports;

  public UserReportS(List<UserReport> reports) {
    this(reports, ByteString.EMPTY);
  }

  public UserReportS(List<UserReport> reports, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.reports = Internal.immutableCopyOf("reports", reports);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.reports = Internal.copyOf(reports);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!reports.isEmpty()) builder.append(", reports=").append(reports);
    return builder.replace(0, 2, "UserReportS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserReportS, Builder> {
    public List<UserReport> reports;

    public Builder() {
      reports = Internal.newMutableList();
    }

    public Builder reports(List<UserReport> reports) {
      Internal.checkElementsNotNull(reports);
      this.reports = reports;
      return this;
    }

    @Override
    public UserReportS build() {
      return new UserReportS(reports, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserReportS extends ProtoAdapter<UserReportS> {
    public ProtoAdapter_UserReportS() {
      super(FieldEncoding.LENGTH_DELIMITED, UserReportS.class, "type.googleapis.com/haiyaa.UserReportS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserReportS value) {
      int result = 0;
      result += UserReport.ADAPTER.asRepeated().encodedSizeWithTag(1, value.reports);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserReportS value) throws IOException {
      UserReport.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.reports);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserReportS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.reports.add(UserReport.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserReportS redact(UserReportS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.reports, UserReport.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
