// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserState in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class UserState extends Message<UserState, UserState.Builder> {
  public static final ProtoAdapter<UserState> ADAPTER = new ProtoAdapter_UserState();

  private static final long serialVersionUID = 0L;

  /**
   * 用户id
   */
  public long userId;

  /**
   * 在线状态, 1 在线，0 离线
   */
  public int online;

  /**
   * 所在房间，不在房间为0
   */
  public long roomId;

  /**
   * 所在游戏，不在游戏为0
   */
  public int gameId;

  /**
   * 游戏状态，Enum GameState
   */
  public int gameState;

  public UserState(long userId, int online, long roomId, int gameId, int gameState) {
    this(userId, online, roomId, gameId, gameState, ByteString.EMPTY);
  }

  public UserState(long userId, int online, long roomId, int gameId, int gameState,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.online = online;
    this.roomId = roomId;
    this.gameId = gameId;
    this.gameState = gameState;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.online = online;
    builder.roomId = roomId;
    builder.gameId = gameId;
    builder.gameState = gameState;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    builder.append(", online=").append(online);
    builder.append(", roomId=").append(roomId);
    builder.append(", gameId=").append(gameId);
    builder.append(", gameState=").append(gameState);
    return builder.replace(0, 2, "UserState{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserState, Builder> {
    public long userId;

    public int online;

    public long roomId;

    public int gameId;

    public int gameState;

    public Builder() {
      userId = 0L;
      online = 0;
      roomId = 0L;
      gameId = 0;
      gameState = 0;
    }

    /**
     * 用户id
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 在线状态, 1 在线，0 离线
     */
    public Builder online(int online) {
      this.online = online;
      return this;
    }

    /**
     * 所在房间，不在房间为0
     */
    public Builder roomId(long roomId) {
      this.roomId = roomId;
      return this;
    }

    /**
     * 所在游戏，不在游戏为0
     */
    public Builder gameId(int gameId) {
      this.gameId = gameId;
      return this;
    }

    /**
     * 游戏状态，Enum GameState
     */
    public Builder gameState(int gameState) {
      this.gameState = gameState;
      return this;
    }

    @Override
    public UserState build() {
      return new UserState(userId, online, roomId, gameId, gameState, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserState extends ProtoAdapter<UserState> {
    public ProtoAdapter_UserState() {
      super(FieldEncoding.LENGTH_DELIMITED, UserState.class, "type.googleapis.com/haiyaa.UserState", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserState value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.online, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.online);
      }
      if (!Objects.equals(value.roomId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(3, value.roomId);
      }
      if (!Objects.equals(value.gameId, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.gameId);
      }
      if (!Objects.equals(value.gameState, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(5, value.gameState);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserState value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.online, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.online);
      if (!Objects.equals(value.roomId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 3, value.roomId);
      if (!Objects.equals(value.gameId, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.gameId);
      if (!Objects.equals(value.gameState, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 5, value.gameState);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserState decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.online(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.roomId(ProtoAdapter.UINT64.decode(reader)); break;
          case 4: builder.gameId(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.gameState(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserState redact(UserState value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
