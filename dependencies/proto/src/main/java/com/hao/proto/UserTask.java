// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserTask in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 用户当前任务
 */
public final class UserTask extends Message<UserTask, UserTask.Builder> {
  public static final ProtoAdapter<UserTask> ADAPTER = new ProtoAdapter_UserTask();

  private static final long serialVersionUID = 0L;

  /**
   * 任务类型Icon
   */
  public String icon;

  /**
   * 任务名
   */
  public String taskName;

  /**
   * 任务值
   */
  public int taskValue;

  /**
   * 当前值
   */
  public int curValue;

  /**
   * 奖励积分
   */
  public int point;

  /**
   * 任务是否已完成 完成✔，未完成
   */
  public int finished;

  public UserTask(String icon, String taskName, int taskValue, int curValue, int point,
      int finished) {
    this(icon, taskName, taskValue, curValue, point, finished, ByteString.EMPTY);
  }

  public UserTask(String icon, String taskName, int taskValue, int curValue, int point,
      int finished, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (icon == null) {
      throw new IllegalArgumentException("icon == null");
    }
    this.icon = icon;
    if (taskName == null) {
      throw new IllegalArgumentException("taskName == null");
    }
    this.taskName = taskName;
    this.taskValue = taskValue;
    this.curValue = curValue;
    this.point = point;
    this.finished = finished;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.icon = icon;
    builder.taskName = taskName;
    builder.taskValue = taskValue;
    builder.curValue = curValue;
    builder.point = point;
    builder.finished = finished;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (icon != null) builder.append(", icon=").append(Internal.sanitize(icon));
    if (taskName != null) builder.append(", taskName=").append(Internal.sanitize(taskName));
    builder.append(", taskValue=").append(taskValue);
    builder.append(", curValue=").append(curValue);
    builder.append(", point=").append(point);
    builder.append(", finished=").append(finished);
    return builder.replace(0, 2, "UserTask{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserTask, Builder> {
    public String icon;

    public String taskName;

    public int taskValue;

    public int curValue;

    public int point;

    public int finished;

    public Builder() {
      icon = "";
      taskName = "";
      taskValue = 0;
      curValue = 0;
      point = 0;
      finished = 0;
    }

    /**
     * 任务类型Icon
     */
    public Builder icon(String icon) {
      this.icon = icon;
      return this;
    }

    /**
     * 任务名
     */
    public Builder taskName(String taskName) {
      this.taskName = taskName;
      return this;
    }

    /**
     * 任务值
     */
    public Builder taskValue(int taskValue) {
      this.taskValue = taskValue;
      return this;
    }

    /**
     * 当前值
     */
    public Builder curValue(int curValue) {
      this.curValue = curValue;
      return this;
    }

    /**
     * 奖励积分
     */
    public Builder point(int point) {
      this.point = point;
      return this;
    }

    /**
     * 任务是否已完成 完成✔，未完成
     */
    public Builder finished(int finished) {
      this.finished = finished;
      return this;
    }

    @Override
    public UserTask build() {
      return new UserTask(icon, taskName, taskValue, curValue, point, finished, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserTask extends ProtoAdapter<UserTask> {
    public ProtoAdapter_UserTask() {
      super(FieldEncoding.LENGTH_DELIMITED, UserTask.class, "type.googleapis.com/haiyaa.UserTask", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserTask value) {
      int result = 0;
      if (!Objects.equals(value.icon, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.icon);
      }
      if (!Objects.equals(value.taskName, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(2, value.taskName);
      }
      if (!Objects.equals(value.taskValue, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.taskValue);
      }
      if (!Objects.equals(value.curValue, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(4, value.curValue);
      }
      if (!Objects.equals(value.point, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(5, value.point);
      }
      if (!Objects.equals(value.finished, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(6, value.finished);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserTask value) throws IOException {
      if (!Objects.equals(value.icon, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.icon);
      if (!Objects.equals(value.taskName, "")) ProtoAdapter.STRING.encodeWithTag(writer, 2, value.taskName);
      if (!Objects.equals(value.taskValue, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.taskValue);
      if (!Objects.equals(value.curValue, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 4, value.curValue);
      if (!Objects.equals(value.point, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 5, value.point);
      if (!Objects.equals(value.finished, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 6, value.finished);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserTask decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.icon(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.taskName(ProtoAdapter.STRING.decode(reader)); break;
          case 3: builder.taskValue(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.curValue(ProtoAdapter.INT32.decode(reader)); break;
          case 5: builder.point(ProtoAdapter.INT32.decode(reader)); break;
          case 6: builder.finished(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserTask redact(UserTask value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
