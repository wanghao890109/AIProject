// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.UserTaskS in user.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import java.util.Objects;
import okio.ByteString;

public final class UserTaskS extends Message<UserTaskS, UserTaskS.Builder> {
  public static final ProtoAdapter<UserTaskS> ADAPTER = new ProtoAdapter_UserTaskS();

  private static final long serialVersionUID = 0L;

  /**
   * 每日任务
   */
  public List<UserTask> DayTask;

  /**
   * 每周任务
   */
  public List<UserTask> WeekTask;

  /**
   * 当前积分
   */
  public int DrawPoint;

  public UserTaskS(List<UserTask> DayTask, List<UserTask> WeekTask, int DrawPoint) {
    this(DayTask, WeekTask, DrawPoint, ByteString.EMPTY);
  }

  public UserTaskS(List<UserTask> DayTask, List<UserTask> WeekTask, int DrawPoint,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.DayTask = Internal.immutableCopyOf("DayTask", DayTask);
    this.WeekTask = Internal.immutableCopyOf("WeekTask", WeekTask);
    this.DrawPoint = DrawPoint;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.DayTask = Internal.copyOf(DayTask);
    builder.WeekTask = Internal.copyOf(WeekTask);
    builder.DrawPoint = DrawPoint;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!DayTask.isEmpty()) builder.append(", DayTask=").append(DayTask);
    if (!WeekTask.isEmpty()) builder.append(", WeekTask=").append(WeekTask);
    builder.append(", DrawPoint=").append(DrawPoint);
    return builder.replace(0, 2, "UserTaskS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<UserTaskS, Builder> {
    public List<UserTask> DayTask;

    public List<UserTask> WeekTask;

    public int DrawPoint;

    public Builder() {
      DayTask = Internal.newMutableList();
      WeekTask = Internal.newMutableList();
      DrawPoint = 0;
    }

    /**
     * 每日任务
     */
    public Builder DayTask(List<UserTask> DayTask) {
      Internal.checkElementsNotNull(DayTask);
      this.DayTask = DayTask;
      return this;
    }

    /**
     * 每周任务
     */
    public Builder WeekTask(List<UserTask> WeekTask) {
      Internal.checkElementsNotNull(WeekTask);
      this.WeekTask = WeekTask;
      return this;
    }

    /**
     * 当前积分
     */
    public Builder DrawPoint(int DrawPoint) {
      this.DrawPoint = DrawPoint;
      return this;
    }

    @Override
    public UserTaskS build() {
      return new UserTaskS(DayTask, WeekTask, DrawPoint, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_UserTaskS extends ProtoAdapter<UserTaskS> {
    public ProtoAdapter_UserTaskS() {
      super(FieldEncoding.LENGTH_DELIMITED, UserTaskS.class, "type.googleapis.com/haiyaa.UserTaskS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(UserTaskS value) {
      int result = 0;
      result += UserTask.ADAPTER.asRepeated().encodedSizeWithTag(1, value.DayTask);
      result += UserTask.ADAPTER.asRepeated().encodedSizeWithTag(2, value.WeekTask);
      if (!Objects.equals(value.DrawPoint, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.DrawPoint);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, UserTaskS value) throws IOException {
      UserTask.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.DayTask);
      UserTask.ADAPTER.asRepeated().encodeWithTag(writer, 2, value.WeekTask);
      if (!Objects.equals(value.DrawPoint, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.DrawPoint);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public UserTaskS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.DayTask.add(UserTask.ADAPTER.decode(reader)); break;
          case 2: builder.WeekTask.add(UserTask.ADAPTER.decode(reader)); break;
          case 3: builder.DrawPoint(ProtoAdapter.INT32.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public UserTaskS redact(UserTaskS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.DayTask, UserTask.ADAPTER);
      Internal.redactElements(builder.WeekTask, UserTask.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
