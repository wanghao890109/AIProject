// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.VersionQuery in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

/**
 * 需要区分版本逻辑的命令，如登录，三方登录
 */
public final class VersionQuery extends Message<VersionQuery, VersionQuery.Builder> {
  public static final ProtoAdapter<VersionQuery> ADAPTER = new ProtoAdapter_VersionQuery();

  private static final long serialVersionUID = 0L;

  /**
   * 查询参数
   */
  public long userId;

  /**
   * 分页参数
   */
  public int skip;

  /**
   * 分页参数
   */
  public int limit;

  /**
   * 设备信息
   */
  public DeviceInfo device;

  public VersionQuery(long userId, int skip, int limit, DeviceInfo device) {
    this(userId, skip, limit, device, ByteString.EMPTY);
  }

  public VersionQuery(long userId, int skip, int limit, DeviceInfo device,
      ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.userId = userId;
    this.skip = skip;
    this.limit = limit;
    this.device = device;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.userId = userId;
    builder.skip = skip;
    builder.limit = limit;
    builder.device = device;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    builder.append(", userId=").append(userId);
    builder.append(", skip=").append(skip);
    builder.append(", limit=").append(limit);
    if (device != null) builder.append(", device=").append(device);
    return builder.replace(0, 2, "VersionQuery{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<VersionQuery, Builder> {
    public long userId;

    public int skip;

    public int limit;

    public DeviceInfo device;

    public Builder() {
      userId = 0L;
      skip = 0;
      limit = 0;
    }

    /**
     * 查询参数
     */
    public Builder userId(long userId) {
      this.userId = userId;
      return this;
    }

    /**
     * 分页参数
     */
    public Builder skip(int skip) {
      this.skip = skip;
      return this;
    }

    /**
     * 分页参数
     */
    public Builder limit(int limit) {
      this.limit = limit;
      return this;
    }

    /**
     * 设备信息
     */
    public Builder device(DeviceInfo device) {
      this.device = device;
      return this;
    }

    @Override
    public VersionQuery build() {
      return new VersionQuery(userId, skip, limit, device, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_VersionQuery extends ProtoAdapter<VersionQuery> {
    public ProtoAdapter_VersionQuery() {
      super(FieldEncoding.LENGTH_DELIMITED, VersionQuery.class, "type.googleapis.com/haiyaa.VersionQuery", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(VersionQuery value) {
      int result = 0;
      if (!Objects.equals(value.userId, 0L)) {
        result += ProtoAdapter.UINT64.encodedSizeWithTag(1, value.userId);
      }
      if (!Objects.equals(value.skip, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(2, value.skip);
      }
      if (!Objects.equals(value.limit, 0)) {
        result += ProtoAdapter.INT32.encodedSizeWithTag(3, value.limit);
      }
      if (!Objects.equals(value.device, null)) {
        result += DeviceInfo.ADAPTER.encodedSizeWithTag(4, value.device);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, VersionQuery value) throws IOException {
      if (!Objects.equals(value.userId, 0L)) ProtoAdapter.UINT64.encodeWithTag(writer, 1, value.userId);
      if (!Objects.equals(value.skip, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 2, value.skip);
      if (!Objects.equals(value.limit, 0)) ProtoAdapter.INT32.encodeWithTag(writer, 3, value.limit);
      if (!Objects.equals(value.device, null)) DeviceInfo.ADAPTER.encodeWithTag(writer, 4, value.device);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public VersionQuery decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.userId(ProtoAdapter.UINT64.decode(reader)); break;
          case 2: builder.skip(ProtoAdapter.INT32.decode(reader)); break;
          case 3: builder.limit(ProtoAdapter.INT32.decode(reader)); break;
          case 4: builder.device(DeviceInfo.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public VersionQuery redact(VersionQuery value) {
      Builder builder = value.newBuilder();
      if (builder.device != null) builder.device = DeviceInfo.ADAPTER.redact(builder.device);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
