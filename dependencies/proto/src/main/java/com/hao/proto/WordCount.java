// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.WordCount in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.Objects;
import okio.ByteString;

public final class WordCount extends Message<WordCount, WordCount.Builder> {
  public static final ProtoAdapter<WordCount> ADAPTER = new ProtoAdapter_WordCount();

  private static final long serialVersionUID = 0L;

  /**
   * 关键字
   */
  public String wordKey;

  /**
   * 频次
   */
  public long hitNum;

  public WordCount(String wordKey, long hitNum) {
    this(wordKey, hitNum, ByteString.EMPTY);
  }

  public WordCount(String wordKey, long hitNum, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    if (wordKey == null) {
      throw new IllegalArgumentException("wordKey == null");
    }
    this.wordKey = wordKey;
    this.hitNum = hitNum;
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.wordKey = wordKey;
    builder.hitNum = hitNum;
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (wordKey != null) builder.append(", wordKey=").append(Internal.sanitize(wordKey));
    builder.append(", hitNum=").append(hitNum);
    return builder.replace(0, 2, "WordCount{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<WordCount, Builder> {
    public String wordKey;

    public long hitNum;

    public Builder() {
      wordKey = "";
      hitNum = 0L;
    }

    /**
     * 关键字
     */
    public Builder wordKey(String wordKey) {
      this.wordKey = wordKey;
      return this;
    }

    /**
     * 频次
     */
    public Builder hitNum(long hitNum) {
      this.hitNum = hitNum;
      return this;
    }

    @Override
    public WordCount build() {
      return new WordCount(wordKey, hitNum, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_WordCount extends ProtoAdapter<WordCount> {
    public ProtoAdapter_WordCount() {
      super(FieldEncoding.LENGTH_DELIMITED, WordCount.class, "type.googleapis.com/haiyaa.WordCount", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(WordCount value) {
      int result = 0;
      if (!Objects.equals(value.wordKey, "")) {
        result += ProtoAdapter.STRING.encodedSizeWithTag(1, value.wordKey);
      }
      if (!Objects.equals(value.hitNum, 0L)) {
        result += ProtoAdapter.INT64.encodedSizeWithTag(2, value.hitNum);
      }
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, WordCount value) throws IOException {
      if (!Objects.equals(value.wordKey, "")) ProtoAdapter.STRING.encodeWithTag(writer, 1, value.wordKey);
      if (!Objects.equals(value.hitNum, 0L)) ProtoAdapter.INT64.encodeWithTag(writer, 2, value.hitNum);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public WordCount decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.wordKey(ProtoAdapter.STRING.decode(reader)); break;
          case 2: builder.hitNum(ProtoAdapter.INT64.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public WordCount redact(WordCount value) {
      Builder builder = value.newBuilder();
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
