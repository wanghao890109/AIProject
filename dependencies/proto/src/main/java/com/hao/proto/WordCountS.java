// Code generated by Wire protocol buffer compiler, do not edit.
// Source: haiyaa.WordCountS in public.proto
package com.hao.proto;

import com.squareup.wire.FieldEncoding;
import com.squareup.wire.Message;
import com.squareup.wire.ProtoAdapter;
import com.squareup.wire.ProtoReader;
import com.squareup.wire.ProtoWriter;
import com.squareup.wire.Syntax;
import com.squareup.wire.internal.Internal;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.StringBuilder;
import java.util.List;
import okio.ByteString;

public final class WordCountS extends Message<WordCountS, WordCountS.Builder> {
  public static final ProtoAdapter<WordCountS> ADAPTER = new ProtoAdapter_WordCountS();

  private static final long serialVersionUID = 0L;

  public List<WordCount> words;

  public WordCountS(List<WordCount> words) {
    this(words, ByteString.EMPTY);
  }

  public WordCountS(List<WordCount> words, ByteString unknownFields) {
    super(ADAPTER, unknownFields);
    this.words = Internal.immutableCopyOf("words", words);
  }

  @Override
  public Builder newBuilder() {
    Builder builder = new Builder();
    builder.words = Internal.copyOf(words);
    builder.addUnknownFields(unknownFields());
    return builder;
  }

  @Override
  public String toString() {
    StringBuilder builder = new StringBuilder();
    if (!words.isEmpty()) builder.append(", words=").append(words);
    return builder.replace(0, 2, "WordCountS{").append('}').toString();
  }

  public static final class Builder extends Message.Builder<WordCountS, Builder> {
    public List<WordCount> words;

    public Builder() {
      words = Internal.newMutableList();
    }

    public Builder words(List<WordCount> words) {
      Internal.checkElementsNotNull(words);
      this.words = words;
      return this;
    }

    @Override
    public WordCountS build() {
      return new WordCountS(words, super.buildUnknownFields());
    }
  }

  private static final class ProtoAdapter_WordCountS extends ProtoAdapter<WordCountS> {
    public ProtoAdapter_WordCountS() {
      super(FieldEncoding.LENGTH_DELIMITED, WordCountS.class, "type.googleapis.com/haiyaa.WordCountS", Syntax.PROTO_3, null);
    }

    @Override
    public int encodedSize(WordCountS value) {
      int result = 0;
      result += WordCount.ADAPTER.asRepeated().encodedSizeWithTag(1, value.words);
      result += value.unknownFields().size();
      return result;
    }

    @Override
    public void encode(ProtoWriter writer, WordCountS value) throws IOException {
      WordCount.ADAPTER.asRepeated().encodeWithTag(writer, 1, value.words);
      writer.writeBytes(value.unknownFields());
    }

    @Override
    public WordCountS decode(ProtoReader reader) throws IOException {
      Builder builder = new Builder();
      long token = reader.beginMessage();
      for (int tag; (tag = reader.nextTag()) != -1;) {
        switch (tag) {
          case 1: builder.words.add(WordCount.ADAPTER.decode(reader)); break;
          default: {
            reader.readUnknownField(tag);
          }
        }
      }
      builder.addUnknownFields(reader.endMessageAndGetUnknownFields(token));
      return builder.build();
    }

    @Override
    public WordCountS redact(WordCountS value) {
      Builder builder = value.newBuilder();
      Internal.redactElements(builder.words, WordCount.ADAPTER);
      builder.clearUnknownFields();
      return builder.build();
    }
  }
}
